{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The VNC window talks only to this app's own commands; no plugin surface is granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
