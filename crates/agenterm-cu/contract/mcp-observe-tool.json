{
  "name": "agenterm_acu_observe",
  "title": "Run one observe-grant agenterm-cu command",
  "description": "Execute one canonical observe-grant agenterm-cu Command through the shared typed Executor. This broad compatibility surface includes caller-named artifact writes, persistent cursors and page JavaScript; commands requiring the actuate grant are rejected before dispatch.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "command": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
          "verb": { "type": "string", "minLength": 1 },
          "target": { "type": "string", "enum": ["current", "ssh", "vnc", "rdp"] }
        },
        "required": ["verb", "target"],
        "description": "One canonical agenterm-cu Command object. The provider strictly validates the exact variant fields selected by verb."
      }
    },
    "required": ["command"]
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "ok": { "type": "boolean" },
      "target": { "type": "string" },
      "command": { "type": "string" },
      "data": {},
      "error": {
        "type": "object",
        "properties": {
          "code": { "type": "string" },
          "message": { "type": "string" },
          "count": { "type": "integer", "minimum": 0 },
          "detail": {}
        },
        "required": ["code", "message"]
      }
    },
    "required": ["ok", "target", "command"]
  },
  "annotations": {
    "readOnlyHint": false,
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": true
  }
}
