# agenterm-cu verb reference

Generated by `scripts/gen-cu-verbs-doc.sh` from `agenterm-cu verbs --json` and
`agenterm-cu help <verb>`; do not edit by hand. The agent-facing quick guide is
`crates/agenterm-cu/README.md`; the design authority is `prd/PRD_02_29_cu_command_surface.md`.

## Index

| verb | scope | family | aliases | summary |
|---|---|---|---|---|
| `capabilities` | observe | system | `caps` | per-target capability matrix for this host |
| `setup` | actuate | system | `path-install` | inspect or refresh the stable launcher while preserving resident owners |
| `permissions` | actuate | system | — | inspect permission state or open one exact consent settings pane |
| `doctor` | observe | system | — | bounded host, permission and desktop-bridge health report |
| `host-open` | actuate | system | `open` | dispatch one path or URL to its registered host application |
| `host-notify` | actuate | system | `notify` | dispatch one bounded desktop notification |
| `audit-query` | observe | system | — | bounded newest-first control audit query |
| `audit-compact` | actuate | system | `audit compact` | plan or atomically apply bounded audit retention |
| `session-start` | actuate | system | `session create` | create one durable runtime lease |
| `session-list` | observe | system | `session list` | list bounded durable runtime leases |
| `session-status` | observe | system | `session status` | inspect one durable runtime lease |
| `session-renew` | actuate | system | `session renew` | renew one runtime lease with its private token |
| `session-end` | actuate | system | `session end` | end one runtime lease and release its locks |
| `lock-acquire` | actuate | system | `lock acquire` | acquire or renew one session-bound target lock |
| `lock-list` | observe | system | `lock list` | list active target locks |
| `lock-release` | actuate | system | `lock release` | release one target lock with its owner lease |
| `job-spawn` | actuate | system | `job spawn` | start one durable detached process under a resident owner |
| `job-adopt` | actuate | system | `job adopt` | adopt one exact current-user POSIX process group under a resident owner |
| `job-list` | observe | system | `job list` | list bounded durable managed-job records |
| `job-status` | observe | system | `job status` | inspect one managed-job identity and live state |
| `job-prune` | actuate | system | `job prune` | plan or apply bounded terminal managed-job receipt retention |
| `job-events` | observe | system | `job events` | read lossless cursor-based stdout and stderr pages |
| `job-resources` | observe | system | `job resources` | sample the managed job's native containment-group members and aggregates |
| `job-priority` | actuate | system | `job priority` | set and verify one owned Unix managed-job process group's nice value |
| `job-policy` | actuate | system | `job policy` | manage resident containment resource enforcement |
| `job-output` | observe | system | `job output` | read one bounded managed-job output stream |
| `job-write` | actuate | system | `job write` | write one bounded base64 chunk and optionally close stdin |
| `job-wait` | observe | system | `job wait` | wait boundedly for one managed job and verify exit |
| `job-set-state` | actuate | system | `job set-state` | set and verify the complete exact managed-job tree run state |
| `job-signal` | actuate | system | `job signal` | deliver one request-bound signal to the complete exact managed-job tree |
| `job-stop` | actuate | system | `job stop` | close stdin then force-stop one identity-bound job |
| `job-renew` | actuate | system | `job renew` | renew one resident managed-job lease |
| `network-interfaces` | observe | network | `network interfaces` | bounded native network interface address inventory |
| `network-probe` | observe | network | `network probe` | resolve once and measure bounded TCP reachability |
| `network-dns` | observe | network | `network dns` | bounded effective DNS resolver and search-domain inventory |
| `network-routes` | observe | network | `network routes` | bounded native network route-table inventory |
| `file-inspect` | observe | file | `file inspect` | inspect one final filesystem entry without following it |
| `file-watch` | observe | file | `file observe` | watch one directory for bounded native created/modified/removed events |
| `file-copy` | actuate | file | `file copy` | plan or apply one recoverable regular-file copy |
| `file-attributes` | observe | file | `file attributes` | inspect bounded extended-attribute metadata for one regular file |
| `file-mode` | actuate | file | `file chmod` | plan or apply one identity-bound Unix permission mode |
| `file-xattr-set` | actuate | file | `file xattr-set` | plan or apply one bounded extended-attribute value |
| `file-xattr-remove` | actuate | file | `file xattr-remove` | plan or apply removal of one extended attribute |
| `file-quarantine-clear` | actuate | file | `file quarantine-clear` | plan or apply removal of macOS quarantine metadata |
| `file-move` | actuate | file | `file move` | plan or apply one recoverable regular-file move |
| `file-transaction` | actuate | file | `file status`, `file rollback`, `file recover`, `file finalize` | inspect or advance one recoverable file-copy or file-move transaction |
| `windows` | observe | windows | `focused-window` | top-level window inventory, filtered and paged |
| `windows-watch` | observe | windows | — | poll-diff over the window inventory |
| `apps` | observe | windows | — | running apps from top-level windows; --all adds installed |
| `app-facts` | observe | windows | — | exact native application metadata facts |
| `app-watch` | observe | windows | `apps watch` | watch bounded application launch and quit transitions by native identity |
| `app-inspect` | observe | windows | — | classify every matching application window in one bounded call |
| `app-menu-inspect` | observe | a11y-observe | — | read one exact macOS application's menu bar without choosing a window |
| `app-menu-invoke` | actuate | a11y-actuate | — | press one exact path in a uniquely resolved macOS application menu |
| `ps` | observe | process | — | bounded process inventory, resource filtering, or one process tree with optional file/socket detail |
| `process-state` | observe | process | `process state` | observe one pid with a stable process-start identity |
| `process-argv` | observe | process | `process argv` | identity-bound bounded process arguments; values opt in |
| `process-cwd` | observe | process | `process cwd` | read one identity-bound process working directory |
| `process-environment` | observe | process | `process env` | identity-bound exec-time environment; values opt in |
| `process-usage` | observe | process | `process usage` | sample or watch identity-bound CPU, memory and page faults |
| `process-fds` | observe | process | `process fds` | identity-bound bounded descriptor inventory |
| `process-maps` | observe | process | `process maps` | identity-bound bounded memory-region inventory |
| `process-threads` | observe | process | `process threads` | identity-bound bounded native-thread inventory |
| `process-sockets` | observe | process | `process sockets` | identity-bound bounded native socket inventory |
| `process-cgroup` | observe | process | `process cgroup` | identity- and membership-bracketed Linux cgroup v2 snapshot |
| `process-wait` | observe | process | `process wait` | wait for one identity-bound process instance to exit |
| `process-kill` | actuate | process | `process kill`, `kill` | terminate one exact identity-bound process object |
| `privilege-plan` | observe | privilege | `privilege plan` | prepare an expiring identity-bound privileged-operation plan |
| `privilege-apply` | actuate | privilege | `privilege apply` | apply one typed plan through the fixed Linux system broker and native consent |
| `privilege-provider` | mixed | privilege | `privilege provider` | observe or explicitly change the fixed macOS privilege-provider lifecycle |
| `process-set-state` | actuate | process | `process set-state` | suspend or resume one exact identity-bound process object |
| `process-policy` | actuate | process | `process policy` | observe exact Darwin background flags and fail closed without exact mutation authority |
| `process-signal` | actuate | process | `process signal`, `signal` | deliver one closed signal through exact native process objects |
| `process-watch` | observe | process | `process watch` | watch a bounded, identity-safe process-set diff |
| `shell-exec` | actuate | process | — | run one bounded contained host-shell command |
| `pty-start` | actuate | terminal | `pty start` | start one durable headless PTY job |
| `pty-list` | observe | terminal | `pty list` | reconcile durable PTY names with live authorities |
| `pty-prune` | actuate | terminal | `pty prune` | reclaim one independently verified stale PTY state |
| `pty-status` | observe | terminal | `pty status` | inspect one durable headless PTY job |
| `pty-read` | observe | terminal | `pty read` | read a loss-aware raw-output page from a PTY job |
| `pty-snapshot` | observe | terminal | `pty snapshot` | snapshot and persist one exact durable PTY screen baseline |
| `pty-diff` | observe | terminal | `pty diff` | diff one durable PTY screen against an identity-bound baseline |
| `pty-events` | observe | terminal | `pty events` | continue one durable PTY job event cursor |
| `pty-resize` | actuate | terminal | `pty resize` | resize and verify one durable PTY job grid |
| `pty-send` | actuate | terminal | `pty send` | send exact UTF-8 bytes to a PTY job |
| `pty-wait` | observe | terminal | `pty wait` | wait for exact bytes or a bounded byte regex in retained PTY output |
| `pty-wait-exit` | observe | terminal | `pty wait-exit` | wait for a PTY job's drained exit receipt |
| `pty-signal` | actuate | terminal | `pty signal` | signal the exact native foreground group of one PTY job |
| `pty-stop` | actuate | terminal | `pty stop` | close one PTY job and its headless authority |
| `term-read` | observe | terminal | `term read` | read one exact external terminal window's bounded accessibility buffer |
| `term-send` | actuate | terminal | `term send` | send to one exact external terminal with independent buffer verification |
| `term-wait` | observe | terminal | `term wait` | wait for a regex in one exact external terminal without leaking timeout content |
| `terminal-list` | observe | terminal | `terminal list` | list AgenTerm-owned tabs with epoch-scoped stable ids |
| `terminal-new` | actuate | terminal | `terminal new` | create one AgenTerm-owned tab and verify its stable id |
| `terminal-close` | actuate | terminal | `terminal close` | close one exact AgenTerm tab with disappearance verification |
| `terminal-read` | observe | terminal | `terminal read` | read one bounded current-screen snapshot |
| `terminal-send` | actuate | terminal | `terminal send` | send exact UTF-8 bytes to one AgenTerm-owned tab |
| `terminal-snapshot` | observe | terminal | `terminal snapshot` | read a structured screen plus an exact event cursor |
| `terminal-scroll` | actuate | terminal | `terminal scroll` | scroll one exact AgenTerm tab's local viewport without terminal input |
| `terminal-screenshot` | observe | terminal | `terminal screenshot` | capture the already-active exact AgenTerm tab as a verified rendered PNG |
| `terminal-events` | observe | terminal | `terminal events` | continue one terminal's loss-aware event cursor |
| `terminal-output` | observe | terminal | `terminal output` | read retained raw bytes through a loss-aware cursor |
| `terminal-wait` | observe | terminal | `terminal wait` | wait for screen text, process exit, or fully drained exit |
| `app` | actuate | windows | `launch`, `quit`, `hide`, `show` | hide / show / quit / launch a whole application |
| `unlock` | observe | windows | — | ask the app to build its full a11y tree |
| `close` | actuate | windows | — | close one window through its own close control (gated) |
| `activate` | actuate | windows | — | make one exact window the desktop foreground owner |
| `raise` | actuate | windows | — | lift one window inside its own app's z-order |
| `minimize` | actuate | windows | — | minimize one window through its own affordance (gated) |
| `restore` | actuate | windows | — | un-minimize one window without activating its app (gated) |
| `receipts` | observe | windows | — | read the target's crash-persistent receipt file |
| `spaces` | observe | windows | — | macOS managed Space inventory (read-only) |
| `displays` | observe | windows | — | native screen frames |
| `tree` | observe | a11y-observe | `elements` | bounded a11y tree of a window or selected subtree; --flat numbers nodes |
| `desktop-state` | observe | a11y-observe | `state` | bounded coherent desktop observation without a screenshot |
| `query` | observe | a11y-observe | `inspect`, `find`, `read` | bounded filtered nodes, once or by poll-diff watch |
| `hit` | observe | a11y-observe | — | the a11y node under a screen point |
| `focused` | observe | a11y-observe | — | the app's own focused control inside the window |
| `observe` | observe | a11y-observe | — | bounded a11y event stream by poll-diff |
| `snapshot` | observe | a11y-observe | — | name a bounded tree baseline, optionally paired with a window PNG |
| `diff` | observe | a11y-observe | — | what changed since a snapshot baseline |
| `verify` | observe | a11y-observe | — | one tree read checked against --expect items |
| `menu-inspect` | observe | a11y-observe | `menu inspect` | read the menu bar in the background |
| `get-text` | observe | a11y-observe | — | independent AT-SPI Text.GetText on a named / focused node |
| `get-extents` | observe | a11y-observe | — | independent AT-SPI Component.GetExtents(Screen) |
| `get-selection` | observe | a11y-observe | — | independent AT-SPI Text selection read-back |
| `get-caret` | observe | a11y-observe | — | independent AT-SPI caret offset read-back |
| `wait` | observe | a11y-observe | — | poll until a window / node / text condition holds |
| `screenshot` | observe | a11y-observe | `shot` | capture a window or the screen to a PNG file |
| `device-screenshot` | observe | a11y-observe | — | inventory or capture a host-published wired-device screen source |
| `simulator-devices` | observe | system | `simulator devices` | list bounded CoreSimulator device identities and states |
| `resource-status` | observe | system | `resource status` | read a bounded current-host CPU, memory, load, uptime and process snapshot |
| `resource-pressure` | observe | system | `resource pressure` | read native host pressure facts without deriving a cross-platform level |
| `power-status` | observe | system | `power status` | read installation-scoped host and boot identity plus uptime |
| `ime-status` | observe | system | `ime status` | read the session input-method framework and environment facts |
| `keyboard-layout` | observe | system | `keyboard layout` | read the active XKB keyboard layout on Linux X11 |
| `runtime-status` | observe | system | `daemon status` | report the on-demand coordinator and per-resource owner topology |
| `audio` | mixed | system | — | observe or approval-bind one exact default-output volume or mute transaction |
| `service` | mixed | system | — | observe native services or approval-bind one exact user-service lifecycle transaction |
| `login-session` | mixed | system | — | observe the exact console session or plan/apply one approval-bound lock |
| `storage-devices` | observe | system | `storage devices`, `storage` | list a bounded privacy-minimized native block-device inventory |
| `storage-volumes` | observe | system | `storage volumes` | list bounded mounted-volume capacity without device identifiers |
| `storage-volume-at` | observe | system | `storage volume-at` | report exact path-bound volume capacity without device identifiers |
| `device-list` | observe | system | `device list` | list bounded native peripherals with installation-scoped opaque identities |
| `device-watch` | observe | system | `device watch` | watch bounded pseudonymous peripheral changes without false events from incomplete providers |
| `device-claims` | observe | system | `device claims` | list bounded device-lease metadata without secrets or native locators |
| `device-claim` | actuate | system | `device claim` | claim one opaque serial device through a resident exclusive native owner |
| `device-status` | observe | system | `device status` | inspect one device lease without exposing its authority secret |
| `device-read` | actuate | system | `device read` | consume one bounded byte chunk from the resident device owner |
| `device-write` | actuate | system | `device write` | write one bounded byte chunk through the resident device owner |
| `device-renew` | actuate | system | `device renew` | renew one session-bound device lease and its runtime lock |
| `device-release` | actuate | system | `device release` | restore and close the exact native handle, then release its runtime lock |
| `simulator-boot` | actuate | system | `simulator boot` | boot one exact simulator and verify its Booted state |
| `simulator-shutdown` | actuate | system | `simulator shutdown` | shut down one exact simulator and verify its Shutdown state |
| `simulator-apps` | observe | system | `simulator apps` | list bounded installed apps on one exact simulator |
| `simulator-status` | observe | system | `simulator status` | inspect exact simulator app status and host processes |
| `simulator-launch` | actuate | system | `simulator launch` | request launch of one exact simulator bundle |
| `simulator-terminate` | actuate | system | `simulator terminate` | request termination of one exact simulator bundle |
| `zoom` | observe | a11y-observe | — | crop one region of a window capture to a PNG |
| `pointer-position` | observe | a11y-observe | `cursor` | absolute pointer coordinates, no event injected |
| `pointer-grab` | actuate | a11y-actuate | — | grab the desktop pointer, verify with an independent X11 probe, then release |
| `pointer-ungrab` | actuate | a11y-actuate | — | release the desktop pointer grab and verify grab is available again |
| `invoke` | actuate | a11y-actuate | — | one semantic a11y action on one node |
| `menu-invoke` | actuate | a11y-actuate | `menu invoke` | press one menu item by exact path in the background |
| `click` | actuate | a11y-actuate | `dclick`, `rclick` | click a node by --node / --name, or --coords --degraded |
| `drag` | actuate | a11y-actuate | — | press, move, release as one gesture (--degraded) |
| `focus` | actuate | a11y-actuate | — | focus a node by --node or --window --name |
| `hover` | actuate | a11y-actuate | — | AT-SPI GenerateMouseEvent(abs) at a named node's extents center |
| `send-text` | actuate | a11y-actuate | `type` | write text into a named / focused node, or type into focus |
| `send-keys` | actuate | a11y-actuate | `key` | deliver key chords to a named / focused node, or the focus |
| `scroll` | actuate | a11y-actuate | — | AT-SPI Component.ScrollTo(TopEdge) on a named node |
| `scroll-wheel` | actuate | a11y-actuate | — | bounded wheel at a named node's AT-SPI extents center |
| `select` | actuate | a11y-actuate | — | AT-SPI Text.SetSelection on a named node |
| `set-caret` | actuate | a11y-actuate | — | AT-SPI Text.SetCaretOffset on a named node |
| `pointer-move` | actuate | a11y-actuate | `move` | move the pointer to absolute screen coordinates |
| `pointer-scroll` | actuate | a11y-actuate | `wheel` | emit a bounded desktop wheel delta at the current pointer |
| `page-js` | observe | browser | `page read` | bounded awaited CDP Runtime.evaluate against one page target |
| `page-targets` | observe | browser | `page targets` | the CDP /json target inventory |
| `page-text` | observe | browser | `page text` | visible page text in reading order (a11y tree or CDP) |
| `page-find` | observe | browser | `page find` | nodes of one CDP page target by CSS, text or role |
| `page-click` | actuate | browser | `page click` | click one node or point in a CDP page target |
| `page-download` | actuate | browser | `page download` | click and verify one background CDP download on disk |
| `page-hover` | actuate | browser | `page hover` | hover one viewport point of a CDP page target in place |
| `page-scroll` | actuate | browser | `page scroll` | wheel one viewport point of a CDP page target in place |
| `page-files` | actuate | browser | `page files` | set and verify one CDP file input without a picker |
| `page-drag` | actuate | browser | `page drag` | drag between two viewport points of a CDP target in place |
| `page-dialog` | actuate | browser | `page dialog` | accept or dismiss one real JavaScript dialog on a CDP target |
| `page-fill` | actuate | browser | `page fill` | type into one field of a CDP page target in place |
| `page-type` | actuate | browser | `page type` | insert text at a CDP page target's current focus |
| `page-nav` | actuate | browser | `page nav` | navigate one CDP page target without selecting it |
| `page-screenshot` | observe | browser | `page screenshot` | PNG of one CDP page target (background may refuse) |
| `tab-list` | observe | browser | `tab list` | the browser tab strip through the a11y tree |
| `tab-select` | actuate | browser | `tab select` | select one tab in the background |
| `tab-close` | actuate | browser | `tab close` | close one tab through its own close button or CDP (gated) |
| `browser-profiles` | observe | browser | `browser profiles` | Chromium profiles of the running browser, with windows |
| `browser-open` | actuate | browser | `browser open` | open one profile's window / URL in the running browser |
| `browser-tabs` | observe | browser | `browser tabs` | complete profile-wide MV3 tab inventory without activation |
| `browser-bridge-setup` | actuate | browser | — | install the fixed current-user MV3 and Native Messaging bundle |
| `browser-bridge-connections` | observe | browser | — | list exact live current-user bridge connections |
| `browser-bridge-status` | observe | browser | — | read protocol status from one exact bridge connection |
| `browser-bridge-tabs` | observe | browser | — | read bounded tabs from one exact bridge connection |
| `browser-bridge-attach` | actuate | browser | — | lease one exact bridge tab to an ACU runtime session |
| `browser-bridge-reload` | actuate | browser | — | reload a background native connection and prove same-profile reconnection |
| `browser-bridge-windows` | observe | browser | — | read bounded windows from one exact bridge connection |
| `browser-bridge-window-state` | actuate | browser | — | set one exact background Chromium window state with read-back |
| `browser-bridge-window-open` | actuate | browser | — | open one exact Chromium window with explicit focus semantics |
| `browser-bridge-debug-read` | observe | browser | — | read bounded AX facts from one exact background tab |
| `browser-bridge-debug-invoke` | actuate | browser | — | focus or press one exact closed-tree AX node in a background tab |
| `browser-bridge-debug-type` | actuate | browser | — | replace one exact closed-tree editable value without exposing its text |
| `browser-bridge-debug-files` | actuate | browser | — | set one exact closed-tree file input with basename and size read-back |
| `browser-session-start` | actuate | browser | `browser session-start` | start one named bounded browser automation session |
| `browser-session-list` | observe | browser | `browser session-list` | list owned browser automation sessions |
| `browser-session-status` | observe | browser | `browser session-status` | observe one named browser automation session |
| `browser-session-stop` | actuate | browser | `browser session-stop` | stop one named browser automation session |
| `browser-session-remove` | actuate | browser | `browser session-remove` | remove one verified terminal browser automation session |
| `page` | observe | browser | — | MCU page group; unmapped page verbs answer typed |
| `clipboard-read` | observe | clipboard | `clip`, `clipboard`, `clipboard read` | clipboard metadata/text, or one native type as bounded bytes |
| `clipboard-write` | actuate | clipboard | `clipboard write` | publish bounded native bytes from a file or direct UTF-8 text |
| `clipboard-write-file` | actuate | clipboard | `clipboard write-file` | put a file reference on the clipboard |
| `clipboard-clear` | actuate | clipboard | `clipboard clear` | empty the clipboard (planned unless --apply) |
| `copy` | actuate | clipboard | — | copy a node's AT-SPI text onto the native clipboard |
| `paste` | actuate | clipboard | — | write clipboard text into a node via AT-SPI |
| `window-place` | actuate | placement | — | one of the 18 placement actions on a window |
| `frame` | actuate | placement | — | window-place --action frame |
| `movewin` | actuate | placement | — | window-place move (keeps the current size) |
| `resize` | actuate | placement | — | window-place resize (keeps the current origin) |
| `maximize` | actuate | placement | — | window-place --action fullscreen |
| `unmaximize` | actuate | placement | — | EWMH unmaximize with read-back |
| `window-opacity` | actuate | windows | — | EWMH window opacity with read-back |
| `orderwin` | actuate | placement | — | relative z-order: raise --window above / below --relative |
| `exec` | none | transports | — | run one JSON command (the ssh / vnc worker mode) |
| `grant` | none | host | — | create / list / revoke bounded persisted grants |
| `host` | none | host | `hotkeys` | resident desktop menu and global shortcuts |
| `help` | none | host | — | this list, or one verb's full reference |
| `verbs` | none | host | — | the verb table (JSON when piped, or --json / --text) |

## system

### `capabilities`

```text
capabilities
```

```text
agenterm-cu capabilities    (also: caps)
  scope: observe    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  capabilities

What the selected target can observe and actuate, verb group by verb
group, with a typed reason for anything this host does not support.
`caps` is the MCU spelling.
```

### `setup`

```text
setup [--check] [--bin-dir PATH]
path-install [--bin-dir PATH]
```

```text
agenterm-cu setup    (also: path-install)
  scope: observe for --check; actuate for apply    family: System & permissions

usage:
  setup --check [--bin-dir PATH]
  setup [--bin-dir PATH]

Checks or atomically publishes one ACU-owned launcher for the exact packaged agenterm-cu binary. Check mode performs no filesystem writes. Apply also explicitly creates or reuses the private installation identity used for opaque device identifiers, serializes with future resident-job admission, aligns future activation when idle, and reports deferred without stopping, restarting, or releasing a live or uncertain owner. It is idempotent, preserves foreign/link-like entries, and refuses non-current targets. Device claiming remains unavailable until its separate native lease contract lands. Permission consent is separate.
```

### `permissions`

```text
permissions [status]
permissions open [accessibility|screen-capture]
```

```text
agenterm-cu permissions
  scope: observe for status; actuate for open    family: System & permissions

usage:
  permissions [status]
  permissions open [accessibility|screen-capture]

Status reports the canonical host permission model without mutation. Open reads the real state first: an already granted permission is a verified no-op; otherwise macOS dispatches only the exact System Settings pane. Omitting the selector chooses Accessibility before Screen Capture, but unknown state fails typed instead of guessing. Linux reports not-applicable and Windows reports provider-specific unsupported. Opening settings is not consent: accepted=true never claims the grant changed, and callers must re-run status after the user acts.
```

### `doctor`

```text
doctor
```

```text
agenterm-cu doctor
  scope: observe    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  doctor

Reuses the canonical capabilities and permissions declarations, then
runs bounded window and display inventory probes. A failed probe remains a typed check in the
successful diagnostic document; `doctor` never mutates setup, consent, helpers or foreground state.
```

### `host-open`

```text
host-open TARGET [--app APPLICATION] [--background]
open TARGET [--app APPLICATION] [--background]
```

```text
agenterm-cu host-open    (also: open)
  scope: actuate    family: System & permissions

usage: host-open TARGET [--app APPLICATION] [--background]

Dispatches one path or URL through the host's registered application mechanism without a shell. A successful reply proves only that the native dispatcher accepted the request: accepted=true, performed=true, verified=false. It never claims that a handler rendered or consumed the target. Explicit application selection is currently macOS-only; unsupported shapes fail typed instead of being ignored.
```

### `host-notify`

```text
host-notify TITLE [BODY] [--subtitle TEXT] [--sound] [--action KEY LABEL]...
notify TITLE [BODY] [--subtitle TEXT] [--sound] [--action KEY LABEL]...
```

```text
agenterm-cu host-notify    (also: notify)
  scope: actuate    family: System & permissions

usage: host-notify TITLE [BODY] [--subtitle TEXT] [--sound] [--action KEY LABEL]...

Dispatches bounded notification text without a shell. The durable receipt contains lengths and digests rather than plaintext. accepted=true proves only that the host dispatcher accepted the request; verified remains false because user presentation and attention are not observable. Subtitle and sound are currently macOS-only; Linux FDO action pairs are available on supported session-bus notification servers; unsupported shapes fail typed instead of being ignored.
```

### `audit-query`

```text
audit-query [--verb TEXT] [--outcome attempt|ok|failed|refused] [--since-ms UTC_MS] [--offset N] [--max N] [--scan-max N] [--byte-max N]
```

```text
agenterm-cu audit-query
  scope: observe    family: Runtime coordination

usage: audit-query [--verb TEXT] [--outcome KIND] [--since-ms UTC_MS] [budgets]

Newest-first bounded read of the append-only control audit. Returned rows, scanned rows and scanned bytes have independent ceilings; truncation and the next offset are explicit.
```

### `audit-compact`

```text
audit-compact [--max-age-days N] [--max-events N] [--max-bytes N] [--apply]
audit compact [--max-age-days N] [--max-events N] [--max-bytes N] [--apply]
```

```text
agenterm-cu audit-compact    (also: audit compact)
  scope: actuate when --apply; observe for a plan

usage: audit-compact [--max-age-days N] [--max-events N] [--max-bytes N] [--apply]

Plans retention by default. --apply serializes with appenders, retains the newest valid records under independent age/event/byte ceilings, and atomically replaces the audit. The receipt reports malformed, expired and bounded drops plus publication durability.
```

### `session-start`

```text
session-start [--label TEXT] [--ttl-seconds N]
session create [--label TEXT] [--ttl N]
```

```text
agenterm-cu session-start    (also: session create)
  scope: actuate    family: Runtime coordination

usage: session-start [--label TEXT] [--ttl-seconds N]

Creates a durable bounded runtime session. The private lease token is returned once; durable state stores only its digest.
```

### `session-list`

```text
session-list
session list
```

```text
agenterm-cu session-list    (also: session list)
  scope: observe    family: Runtime coordination

usage: session-list

Lists bounded active and retained terminal runtime sessions without revealing lease tokens.
```

### `session-status`

```text
session-status SESSION_ID
session status SESSION_ID
```

```text
agenterm-cu session-status    (also: session status)
  scope: observe    family: Runtime coordination

usage: session-status SESSION_ID

Reads one runtime session's label, state, creation time and expiry.
```

### `session-renew`

```text
session-renew SESSION_ID --lease LEASE [--ttl-seconds N]
session renew SESSION_ID LEASE [--ttl N]
```

```text
agenterm-cu session-renew    (also: session renew)
  scope: actuate    family: Runtime coordination

usage: session-renew SESSION_ID --lease LEASE [--ttl-seconds N]

Renews an active runtime session after verifying its private lease token.
```

### `session-end`

```text
session-end SESSION_ID --lease LEASE --confirm
session end SESSION_ID LEASE --force
```

```text
agenterm-cu session-end    (also: session end)
  scope: actuate    family: Runtime coordination

usage: session-end SESSION_ID --lease LEASE --confirm

Explicitly ends a runtime session and releases every target lock it owns. Requires --confirm (or MCU-compatible --force).
```

### `lock-acquire`

```text
lock-acquire SESSION_ID LEASE NAMESPACE:TARGET [--ttl-seconds N]
lock acquire SESSION_ID LEASE NAMESPACE:TARGET [--ttl N]
```

```text
agenterm-cu lock-acquire    (also: lock acquire)
  scope: actuate    family: Runtime coordination

usage: lock-acquire SESSION_ID LEASE NAMESPACE:TARGET [--ttl-seconds N]

Acquires one namespace:target lock bound to a live session. Same-owner reacquisition is idempotent; a competing owner fails typed.
```

### `lock-list`

```text
lock-list
lock list
```

```text
agenterm-cu lock-list    (also: lock list)
  scope: observe    family: Runtime coordination

usage: lock-list

Lists active session-bound target locks after expiry cleanup.
```

### `lock-release`

```text
lock-release LOCK_ID --lease LEASE
lock release LOCK_ID LEASE
```

```text
agenterm-cu lock-release    (also: lock release)
  scope: actuate    family: Runtime coordination

usage: lock-release LOCK_ID --lease LEASE

Releases one lock by lock id after verifying its owning session lease.
```

### `job-spawn`

```text
job-spawn [--env NAME=VALUE] [--unset-env NAME] [--cwd DIR] [--ttl-seconds N] [--cpu-seconds N] [--memory-bytes N] [--file-size-bytes N] [--max-open-files N] [--max-processes N] -- PROGRAM [ARG...]
job spawn ...
```

```text
agenterm-cu job-spawn    (also: job spawn)
  scope: actuate    family: Managed jobs

usage: job-spawn [--env NAME=VALUE] [--unset-env NAME] [--cwd DIR] [--ttl-seconds N]
                 [--cpu-seconds N] [--memory-bytes N] [--file-size-bytes N]
                 [--max-open-files N] [--max-processes N] -- PROGRAM [ARG...]

Starts one contained child under an independent resident owner. Optional CPU-time, address-space, file-size, open-file and process-count hard limits are installed before the first user instruction. Windows Job Objects support CPU-time, job-memory and active-process limits; file-size/open-file requests fail typed before spawn. macOS supports CPU-time, file-size, open-file and process-count limits; address-space memory requests fail typed because finite RLIMIT_AS is not useful below dyld's process-wide mapping. Requires global request/session identity. Exact replay returns the same job_id and generation without spawning twice. Command, environment and private owner nonce are never returned by list/status.
```

### `job-adopt`

```text
job-adopt PID --start-identity ID [--ttl-seconds N] [--expiry detach|stop] [--force]
job adopt ...
```

```text
agenterm-cu job-adopt    (also: job adopt)
  scope: actuate    family: Managed jobs

usage: job-adopt PID --start-identity ID [--ttl-seconds N] [--expiry detach|stop] [--force]

Adopts an existing POSIX process-group leader only after binding its exact start identity, stable bounded membership and current-user ownership, and retaining exact native termination authority for every member. Inability to retain that authority refuses adoption before durable intent. The default expiry detaches without mutating external work; expiry=stop requires --force. Windows returns typed unsupported rather than inferring retroactive Job Object ownership. Adopted jobs expose resources but no stdin/stdout ownership. Requires global request/session identity; exact replay never adopts twice.
```

### `job-list`

```text
job-list [--state STATE] [--offset N] [--max N]
job list ...
```

```text
agenterm-cu job-list    (also: job list)
  scope: observe    family: Managed jobs

usage: job-list [--state STATE] [--offset N] [--max N]

Lists bounded retained job identities and public lifecycle state. STATE is start-intent, starting, running, start-failed, exited, signaled, detached, or orphaned-uncertain.
```

### `job-status`

```text
job-status JOB_ID
job status JOB_ID
```

```text
agenterm-cu job-status    (also: job status)
  scope: observe    family: Managed jobs

usage: job-status JOB_ID

Reads durable state and, while the resident owner is reachable, its identity-bound live child status. Owner nonce, command and environment remain private.
```

### `job-prune`

```text
job-prune [--max-age-seconds N] [--keep-newest N] [--apply]
job prune ...
```

```text
agenterm-cu job-prune    (also: job prune)
  scope: actuate for --apply; observe for plan    family: Managed jobs

usage: job-prune [--max-age-seconds N] [--keep-newest N] [--apply]

Without --apply, returns the exact bounded candidate ids without mutation. Apply requires global request/session identity and recomputes under the store lock. Only start-failed, exited and signaled receipts older than the age boundary and outside the newest-retention set can be removed; live, detached and orphaned-uncertain records are never candidates.
```

### `job-events`

```text
job-events JOB_ID GENERATION [--stdout-cursor N] [--stderr-cursor N] [--timeout-ms N] [--max-bytes N]
```

```text
agenterm-cu job-events    (also: job events)
  scope: observe    family: Managed jobs

usage: job-events JOB_ID GENERATION [--stdout-cursor N] [--stderr-cursor N] [--timeout-ms N] [--max-bytes N]

Reads independent bounded stdout/stderr rings. Cursors are canonical decimal byte positions; truncation and native read failures stay explicit.
```

### `job-resources`

```text
job-resources JOB_ID GENERATION [--watch-ms N --interval-ms N --max-samples N] [--members-per-sample]
job resources JOB_ID GENERATION [--watch-ms N --interval-ms N --max-samples N] [--members-per-sample]
```

```text
agenterm-cu job-resources    (also: job resources)
  scope: observe    family: Managed jobs

usage: job-resources JOB_ID GENERATION [--watch-ms N --interval-ms N --max-samples N] [--members-per-sample]

Samples the managed job's native containment group once or as a bounded series for 1..=300000 ms. Watch mode accepts an explicit 1..=60000 ms interval and a 1..=1000 sample ceiling; without them it adaptively spaces at most 1000 samples across the duration. Reaching the sample ceiling reports `truncated=true`, `completed=false`, and `ended_reason=max-samples`. `--members-per-sample` includes each complete member list in its sample; at most 131072 member rows are returned, and the next overflowing sample is not emitted before the result stops with `ended_reason=member-rows`, `truncated=true`, and `completed=false`. Every member is identity-bracketed and the durable root identity must remain present. Results include a membership digest, exact decimal aggregate and per-member CPU/RSS/page-fault counters, and explicit provider, membership_complete, breakaway_prevented, and tree_complete truth. Windows Job Objects prevent breakaway; POSIX process groups enumerate current membership but do not claim complete genealogy. Legacy `job resources --watch S` maps seconds exactly and preserves its one-second/300-sample defaults. Legacy `--max 1..4096` projects returned member rows in pid order without changing complete counts, digests or aggregates. Legacy `--top 0..20` ranks a point by cumulative CPU time and each watched sample by interval CPU rate; `--top` and `--max` together still rank the complete membership rather than the displayed prefix.
```

### `job-priority`

```text
job-priority JOB_ID GENERATION NICE
job priority JOB_ID GENERATION NICE
```

```text
agenterm-cu job-priority    (also: job priority)
  scope: actuate    family: Managed jobs

usage: job-priority JOB_ID GENERATION NICE

Requires global request and owning session identity. Unix binds one exact native process group, writes a receipt before the group-level setpriority effect, and verifies the same bounded member identities plus every resulting nice value. An uncertain write or changed membership is never retryable. Windows refuses before mutation because priority classes are not Unix nice semantics.
```

### `job-policy`

```text
job-policy JOB_ID GENERATION status|set|clear [--max-rss-bytes N] [--max-cpu-pct N] [--max-processes N] [--interval-ms N] [--samples N] [--action stop|terminate] [--force]
job policy ...
```

```text
agenterm-cu job-policy    (also: job policy)
  scope: status=observe; set|clear=actuate    family: Managed jobs

usage: job-policy JOB_ID GENERATION status|set|clear [thresholds]

Set installs one lease-scoped resident policy with at least one of --max-rss-bytes, --max-cpu-pct or --max-processes. Sampling is bounded by --interval-ms 250..60000 and --samples 1..20. CPU uses monotonic cumulative-counter deltas; the first sample is an explicit warm-up. After consecutive violations, stop freezes the exact Unix process group and verifies every stable member, while terminate closes the native containment on every host and requires --force. Status exposes armed, violating, enforced or error without caller polling as the enforcement mechanism. Clear removes future enforcement but never silently reverses an already performed stop or termination. Windows refuses stop before installation because Job Objects have no documented group-freeze operation.
```

### `job-output`

```text
job-output JOB_ID GENERATION [--stream stdout|stderr] [--cursor N] [--max-bytes N]
```

```text
agenterm-cu job-output    (also: job output)
  scope: observe    family: Managed jobs

usage: job-output JOB_ID GENERATION [--stream stdout|stderr] [--cursor N] [--max-bytes N]

Reads exactly one bounded output stream without splitting its byte budget with the peer stream. Data remains binary-safe base64; cursors are canonical decimal byte positions and truncation stays explicit.
```

### `job-write`

```text
job-write JOB_ID GENERATION [--data-base64 DATA] [--close-stdin]
```

```text
agenterm-cu job-write    (also: job write)
  scope: actuate    family: Managed jobs

usage: job-write JOB_ID GENERATION [--data-base64 DATA] [--close-stdin]

Writes at most 64 KiB decoded bytes and optionally closes stdin in the same resident request. Requires global request identity; uncertain delivery fails closed rather than retrying bytes.
```

### `job-wait`

```text
job-wait JOB_ID GENERATION [--timeout-ms N] [--expect-exit CODE]
```

```text
agenterm-cu job-wait    (also: job wait)
  scope: observe    family: Managed jobs

usage: job-wait JOB_ID GENERATION [--timeout-ms N] [--expect-exit CODE]

Waits up to 24 hours through bounded resident requests. --expect-exit turns a different or unfinished result into a typed mismatch.
```

### `job-set-state`

```text
job-set-state JOB_ID GENERATION running|stopped [--timeout-ms N]
```

```text
agenterm-cu job-set-state    (also: job set-state)
  scope: actuate    family: Managed jobs

usage: job-set-state JOB_ID GENERATION running|stopped [--timeout-ms N]

Requires global request and owning session identity, then binds the durable root start identity and uses the crash-recoverable exact-process tree transaction. Unix freezes a stable bounded tree and verifies every member; Windows refuses because no documented exact Job Object freeze API is available.
```

### `job-signal`

```text
job-signal JOB_ID GENERATION HUP|INT|TERM|KILL|STOP|CONT|USR1|USR2 [--timeout-ms N] [--force]
```

```text
agenterm-cu job-signal    (also: job signal)
  scope: actuate    family: Managed jobs

usage: job-signal JOB_ID GENERATION HUP|INT|TERM|KILL|STOP|CONT|USR1|USR2 [--timeout-ms N] [--force]

Requires global request and owning session identity. The complete bounded Unix tree is stabilized under exact process references with a durable recovery transaction before delivery. STOP/CONT/TERM/KILL have observable postconditions; HUP/INT/USR1/USR2 report accepted delivery without inventing a process-specific effect. Reusing the same request id returns the sealed prior outcome without a second signal. --force is required exactly for SIGKILL. Windows refuses because no documented exact Job Object signal surface is available.
```

### `job-stop`

```text
job-stop JOB_ID GENERATION [--grace-ms N] [--expect-stopped]
```

```text
agenterm-cu job-stop    (also: job stop)
  scope: actuate    family: Managed jobs

usage: job-stop JOB_ID GENERATION [--grace-ms N] [--expect-stopped]

With grace, first closes stdin and waits; then stops the exact contained child object. Requires global request identity. --expect-stopped refuses an unverified terminal state.
```

### `job-renew`

```text
job-renew JOB_ID GENERATION [--ttl-seconds N]
```

```text
agenterm-cu job-renew    (also: job renew)
  scope: actuate    family: Managed jobs

usage: job-renew JOB_ID GENERATION [--ttl-seconds N]

Renews the resident owner lease for 1..=86400 seconds. Requires global request identity. Expiry remains an owned-resource cleanup boundary.
```

### `simulator-devices`

```text
simulator devices [--max N]
simulator-devices [--max N]
```

```text
agenterm-cu simulator-devices    (also: simulator devices)
  scope: observe    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  simulator devices [--max N]
  simulator-devices [--max N]

arguments:
  --max N                       device result bound, 1..200 (default 200)

Lists exact CoreSimulator UDIDs and their reported states. The bound is
validated before platform dispatch. On non-macOS hosts this typed-fails as
simulator_unsupported.
```

### `resource-status`

```text
resource status
resource-status
```

```text
agenterm-cu resource-status    (also: resource status)
  scope: observe    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  resource status
  resource-status

Returns one bounded current-host snapshot with platform, architecture, host
identity, uptime, native load semantics, CPU identity, strict free memory,
reclaimable available memory and the complete native process count. The
queries are sequential and atomicSnapshot is always false. Windows explicitly
reports loadAverageSemantics=windows-not-available instead of fabricating a
native load measurement.
```

### `resource-pressure`

```text
resource pressure
resource-pressure
```

```text
agenterm-cu resource-pressure    (also: resource pressure)
  scope: observe    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  resource pressure
  resource-pressure

Returns native pressure facts without inventing one portable percentage or
severity. Linux exposes bounded PSI windows independently for memory, CPU and
I/O. macOS exposes the raw vm.memory_pressure value and names CPU/I/O as
unavailable. Windows exposes the native low/high memory resource notification
states and names CPU/I/O as unavailable. The three providers are sampled
sequentially, so atomicSnapshot is false and derived is always false. Use the
existing bounded `ps --sort cpu|memory` command for ranked process samples.
```

### `power-status`

```text
power status
power-status
```

```text
agenterm-cu power-status    (also: power status)
  scope: observe    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  power status
  power-status

Returns an installation-scoped opaque host identity, an opaque identity for
this exact operating-system boot and monotonic uptime. Observation never
creates or rotates installation state: run setup once if the identity is not
enrolled. No hardware serial, native process-start identity or private key is
returned. The boot anchor is sampled before and after uptime; any identity
change fails typed instead of producing a mixed receipt.
```

### `ime-status`

```text
ime status
ime-status
```

```text
agenterm-cu ime-status    (also: ime status)
  scope: observe    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  ime status
  ime-status

Reports the session input-method framework when IBus or Fcitx answers on the
desktop bus, including GTK_IM_MODULE, QT_IM_MODULE and XMODIFIERS facts.
When no framework is observable the reply is a typed unsupported result with
repair alternatives instead of screenshot or coordinate degradation.
```

### `keyboard-layout`

```text
keyboard layout
keyboard-layout
```

```text
agenterm-cu keyboard-layout    (also: keyboard layout)
  scope: observe    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  keyboard layout
  keyboard-layout

Reports the active XKB rules/model/layout/variant from the X11 root window
_XKB_RULES_NAMES property. When the mechanism is unavailable the reply is a
typed unsupported result with repair alternatives instead of screenshot or
coordinate degradation.
```

### `runtime-status`

```text
daemon status
runtime-status
```

```text
agenterm-cu runtime-status    (also: daemon status)
  scope: observe    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  daemon status
  runtime-status

Reports ACU's actual on-demand coordinator, active session/lock counts and
durable managed-job state counts. ACU has no global daemon: each long-lived
job or browser session has its own bounded resident owner. This command is
read-only and does not probe child liveness; use job-status for that exact
owner check. daemon start/restart/stop are not aliases and must not become
no-op successes.
```

### `audio`

```text
audio status
audio plan volume 0..100 [--ttl-seconds N]
audio plan muted true|false [--ttl-seconds N]
audio apply --request R --approve H
```

```text
agenterm-cu audio
  scope: observe for status/plan; actuate for apply    family: System & permissions

usage:
  audio status
  audio plan volume 0..100 [--ttl-seconds N]
  audio plan muted true|false [--ttl-seconds N]
  audio apply --request R --approve H

Status reads the exact current default-output device, integer volume and mute
state without opening an application. Plan binds that complete state and a
short expiry, performs no mutation, and returns request/approval plus a sealed
reverse plan. Apply checks durable replay before fresh admission, reserves before
CoreAudio mutation, and requires exact read-back. A failed read-back rolls back
only while the same device remains current; verified rollback is terminal while
an uncertain effect is never replayed. Linux and Windows return audio_unsupported.
```

### `service`

```text
service list|system-list [--match S] [--max N]
service status|system-status NAME
service plan start|stop|restart|bootstrap|bootout NAME [--definition PATH] [--ttl-seconds N]
service apply --request R --approve H
service bootstrap PLIST | start|restart|stop NAME | bootout NAME --force [--ttl-seconds N] (global request identity required)
```

```text
agenterm-cu service
  scope: observe for inventory/status/plan; actuate for apply/direct lifecycle    family: System & permissions

usage:
  service list|system-list [--match S] [--max N]
  service status|system-status NAME
  service plan start|stop|restart|bootstrap|bootout NAME [--definition PATH] [--ttl-seconds N]
  service apply --request R --approve H
  service bootstrap PLIST
  service start|restart|stop NAME
  service bootout NAME --force

Direct lifecycle is the identity-bound compatibility transaction and requires
the global --request-id, --session and --session-lease triple. It acquires the
exact service target lock, then uses the same typed plan/apply state machine;
outer replay therefore never repeats an uncertain native effect. Bootstrap
derives and binds the launchd Label from a current-user-owned plist; bootout
requires --force. List and status resolve the current native authority domain.
Plan performs no mutation and binds the complete before snapshot plus a bounded
definition digest. System services are observable; system mutation returns
service_requires_privilege. Linux uses systemd D-Bus, macOS uses the fixed
/bin/launchctl path, and Windows returns service_unsupported.
```

### `login-session`

```text
login-session status
login-session plan lock [--ttl-seconds N]
login-session apply --request R --approve H
```

```text
agenterm-cu login-session
  scope: observe for status/plan; actuate for apply    family: System & permissions

usage:
  login-session status
  login-session plan lock [--ttl-seconds N]
  login-session apply --request R --approve H

Status returns a bounded current console-session inventory and lock state. Plan
re-reads one completed console session owned by the current user and returns an
encoded request plus separate contract and approval digests without mutation;
its approval alias is the exact value to pass to --approve. Apply checks an
existing durable receipt before expiry or live-provider checks, then validates
a fresh request's expiry, user and exact session identity, reserves before
delivery, and verifies the locked postcondition. A crash or uncertain delivery
is durably closed and never automatically repeated. An already-locked session
consumes the approval as a verified no-op. Unlock remains a human credential or
biometric boundary and is never automated. Linux and Windows return
login_session_unsupported rather than claiming macOS semantics.
```

### `storage-devices`

```text
storage [--max N]
storage devices [--max N]
storage-devices [--max N]
```

```text
agenterm-cu storage-devices    (also: storage devices, storage)
  scope: observe    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  storage [--max N]
  storage devices [--max N]
  storage-devices [--max N]

arguments:
  --max N                       result bound, 1..5000 (default 500)

Returns a bounded native block-device inventory with exact capacities encoded
as decimal strings. Missing host fields are explicit in unavailable_fields.
Serial numbers, WWNs and Windows UniqueId are never queried or returned. The
command is read-only: it does not mount, eject, format or mutate metadata.
```

### `storage-volumes`

```text
storage volumes [--max N]
storage-volumes [--max N]
```

```text
agenterm-cu storage-volumes    (also: storage volumes)
  scope: observe    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  storage volumes [--max N]
  storage-volumes [--max N]

arguments:
  --max N                       result bound, 1..512 (default 128)

Returns a bounded native mounted-volume inventory. Each row contains only the
public mount path, an explicit utf8/lossy encoding label and exact
total/free/current-user-available/allocation-unit counts as decimal strings. It
never returns serial numbers, UUIDs or backing device identifiers. The command
is read-only and reports partial scans instead of inventing complete coverage.
Potentially blocking or automounting Unix filesystems are counted as skipped;
Windows currently reports only fixed drive-letter and RAM-disk letters and marks
that coverage incomplete rather than probing removable or remote media.

The Bun-free legacy entry maps `resource volumes [--max N]` here, making its
archived default of 500 explicit. A requested bound above the native 512-row
ceiling fails locally rather than being clamped. The native reply intentionally
does not restore legacy filesystem device names or derived used percentages.
Exact-path capacity uses `storage-volume-at` instead of guessing from this
truncated, skipped or lossy inventory.

### `storage-volume-at`

```text
storage volume-at PATH
storage-volume-at PATH
```

```text
agenterm-cu storage-volume-at    (also: storage volume-at)
  scope: observe    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  storage volume-at PATH
  storage-volume-at PATH

Returns exact capacity facts for the volume containing one existing file,
directory or other filesystem entry. The input is canonicalized and symbolic
links are followed. Capacity is reported as decimal strings. Mount identity is
proved by macOS statfs, Linux st_dev plus the current mount namespace mountinfo,
or Win32 volume-path resolution; Linux returns a typed null mount path rather
than guessing when no matching mount record exists. The reply includes path
kind, canonical and mount path encoding, proof, inventory membership and a
privacy declaration. It never returns backing device names, fsids, volume GUIDs
or serial numbers. The Bun-free legacy `resource disk [PATH]` entry maps here
and makes its archived default path of `/` explicit.
```
```

### `device-list`

```text
device list [--type usb|bluetooth|audio|camera|gpu|all] [--max N]
device-list [--type usb|bluetooth|audio|camera|gpu|all] [--max N]
```

```text
agenterm-cu device-list    (also: device list)
  scope: observe    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  device list [--type usb|bluetooth|audio|camera|gpu|all] [--max N]
  device-list [--type usb|bluetooth|audio|camera|gpu|all] [--max N]

arguments:
  --type KIND                   usb|bluetooth|audio|camera|gpu|all (default all)
  --max N                       result bound, 1..5000 (default 500)

Returns bounded native USB, Bluetooth, audio, camera and GPU inventory. Every
row uses an installation-scoped opaque id; identity_continuity says whether
the provider identity should survive ordinary reconnect or is topology-bound.
Serial numbers, hardware addresses, provider instance ids and raw native paths
are never returned. Provider status is explicit as complete, partial or
unavailable. Observation never creates or rotates the installation identity;
an unenrolled installation fails typed.
```

### `device-watch`

```text
device watch [--type usb|bluetooth|audio|camera|gpu|all] [--max N] [--duration-ms N] [--interval-ms N] [--event-max N]
device-watch [--type usb|bluetooth|audio|camera|gpu|all] [--max N] [--duration-ms N] [--interval-ms N] [--event-max N]
```

```text
agenterm-cu device-watch    (also: device watch)
  scope: observe    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  device watch [--type usb|bluetooth|audio|camera|gpu|all] [--max N]
               [--duration-ms N] [--interval-ms N] [--event-max N]
  device-watch [same options]

arguments:
  --type KIND                   usb|bluetooth|audio|camera|gpu|all (default all)
  --max N                       inventory bound per sample, 1..5000 (default 500)
  --duration-ms N               overall duration, 1000..3600000 (default 10000)
  --interval-ms N               poll interval, 250..60000 (default 2000)
  --event-max N                 event ceiling, 1..5000 (default 1000)

Polls the same privacy-preserving native inventory as device-list under one
monotonic deadline. Only two consecutive complete, non-truncated provider
snapshots may emit added, removed or changed events. Partial or unavailable
providers suppress inferred events and report incomplete coverage instead of
fabricating device changes. Returned identities remain installation-scoped
pseudonyms; serials, addresses, instance ids and native paths never appear.
```

### `device-claims`

```text
device claims [--offset N] [--max N]
device-claims [--offset N] [--max N]
```

```text
agenterm-cu device-claims    (also: device claims)
  scope: observe    family: System & permissions

usage:
  device claims [--offset N] [--max N]

Lists bounded durable lease metadata. Plaintext lease secrets and native device locators are never returned.
```

### `device-claim`

```text
device claim DEVICE_ID [--ttl-seconds N] [--baud N --data-bits N --parity KIND --stop-bits N --flow KIND]
device-claim DEVICE_ID [same options]
```

```text
agenterm-cu device-claim    (also: device claim)
  scope: actuate    family: System & permissions

usage:
  device claim DEVICE_ID [--ttl-seconds N] [--baud N] [--data-bits N]
               [--parity none|even|odd] [--stop-bits 1|2]
               [--flow none|software|hardware]

Claims the opaque device id with one resident process holding one kernel-exclusive native handle. The lease secret is returned only by the first successful call; preserve it privately. Without serial options, the current line settings are preserved and reported with `serial.mode=preserved`; no serial setter or restore is performed. When any serial option is present, omitted serial fields default to 9600/8/none/1/none, exact native readback is reported with `serial.mode=configured`, and release restores the prior settings. Optional observed fields use null plus bounded `unmapped` reason tokens from `baud`, `baud-split`, `data_bits`, `parity`, `stop_bits`, `flow-mixed` and `raw_mode` when the host cannot represent a native value. On Unix, `raw_mode` means `ICANON`, `ECHO`, `ISIG`, `ICRNL`, `IXON` and `OPOST` are all clear; Windows reports it as unmapped.
```

### `device-status`

```text
device status LEASE_ID --generation N
device-status LEASE_ID --generation N
```

```text
agenterm-cu device-status    (also: device status)
  scope: observe    family: System & permissions

usage:
  device status LEASE_ID --generation N

Reports state, exact owner liveness, byte counters, serial preservation/configuration facts and expiry without returning authority or a native locator.
```

### `device-read`

```text
device read LEASE_ID --generation N --lease SECRET [--max-bytes N] [--timeout-ms N] [--encoding base64|hex]
device-read LEASE_ID [same options]
```

```text
agenterm-cu device-read    (also: device read)
  scope: actuate    family: System & permissions

usage:
  device read LEASE_ID --generation N --lease SECRET [--max-bytes N] [--timeout-ms N] [--encoding base64|hex]

Consumes one bounded chunk from the same resident native handle. A timeout is typed; no second device open is performed.
```

### `device-write`

```text
device write LEASE_ID --generation N --lease SECRET (--data-base64 DATA|--hex HEX) [--timeout-ms N]
device-write LEASE_ID [same options]
```

```text
agenterm-cu device-write    (also: device write)
  scope: actuate    family: System & permissions

usage:
  device write LEASE_ID --generation N --lease SECRET (--data-base64 DATA|--hex HEX) [--timeout-ms N]

Writes at most 65536 decoded bytes through the resident owner and reports the known delivery result. Payload and lease authority are redacted from audit output.
```

### `device-renew`

```text
device renew LEASE_ID --generation N --lease SECRET [--ttl-seconds N]
device-renew LEASE_ID [same options]
```

```text
agenterm-cu device-renew    (also: device renew)
  scope: actuate    family: System & permissions

usage:
  device renew LEASE_ID --generation N --lease SECRET [--ttl-seconds N]

Renews the session-bound native owner and the matching runtime lock without reopening the device.
```

### `device-release`

```text
device release LEASE_ID --generation N --lease SECRET
device-release LEASE_ID --generation N --lease SECRET
```

```text
agenterm-cu device-release    (also: device release)
  scope: actuate    family: System & permissions

usage:
  device release LEASE_ID --generation N --lease SECRET

Restores the saved device configuration, closes the exact native handle and then releases its runtime lock. Cleanup uncertainty is a typed failure, never a false success.
```

### `simulator-boot`

```text
simulator boot UDID --expect booted [--timeout-ms N]
simulator-boot UDID --expect booted [--timeout-ms N]
```

```text
agenterm-cu simulator-boot    (also: simulator boot)
  scope: actuate    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  simulator boot UDID --expect booted [--timeout-ms N]
  simulator-boot UDID --expect booted [--timeout-ms N]

arguments:
  UDID                          exact 36-character simulator device UDID
  --expect booted               required literal acknowledgement
  --timeout-ms N                operation deadline, 1..600000 (default 120000)

Success means the exact device was read back in Booted state and reports
verified=true. It does not claim that Simulator.app was opened or activated.
On non-macOS hosts this typed-fails as simulator_unsupported.
```

### `simulator-shutdown`

```text
simulator shutdown UDID --expect shutdown [--timeout-ms N]
simulator-shutdown UDID --expect shutdown [--timeout-ms N]
```

```text
agenterm-cu simulator-shutdown    (also: simulator shutdown)
  scope: actuate    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  simulator shutdown UDID --expect shutdown [--timeout-ms N]
  simulator-shutdown UDID --expect shutdown [--timeout-ms N]

arguments:
  UDID                          exact 36-character simulator device UDID
  --expect shutdown             required literal acknowledgement
  --timeout-ms N                operation deadline, 1..600000 (default 120000)

Success means the exact device was read back in Shutdown state and reports
verified=true. An already-Shutdown device is a verified no-op. The command
does not open or activate Simulator.app; an unsettled post-dispatch result is
reported as simulator_effect_unknown. Non-macOS hosts typed-fail as
simulator_unsupported.
```

### `simulator-apps`

```text
simulator apps UDID [--max N]
simulator-apps UDID [--max N]
```

```text
agenterm-cu simulator-apps    (also: simulator apps)
  scope: observe    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  simulator apps UDID [--max N]
  simulator-apps UDID [--max N]

arguments:
  UDID                          exact 36-character simulator device UDID
  --max N                       application result bound, 1..200 (default 200)

Lists installed application identities on the exact simulator. Identity and
bounds are validated before platform dispatch. On non-macOS hosts this
typed-fails as simulator_unsupported.
```

### `simulator-status`

```text
simulator status BUNDLE_ID --device UDID
simulator-status UDID BUNDLE_ID
```

```text
agenterm-cu simulator-status    (also: simulator status)
  scope: observe    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  simulator status BUNDLE_ID --device UDID
  simulator-status UDID BUNDLE_ID

arguments:
  UDID                          exact 36-character simulator device UDID
  BUNDLE_ID                     exact dotted application bundle identifier
  --device UDID                 grouped-form exact simulator device identity

Reports installation and running state for the exact app on an already-Booted
CoreSimulator device. Process rows contain host_pid plus a domain-separated
start_identity_sha256; raw identity, executable paths and environment values
are never published. Device, app executable and process identity joins are
bracketed and bounded. On non-macOS hosts this typed-fails as
simulator_unsupported.
```

### `simulator-launch`

```text
simulator launch UDID BUNDLE_ID --expect accepted [--timeout-ms N]
simulator-launch UDID BUNDLE_ID --expect accepted [--timeout-ms N]
```

```text
agenterm-cu simulator-launch    (also: simulator launch)
  scope: actuate    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  simulator launch UDID BUNDLE_ID --expect accepted [--timeout-ms N]
  simulator-launch UDID BUNDLE_ID --expect accepted [--timeout-ms N]

arguments:
  UDID                          exact 36-character simulator device UDID
  BUNDLE_ID                     exact dotted application bundle identifier
  --expect accepted             required literal acknowledgement
  --timeout-ms N                operation deadline, 1..600000 (default 120000)

Success reports accepted=true and verified=false: CoreSimulator accepted the
exact launch request, but the receipt never claims that the app is running.
On non-macOS hosts this typed-fails as simulator_unsupported.
```

### `simulator-terminate`

```text
simulator terminate UDID BUNDLE_ID --expect accepted [--timeout-ms N]
simulator-terminate UDID BUNDLE_ID --expect accepted [--timeout-ms N]
```

```text
agenterm-cu simulator-terminate    (also: simulator terminate)
  scope: actuate    family: System & permissions

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  simulator terminate UDID BUNDLE_ID --expect accepted [--timeout-ms N]
  simulator-terminate UDID BUNDLE_ID --expect accepted [--timeout-ms N]

arguments:
  UDID                          exact 36-character simulator device UDID
  BUNDLE_ID                     exact dotted application bundle identifier
  --expect accepted             required literal acknowledgement
  --timeout-ms N                operation deadline, 1..600000 (default 120000)

Success reports accepted=true and verified=false: CoreSimulator accepted the
exact termination request, but the receipt never claims that the app stopped.
On non-macOS hosts this typed-fails as simulator_unsupported.
```


## network

### `network-interfaces`

```text
network-interfaces [--max N]
network interfaces [--max N]
```

```text
agenterm-cu network-interfaces    (also: network interfaces)
  scope: observe    family: Network

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  network-interfaces [--max N]
  network interfaces [--max N]

arguments:
  --max N                       returned address rows, 1..=5000 (default 1000)

Enumerates native IPv4 and IPv6 interface addresses without invoking a shell.
Rows carry the platform's native interface identity, address, prefix/netmask,
MAC when available, loopback state and IPv6 scope. Results are stable-sorted
and bounded; scan truncation, response truncation and per-row read errors stay
explicit instead of making a partial snapshot look complete.
```

### `network-probe`

```text
network-probe HOST [--port N] [--attempts N] [--timeout-ms N]
network probe HOST [--port N] [--attempts N] [--timeout-ms N]
```

```text
agenterm-cu network-probe    (also: network probe)
  scope: observe    family: Network

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  network-probe HOST [--port N] [--attempts N] [--timeout-ms N]
  network probe HOST [--port N] [--attempts N] [--timeout-ms N]

arguments:
  HOST                          bare hostname or IP address
  --port N                      TCP port, 1..=65535 (default 443)
  --attempts N                  exact attempts, 1..=20 (default 3)
  --timeout-ms N                resolver and per-connect bound, 100..=60000 (default 3000)

Resolves through the host resolver once, deduplicates and freezes the
address set, then makes the exact requested number of TCP attempts round-robin.
The blocking resolver lives in an invocation-owned internal child; the parent
enforces the overall deadline and kills and reaps that child on expiry. A TCP
refusal or timeout is a successful unreachable observation. Resolver, worker
and protocol failures stay typed. This is reachability evidence, not HTTP/TLS.
```

### `network-dns`

```text
network-dns [--max N]
network dns [--max N]
```

```text
agenterm-cu network-dns    (also: network dns)
  scope: observe    family: Network

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  network-dns [--max N]
  network dns [--max N]

arguments:
  --max N                       combined resolver/domain rows, 1..=5000 (default 1000)

Returns bounded effective DNS resolver and search-domain rows with native
interface/service identity where the operating system supplies it. macOS uses
the complete scoped `scutil --dns` view in a contained bounded child; Windows
uses GetAdaptersAddresses directly. Linux detects systemd-resolved's local
stub and reads its upstream resolver file; if that source is unavailable the
reply says coverage=stub-only and complete=false instead of presenting
127.0.0.53 as the complete DNS configuration. No resolver is contacted.
```

### `network-routes`

```text
network-routes [--max N]
network routes [--max N]
```

```text
agenterm-cu network-routes    (also: network routes)
  scope: observe    family: Network

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  network-routes [--max N]
  network routes [--max N]

arguments:
  --max N                       returned route rows, 1..=5000 (default 1000)

Enumerates the native IPv4 and IPv6 route table without invoking a shell.
Rows preserve the native interface identity (ifindex on Unix, adapter LUID on
Windows), normalize destination prefixes, and keep platform-only metadata
explicit. The native scan is capped at 10,000 records, output at 1 MiB, and an
interrupted or malformed kernel snapshot fails typed instead of appearing
complete. A null gateway means an on-link route; it is not missing evidence.
```


## file

### `file-inspect`

```text
file-inspect PATH
file inspect PATH
```

```text
agenterm-cu file-inspect    (also: file inspect)
  scope: observe    family: Filesystem

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  file-inspect PATH
  file inspect PATH

arguments:
  PATH                          one file, directory, or link-like entry

Reads bounded metadata for exactly the final entry without following a final
link. The reply carries kind, lossless decimal size and timestamps, readonly
state, platform metadata, and a stable opened-object identity for ordinary
files and directories. Link-object identity is available from Unix metadata;
Windows reports that identity as typed unavailable instead of following the
link. No file contents are read.
```

### `file-watch`

```text
file-watch PATH [--duration-ms N] [--max-events N]
file observe PATH [--duration-ms N] [--max-events N]
```

```text
agenterm-cu file-watch    (also: file observe)
  scope: observe    family: Filesystem

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  file-watch PATH [--duration-ms N] [--max-events N]
  file observe PATH [--duration-ms N] [--max-events N]

arguments:
  PATH                          one existing directory to watch
  --duration-ms N               monotonic duration, 1..=86400000 (default 30000)
  --max-events N                event ceiling, 1..=4096 (default 256)

Watches one existing directory, never recursively, through the host native
filesystem notification API and emits bounded created/modified/removed events
with relative entry names and decoded masks. Reaching --max-events before the
deadline reports truncated=true and completed=false. Linux uses inotify with one
event per kernel record. macOS uses FSEvents on the same single directory, keeps
only direct entries, resolves cumulative flags by entry existence at delivery
and delivers each kind at least once with coalescing allowed. Windows uses
ReadDirectoryChangesW on the same single directory without subtree watching;
renames arrive as one removed and one created record, and a notification-buffer
overflow fails typed instead of returning an incomplete snapshot.
```

### `file-copy`

```text
file-copy SOURCE DESTINATION [--replace] [--apply]
file copy SOURCE DESTINATION [--replace] [--apply]
```

```text
agenterm-cu file-copy    (also: file copy)
  scope: observe for plan; actuate for --apply    family: Filesystem

usage (after the global flags):
  file-copy SOURCE DESTINATION [--replace] [--apply]
  file copy SOURCE DESTINATION [--replace] [--apply]

The default is a mutation-free plan. It snapshots an opened, non-symlink regular source and any existing regular destination by identity, size, modification time, permissions and SHA-256. --apply reserves a private recovery receipt before mutation, serializes the exact destination across processes, publishes a verified same-directory temporary with source permissions, and keeps a replaced destination as a backup until finalize. The receipt reports destination_durable=false if publication became visible but parent-directory durability could not be confirmed. --replace is mandatory when the destination exists. File contents are never returned.
```

### `file-attributes`

```text
file-attributes PATH [--include-values]
file attributes PATH [--include-values]
```

```text
agenterm-cu file-attributes    (also: file attributes)
  scope: observe    family: Filesystem

usage (after the global flags):
  file-attributes PATH [--include-values]
  file attributes PATH [--include-values]

Inspects bounded extended-attribute metadata for one opened non-symlink regular file. The default reply exposes only attribute names, namespaces, byte lengths, SHA-256 digests, and stable object identity. --include-values explicitly discloses bounded values as hexadecimal. Linux and macOS use native xattrs; Windows returns typed unsupported instead of pretending ADS or ACLs are equivalent.
```

### `file-mode`

```text
file-mode PATH OCTAL [--apply]
file chmod PATH OCTAL [--apply]
```

```text
agenterm-cu file-mode    (also: file chmod)
  scope: observe for plan; actuate for --apply    family: Filesystem

usage (after the global flags):
  file-mode PATH OCTAL [--apply]
  file chmod PATH OCTAL [--apply]

The default is a mutation-free preview over one opened, non-symlink regular file. OCTAL is one to four digits bounded to 07777. --apply plans again from the current opened object, rechecks its identity and current mode immediately before fchmod, then verifies native readback. A same-mode request is a verified no-op and reports performed=false. The reply includes previous_mode as a fact; a later reversal is a new identity-bound invocation, not a durable transaction. Files the caller cannot open are refused rather than weakened to a race-prone path chmod. Linux and macOS use native descriptor operations. Windows returns typed unsupported because ACLs and DOS attributes are not Unix octal modes. A post-effect readback failure returns file_mode_effect_unknown instead of claiming success or rollback.
```

### `file-xattr-set`

```text
file-xattr-set PATH NAME --value-hex HEX [--apply]
file xattr-set PATH NAME --value-hex HEX [--apply]
```

```text
agenterm-cu file-xattr-set    (also: file xattr-set)
  scope: observe for plan; actuate for --apply    family: Filesystem

usage:
  file-xattr-set PATH NAME --value-hex HEX [--apply]

The default is a zero-write preview. The decoded value is capped at 4 MiB. The reply and persistent audit contain only presence, byte length and SHA-256 for previous/requested/after states; raw values are never returned. --apply rechecks opened-object identity and the exact previous value before native mutation, then verifies readback. Same-value set is a verified no-op. Post-effect readback failure is file_xattr_effect_unknown. Linux and macOS use descriptor-native xattrs; Windows is typed unsupported.
```

### `file-xattr-remove`

```text
file-xattr-remove PATH NAME [--apply]
file xattr-remove PATH NAME [--apply]
```

```text
agenterm-cu file-xattr-remove    (also: file xattr-remove)
  scope: observe for plan; actuate for --apply    family: Filesystem

usage:
  file-xattr-remove PATH NAME [--apply]

The default is a zero-write preview. --apply rechecks opened-object identity and the exact previous value, removes only that attribute, and verifies absence. Removing an absent attribute is a verified no-op. Public and audit output contains no raw previous value. Linux and macOS use descriptor-native xattrs; Windows is typed unsupported.
```

### `file-quarantine-clear`

```text
file-quarantine-clear PATH [--apply]
file quarantine-clear PATH [--apply]
```

```text
agenterm-cu file-quarantine-clear    (also: file quarantine-clear)
  scope: observe for plan; actuate for --apply    family: Filesystem

usage:
  file-quarantine-clear PATH [--apply]

The default is a zero-write preview of removing com.apple.quarantine. --apply rechecks exact opened-object identity and previous attribute state, removes it, and verifies absence without returning its raw value. An absent attribute is a verified no-op. Non-macOS hosts return typed unsupported rather than pretending another metadata vocabulary is equivalent.
```

### `file-move`

```text
file-move SOURCE DESTINATION [--replace] [--apply]
file move SOURCE DESTINATION [--replace] [--apply]
```

```text
agenterm-cu file-move    (also: file move)
  scope: observe for plan; actuate for --apply    family: Filesystem

usage (after the global flags):
  file-move SOURCE DESTINATION [--replace] [--apply]
  file move SOURCE DESTINATION [--replace] [--apply]

The default is a mutation-free plan with the same identity, size, modification time, permissions and SHA-256 snapshots as file-copy. --apply reserves a private recovery receipt before mutation, takes the cross-process locks for both exact paths, publishes a verified same-directory temporary at the destination, retains a replaced destination as a backup, then retires the source into a same-directory source backup. Both backups stay until finalize, so rollback restores the exact recorded source and destination objects. Copying then retiring keeps one recoverable path for same-volume and cross-volume moves. --replace is mandatory when the destination exists. File contents are never returned.
```

### `file-transaction`

```text
file-transaction status|rollback|recover|finalize TRANSACTION_ID
file status|rollback|recover|finalize TRANSACTION_ID
```

```text
agenterm-cu file-transaction    (also: file status|rollback|recover|finalize)
  scope: observe for status; actuate for mutation    family: Filesystem

usage (after the global flags):
  file-transaction status|rollback|recover|finalize TRANSACTION_ID
  file status|rollback|recover|finalize TRANSACTION_ID

The receipt operation (file.copy or file.move) selects the owner. status returns the durable receipt without file content. rollback restores the exact recorded prior destination (and, for a move, the retired source) or removes a newly installed destination. recover reconciles reserved, partial-copy, backup-moved, installed-before-receipt and installed-before-retire crash windows. finalize verifies the completed post-state and deletes the retained backups. Every operation takes the same path locks as apply and refuses changed or ambiguous state with a typed error.
```


## windows

### `windows`

```text
windows [--pid N] [--app SUB] [--title SUB] [--space ID] [state filters] [--all]
        [--browser-profile SUB] [--meta] [--ax-meta] [AX root filters]
        [--ax-scan-max N] [--offset N] [--max N]
focused-window                                      (= windows --focused true)
```

```text
agenterm-cu windows    (also: focused-window)
  scope: observe    family: Windows & apps

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  windows [--pid N] [--app SUB] [--title SUB] [--space ID] [state filters] [--all]
          [--browser-profile SUB] [--meta] [--ax-meta] [AX root filters]
          [--ax-scan-max N] [--offset N] [--max N]
  focused-window                                      (= windows --focused true)

arguments:
  --pid N                       owning process id
  --app SUB                     application name substring
  --title SUB                   window title substring
  --space ID                    exact macOS managed Space id
  --focused [BOOL]              only the focused window (true, default; reply adds focused_app + window) or unfocused windows
  --minimized [BOOL]            only minimized (true, default) or shown windows
  --onscreen [BOOL]             on-screen or off-screen windows
  --occluded [BOOL]             geometrically covered or uncovered windows
  --all                         include the host's off-screen top-level inventory
  --browser-profile SUB         case-insensitive substring of the window's browser_profile (Chromium profile name)
  --meta                        force the counted object envelope
  --ax-meta                     include bounded accessibility-root metadata
  --ax-role ROLE                exact accessibility root role
  --ax-subrole SUBROLE          exact accessibility root subrole
  --ax-identifier ID            exact accessibility root identifier
  --ax-scan-max N               AX candidate scan ceiling, 1..1000 (default 200)
  --offset N                    page start
  --max N                       page size

Bare: the window array. With any filter, metadata or page flag the reply is
{windows, visited, matched, returned, offset, truncated}. All filters run on
the complete candidate inventory before paging. --all adds off-screen rows;
Space/occlusion without native truth fail typed. AX root scans are bounded,
report unavailable roots, and set truncated when the scan ceiling hides candidates. Browser rows
carry browser_profile (the Chromium profile name from the window's
" - <App> - <profile>" identity suffix, read from the AX root when the
inventory title lacks it; the <App> segment is matched loosely against
app_name, because only macOS reports a display name there -- Linux
reports /proc/<pid>/comm and Windows the image name);
--browser-profile SUB keeps only those rows
(case-insensitive substring), so an agent can address one profile's
window of a multi-profile instance. Focus: the mechanism's own mark is
kept when it made one; otherwise the frontmost application (NSWorkspace
on macOS) decides -- its own AXFocusedWindow, else its topmost window in
the stacking order -- because the system-wide accessibility focus read
fails from a process outside the GUI front chain (tmux, SSH, an agent
bridge). Every object reply carries focus: {handle, via, reason}; with
--focused true (alias focused-window) it also carries focused_app
{name, pid, bundle_id} and window (the row, or null with reason
frontmost_app_has_no_inventory_window / no_frontmost_app), so a
"front window unchanged" check never reads an empty list as "none".
```

### `windows-watch`

```text
windows-watch [--pid N] [--app SUB] [--title SUB] [--space ID] [state filters] [--all]
              [--type appeared,disappeared,changed] [--duration-ms N] [--interval-ms N]
              [--max-events N] [--max-windows N]
```

```text
agenterm-cu windows-watch
  scope: observe    family: Windows & apps

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  windows-watch [--pid N] [--app SUB] [--title SUB] [--space ID] [state filters] [--all]
                [--type appeared,disappeared,changed] [--duration-ms N] [--interval-ms N]
                [--max-events N] [--max-windows N]

arguments:
  --pid N                       owning process id
  --app SUB                     application name substring
  --title SUB                   window title substring
  --space ID                    macOS managed Space id; evaluated inside every poll
  --focused [BOOL]              focused or unfocused windows
  --minimized [BOOL]            minimized or non-minimized windows
  --onscreen [BOOL]             on-screen or off-screen windows
  --occluded [BOOL]             geometrically covered or uncovered windows
  --all                         include the host's off-screen top-level inventory
  --type KINDS                  comma-separated appeared,disappeared,changed
  --duration-ms N               watch window, 0..300000; 0 takes one extra sample
  --interval-ms N               poll interval, 20 ms through the duration
  --max-events N                retain at most N events, 1..10000
  --max-windows N               complete filtered snapshot ceiling, 1..10000 (default 2000)

Poll-diff filters are evaluated inside every complete snapshot, before diffing,
so crossing a state or Space boundary becomes appeared/disappeared. Event-type
filtering happens before the event ceiling. Reaching exactly max-events is not
called truncation; observing one more retained event sets truncated=true and
completed=false. Snapshot overflow and unavailable occlusion truth fail typed.
--all uses the additive all-top-level ABI; older native libraries fail typed.
A Space filter is macOS-only. Legacy windows watch seconds/defaults map exactly
(10 s, 0.5 s, 500 events, 2000 windows; maxima 300 s/10000/5000).
```

### `apps`

```text
apps [--running] [--all]
```

```text
agenterm-cu apps
  scope: observe    family: Windows & apps

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  apps [--running] [--all]

arguments:
  --running                     running applications only (the default set)
  --all                         also the installed applications no window can reveal

Running apps from top-level windows (pids + window count). --all also
lists the applications installed on this host that no window can reveal,
each marked running or not.
```

### `app-facts`

```text
app-facts --selector VALUE [--signing] [--verify] [--entitlements]
```

```text
agenterm-cu app-facts
  scope: observe    family: Windows & apps

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  app-facts --selector VALUE [--signing] [--verify] [--entitlements]

Resolves exactly one application by normalized path, desktop-entry, bundle or Windows
Uninstall id, or unique Name. Linux reads bounded XDG desktop entries directly. macOS
reads bounded application bundles and native Security.framework signing facts
in-process; signature verification checks every architecture without network access but
does not recursively validate nested code. Windows reads bounded current-user and
machine Uninstall registrations across both registry views, PE version resources,
queryable process image paths, and embedded Authenticode signer facts in-process.
Windows verification opens no UI or network; catalog signatures, Start Menu shortcuts
and MSIX identities remain typed gaps in this cut. All providers bracket identity and
never borrow window evidence. On macOS, running is scoped to the caller's effective
user; on Windows it is scoped to processes the caller may query. Missing, ambiguous,
truncated, and drifting resolution fail typed. Every returned field is a Fact with
present, absent, not-requested, not-applicable, unsupported, or unavailable status;
non-present facts carry a reason. Desktop-entry Version is file-format metadata and is
never reported as the application version. Exec wrappers, PATH lookup, or field codes
make executable and running unavailable rather than guessed. Linux signature facts are
unsupported and entitlements are not applicable; macOS and Windows desktop-entry ids
are not applicable, and Windows has no Apple entitlements.
```

### `app-watch`

```text
app-watch --app SELECTOR [--app SELECTOR ...] [--duration-ms N --interval-ms N --max-events N --max-processes N]
```

```text
agenterm-cu app-watch    (also: apps watch)
  scope: observe    family: Windows & apps

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  app-watch --app SELECTOR [--app SELECTOR ...] [--duration-ms N --interval-ms N --max-events N --max-processes N]
  apps watch SELECTOR

Resolves 1..=16 application selectors through app-facts, freezes each canonical
executable identity, and polls a complete native process inventory while bounding only
matched basename candidates. Events are application-level launched and quit transitions:
helper or renderer instances changing while at least one exact executable instance remains
do not invent lifecycle events. Every matched process carries a native start identity, so
PID reuse cannot preserve an old instance. Each event names one deterministic boundary
representative; it does not claim which simultaneous instance caused a sampled transition.
Reaching max-events reports truncated=true/completed=false; an oversized matched-candidate
inventory or changed application identity fails typed instead of diffing partial truth.
Polling is interval-bounded, so a launch and quit wholly between two samples is not
observable. Replies expose only selected applications and their exact matching instances.
```

### `app-inspect`

```text
app-inspect --app NAME [--depth N] [--max-nodes N] [--max-windows N]
inspect --app NAME [--depth N] [--max-nodes N] [--max-windows N]
```

```text
agenterm-cu app-inspect
  scope: observe    family: Windows & apps

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  app-inspect --app NAME [--depth N] [--max-nodes N] [--max-windows N]
  inspect --app NAME [--depth N] [--max-nodes N] [--max-windows N]

Classifies every visible top-level window whose application name contains NAME.
The inventory, accessibility walk and owning process identity are checked in one
bounded Executor call; a multi-window application is never reduced to one guessed
handle. Each row reports its own AX classification or typed acquisition failure.
No window is activated and no screenshot or page content is returned.
```

### `app`

```text
app <hide|show|quit|launch> [--window HANDLE] [--pid N] [--path P] [--snapshot --expect gone]
hide|show|quit --window HANDLE | --pid N        (alias of app <action>)
launch PATH | launch --path PATH                (alias of app launch)
```

```text
agenterm-cu app    (also: launch, quit, hide, show)
  scope: actuate    family: Windows & apps

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  app <hide|show|quit|launch> [--window HANDLE] [--pid N] [--path P] [--snapshot --expect gone]
  hide|show|quit --window HANDLE | --pid N        (alias of app <action>)
  launch PATH | launch --path PATH                (alias of app launch)

arguments:
  <action>                      hide | show | quit | launch (or --action <one of them>)
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --pid N                       process id; show needs it because hiding removed the windows
  --path P                      launch: installed application path
  --snapshot                    write the bounded tree to the receipt before acting
  --expect gone                 checkable postcondition: the handle reads back as absent

Steps a whole application, not one of its windows. hide/show take it
aside and back (show takes --pid: hiding removed the windows the handle
named); quit is destructive and carries the same three-part gate as close
-- it presses the application's own Quit item and reads the process back.
launch --path starts an installed application; the reply says pid: null
because the launcher owns the process, so wait for its window if a pid is
needed. `launch PATH`, `quit`, `hide` and `show` are the MCU spellings of
`app <action>`.
```

### `unlock`

```text
unlock --window HANDLE
```

```text
agenterm-cu unlock
  scope: observe    family: Windows & apps

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  unlock --window HANDLE

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)

Asks the owning application to build its full accessibility tree,
reading the bounded tree before and after. The poke is per host and the
reply's poke field names the one that ran: macOS sets AXManualAccessibility
(plus AXEnhancedUserInterface on the application); Linux flips the
desktop-wide org.a11y.Status switch (IsEnabled + ScreenReaderEnabled on the
session-bus name org.a11y.Bus) that a Chromium renderer watches before it
publishes a web tree; Windows has no separate poke to make, because a
Chromium process turns accessibility on when it answers WM_GETOBJECT for
its window and the UIA tree walk sends that itself -- there poked is false
WITH a reason, which is not a failure.

Reports poked (the request was delivered), grew and returned_before
separately, because the poke's own status is not the outcome: AppKit calls
the attribute unsupported even when it lands, and the Linux flags may
already be set, so only the re-read can claim anything about the tree.

Evidence: macOS is proven on a real Brave instance
(scripts/cu-brave-live-smoke.sh). The Linux and Windows paths are
code-complete but have no live run yet; scripts/cu-linux-smoke.sh and
scripts/qjs/cu-windows-browser-smoke.qjs carry the journey and exit with a
typed SKIP until a host with a Chromium-family browser runs them.
```

### `close`

```text
close --window HANDLE [--pid N] [--title T] --snapshot --expect gone
```

```text
agenterm-cu close
  scope: actuate    family: Windows & apps

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  close --window HANDLE [--pid N] [--title T] --snapshot --expect gone

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --pid N                       bind the handle to this process in the same inventory read
  --title T                     bind the handle to this exact title in the same inventory read
  --snapshot                    write the bounded tree to the receipt before acting
  --expect gone                 checkable postcondition: the handle reads back as absent

The destructive verb: closes one top-level window in the background
through the platform's own close control (macOS AXCloseButton + AXPress).
The gate is three parts, all checked before anything is touched: an exact
target (--window, bound to --pid / exact --title in the same inventory
read), a prior snapshot (--snapshot: the bounded tree written to the
receipt) and a checkable postcondition (--expect gone: the handle read back
as absent). Missing any -> "refused" (detail.reason destructive_gate,
missing [...]) with nothing performed.
```

### `activate`

```text
activate --window HANDLE
```

```text
agenterm-cu activate
  scope: actuate    family: Windows & apps

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  activate --window HANDLE

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)

Makes one exact top-level window the desktop foreground owner and
then polls the public window inventory until that exact handle reads
focused. This is the whole-window operation MCU spells `focus HANDLE`.

It is deliberately not `focus`, which targets one accessibility node
inside a window, and not `raise`, which changes only an application's own
window order without activating the application. A mechanism refusal or
missing focused read-back fails typed; sending an activation request is
not by itself success.
```

### `raise`

```text
raise --window HANDLE
```

```text
agenterm-cu raise
  scope: actuate    family: Windows & apps

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  raise --window HANDLE

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)

Lifts one window in front of the other windows OF ITS OWN APPLICATION
(macOS: AXRaise on the window element), then reads that order back. It
does not activate the application and does not change the system
frontmost application: the frontmost pid is read before and after and the
reply carries both plus frontmost_app_unchanged. If it moved anyway, the
verb fails typed "foreground_changed" rather than reporting success.

raise is NOT focus. `focus` gives one accessibility NODE inside a window
the keyboard focus and never touches stacking; `raise` moves a whole
WINDOW in front of its siblings and never moves the accessibility focus.
Neither is `orderwin`, which orders one window against another named
window in the DESKTOP-wide order.

Verification is the application-local stacking read-back: rank 0 of its
app's windows. A host that reports no stacking order answers verified
false with reason stacking_unreadable rather than letting the absence of
a contradiction read as success; a window the order refuses to move
fails typed "window_order_not_applied".
```

### `minimize`

```text
minimize --window HANDLE --expect minimized
```

```text
agenterm-cu minimize
  scope: actuate    family: Windows & apps

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  minimize --window HANDLE --expect minimized

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --expect minimized            checkable postcondition: the window reads back minimized

Sends one window to the dock through the window's own minimize
affordance (macOS: the window attribute AXMinimized set to true). Never a
keyboard shortcut, never by activating the application; the frontmost pid
is read before and after and reported.

Gated like `close`, minus the snapshot: an exact target (--window HANDLE)
and a checkable postcondition (--expect minimized). Missing either ->
"refused" (detail.reason destructive_gate, missing [...]) with nothing
performed. A window that is ALREADY minimized is a verified no-op
(performed false, verified true, reason already_minimized) -- the same
contract `invoke set-checked` has for a state that already holds.

Read-back is the window's own minimized state, polled until it settles.
Note for macOS: the `windows` inventory is on-screen only, so a minimized
window LEAVES it rather than appearing with minimized true; the reply
reports both after.minimized (the window's own state) and
after.inventory_present so the two are never confused.
```

### `restore`

```text
restore --window HANDLE --expect restored
```

```text
agenterm-cu restore
  scope: actuate    family: Windows & apps

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  restore --window HANDLE --expect restored

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --expect restored             checkable postcondition: the window reads back not minimized

Brings one minimized window back (macOS: AXMinimized set to false)
without activating its application. Same gate as `minimize` with
--expect restored, the same already-restored verified no-op, and the same
minimized read-back; the window handle is stable across minimize and
restore, so the handle `windows` gave before the minimize is the one to
pass here.
```

### `receipts`

```text
receipts [--window HANDLE] [--max N]
```

```text
agenterm-cu receipts
  scope: observe    family: Windows & apps

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  receipts [--window HANDLE] [--max N]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --max N                       lines to return (default 50, at most 1000)

The target's crash-persistent receipt file (<audit dir>/cu-receipts/
<target>.jsonl) read back in order: every invoke / menu invoke / click /
focus / close appends a "reserved" line before the mechanism and a
"completed" / "failed" line after the read-back; a "reserved" line with no
partner is the crash signature (uncertain, never "did not happen").
Default 50, at most 1000.
```

### `spaces`

```text
spaces
```

```text
agenterm-cu spaces
  scope: observe    family: Windows & apps

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  spaces

macOS SkyLight managed Space inventory (read-only). linux/windows answer
typed unsupported.
```

### `displays`

```text
displays
```

```text
agenterm-cu displays
  scope: observe    family: Windows & apps

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  displays

Native screen frames via agt_screen_list (MCU system group).
```

### `window-opacity`

```text
window-opacity --window HANDLE --opacity 0..1 --expect <same>
```

```text
agenterm-cu window-opacity
  scope: actuate    family: Windows

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  window-opacity --window HANDLE --opacity 0..1 --expect <same>

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --opacity 0..1                requested opacity (0 = transparent, 1 = opaque)
  --expect 0..1                 checkable postcondition: opacity reads back at the requested value

Sets EWMH `_NET_WM_WINDOW_OPACITY` on one exact window without activating
its application. Success is proven by the windows inventory `opacity` field
or an independent xprop read-back. Linux X11 only; other hosts answer typed
unsupported with alternatives.
```


## a11y-observe

### `app-menu-inspect`

```text
app-menu-inspect --app EXACT [--depth N] [--max-nodes N] [--title T [--exact]] [--enabled true|false] [--offset N] [--max N]
```

```text
agenterm-cu app-menu-inspect
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  app-menu-inspect --app EXACT [--depth N] [--max-nodes N]
                   [--title T [--exact]] [--enabled true|false] [--offset N] [--max N]

Resolves one running macOS application by case-insensitive exact name and unique
process identity, then reads its application-global AX menu bar without choosing
a representative window. The process start identity, complete matching window set
and foreground identity are bracketed around the bounded read. Linux and Windows
return app_menu_platform_unsupported; use menu-inspect --window there.
```

### `tree`

```text
tree [--window HANDLE] [--depth N] [--max-nodes N] [--max-value-bytes N] [--flat] [--selector PATH]
elements [--window HANDLE] [--depth N] [--max-nodes N] [--max-value-bytes N] [--selector PATH]     (alias of tree --flat)
```

```text
agenterm-cu tree    (also: elements)
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  tree [--window HANDLE] [--depth N] [--max-nodes N] [--max-value-bytes N] [--flat] [--selector PATH]
  elements [--window HANDLE] [--depth N] [--max-nodes N] [--max-value-bytes N] [--selector PATH]     (alias of tree --flat)

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --depth N                     walk depth (root = 0, at most 64)
  --max-nodes N                 node budget while the platform walks (1..20000)
  --max-value-bytes N           per-node text preview (default 4096; 0 keeps length/digest metadata)
  --flat                        number nodes (index, depth) in walk order
  --selector PATH               return one unique complete nested subtree

Depth (root=0, <=64) and node budget (1..20000) apply while the platform
walks. Node text defaults to a 4096-byte UTF-8 preview; shorter previews retain
the complete observed byte length and SHA-256, while a provider-truncated value
is marked incomplete and never receives a misleading full-value digest.
Without --flat, --selector returns `root` with recursive `children`; --flat
instead numbers the same subtree in walk order, with selector-relative depth
and original flatten indices. An unindexed segment that matches siblings fails
`a11y_node_ambiguous`; add `[index]`. A miss fails `a11y_node_not_found`. If
depth or max-nodes truncates the window-root walk, selection fails
`a11y_tree_truncated` because uniqueness and complete descendants are unproven.
No second backend walk is claimed. `elements` is the MCU spelling of tree --flat.
```

### `desktop-state`

```text
desktop-state [--window HANDLE] [--depth N] [--max-nodes N]
state [--window HANDLE] [--depth N] [--max-nodes N]     (MCU spelling)
```

```text
agenterm-cu desktop-state    (also: state)
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  desktop-state [--window HANDLE] [--depth N] [--max-nodes N]
  state [--window HANDLE] [--depth N] [--max-nodes N]     (MCU spelling)

arguments:
  --window HANDLE               exact window from `windows`; omitted only when focus resolves uniquely
  --depth N                     tree walk depth (root = 0, at most 64)
  --max-nodes N                 node budget while the platform walks (1..20000)

Composes one bounded top-level window inventory, one selected window's bounded
accessibility tree, and the absolute pointer position. It never captures a
screenshot. An explicit handle must be in the first inventory; an omitted handle
requires a unique resolved focused window. The target is re-enumerated after the
tree read and any changed/disappeared row fails typed `desktop_state_changed`.
The tree says whether its node bound truncated; a desktop with more than 512
windows refuses rather than returning a partial inventory.
```

### `query`

```text
query --window HANDLE|App#N | HANDLE [--depth N] [--max-nodes N] [--role R,R] [--subrole S,S]
      [--action A,A] [--min-depth N] [--max-depth N]
      [--text T | --text-exact T] [--identifier ID] [--actionable]
      [--enabled true|false] [--focused true|false] [--selected true|false]
      [--checked true|false] [--expanded true|false] [--within X,Y,W,H]
      [--offset N] [--max N] [--selector PATH]
      [--watch-ms N [--until present|absent|change] [--interval-ms N] [--max-events N]]
inspect/find/read are ACU query conveniences, not lossless MCU receipt aliases
```

```text
agenterm-cu query    (also: inspect, find, read)
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  query --window HANDLE|App#N | HANDLE [--depth N] [--max-nodes N] [--role R,R] [--subrole S,S]
        [--action A,A] [--min-depth N] [--max-depth N]
        [--text T | --text-exact T] [--identifier ID] [--actionable]
        [--enabled true|false] [--focused true|false] [--selected true|false]
        [--checked true|false] [--expanded true|false] [--within X,Y,W,H]
        [--offset N] [--max N] [--selector PATH]
        [--watch-ms N [--until present|absent|change] [--interval-ms N] [--max-events N]]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --depth N                     walk depth (root = 0, at most 64)
  --max-nodes N                 node budget while the platform walks (1..20000)
  --role R,R                    roles to keep (AXTextArea or text-area spellings)
  --subrole S,S                 backend-native role refinements; absent stays unknown
  --action A,A                  case-insensitive exact action names
  --min-depth N                minimum node depth (root = 0, at most 64)
  --max-depth N                maximum node depth (>= min-depth, at most 64)
  --text T                      name / value substring
  --text-exact T                exact name / value (not with --text)
  --identifier ID               accessibility identifier
  --actionable                  action-bearing or known actionable control roles
  --enabled B                  known enabled state; unknown does not match false
  --focused B                  known focused state; unknown does not match false
  --selected B                 known selected state; unknown does not match false
  --checked B                  known checked state; mixed/unknown do not match
  --expanded B                 known expanded state; unknown does not match false
  --within X,Y,W,H              screen rectangle the node must intersect
  --offset N                    page start
  --max N                       page size
  --selector PATH               MCU Role[idx] / Role@title / *@title / #desc path
  --watch-ms N                 bounded poll duration (100..30000 ms)
  --until CONDITION             present, absent, or change; requires --watch-ms
  --interval-ms N              poll interval (50..2000; default 250)
  --max-events N               retained identity-only diff events (1..2000; default 500)

Without --watch-ms this is one bounded filtered flat node list. With a watch,
every poll repeats the exact same filters and returns one full final query plus
bounded identity-only appeared/disappeared/changed events. Later acquisition
errors count as missing samples; they never masquerade as absence. False state
filters require an explicit opposite state: an unknown backend value never
matches false. `absent` requires an untruncated complete scan. Foreground
identity is compared before and after; a changed foreground or unmet --until
fails typed. inspect/find/read remain ACU query conveniences only; they are not
lossless aliases for MCU's classification, cross-field find, or node snapshot
receipts.
```

### `hit`

```text
hit --window HANDLE --x X --y Y [--depth N] [--max-nodes N]
```

```text
agenterm-cu hit
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  hit --window HANDLE --x X --y Y [--depth N] [--max-nodes N]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --x X                         screen x (the space node bounds and query --within use)
  --y Y                         screen y
  --depth N                     walk depth (root = 0, at most 64)
  --max-nodes N                 node budget while the platform walks (1..20000)

Screen coordinates -> the node under them, in the same shape `query`
returns (index, depth, id, role, name, bounds, ...), so the id goes
straight into invoke --node / click --node. `containing` lists every node
whose rectangle holds the point, so an ambiguous spot is visible rather
than hidden behind the winner.

Ranking: non-structural layout containers beat structural ones (GTK
`filler` must not steal a point from the control it wraps), then deepest
wins, then smallest area, then the later position in walk order (a
sibling drawn afterwards sits on top). A zero-area rectangle is never a
hit. Nothing here reads or moves the pointer.

The point is resolved inside the window's own bounded walk, not through
the platform's point-to-element call (macOS
AXUIElementCopyElementAtPosition): that call returns a live element with
no address in the id space tree / query publish, so its answer could not
be handed to invoke --node -- which is the whole reason to ask. A miss is
typed "a11y_node_not_found" with the walk's visited / truncated counts.
```

### `focused`

```text
focused --window HANDLE [--role ROLE] [--max-value-bytes N]
```

```text
agenterm-cu focused
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  focused --window HANDLE [--role ROLE] [--max-value-bytes N]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --role ROLE                   expected role ("unverified" on mismatch)
  --max-value-bytes N           value preview size (default 4096; 0 keeps only value_bytes)

The application's own focused control inside the window (id / role /
name / identifier / states / value preview), read without the foreground;
--role binds the expected role ("unverified" on mismatch); default preview
4096 bytes, 0 keeps only value_bytes.
```

### `observe`

```text
observe --window HANDLE (--duration S | --duration-ms N) [--depth N] [--max-nodes N]
        [--max-events N] [--notification A,B] [--interval-ms N] [--mode poll-diff|notifications]
        [--ready-path PATH]
```

```text
agenterm-cu observe
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  observe --window HANDLE (--duration S | --duration-ms N) [--depth N] [--max-nodes N]
          [--max-events N] [--notification A,B] [--interval-ms N] [--mode poll-diff|notifications]
          [--ready-path PATH]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --duration S                  seconds, fractions allowed, within (0, 120]
  --duration-ms N               exact milliseconds (not with --duration)
  --depth N                     walk depth (root = 0, at most 64)
  --max-nodes N                 node budget while the platform walks (1..20000)
  --max-events N                stop after N events (<= 5000, default 200)
  --notification A,B            event kinds to keep
  --interval-ms N               poll interval
  --mode poll-diff|notificationsevent source
  --ready-path PATH             atomic marker after the complete poll-diff baseline

Bounded event stream by poll-diff over the same bounded tree:
ValueChanged / TitleChanged / StateChanged / FocusChanged / Created /
Destroyed with monotonic seq and t_ms; stops at --max-events (<= 5000,
default 200) with truncated true; reports polls / emitted / filtered /
stopped. --ready-path publishes a caller-owned no-overwrite JSON marker
after the complete poll-diff baseline and before its duration starts; native
notifications reject it until subscription readiness is available. Poll-diff
events project the node's actions, bounds, depth, states and text from the same
before/after walk that created the event; notification-mode events mark those
tree facts incomplete. The legacy `cli acu observe` filters this captured event
set before paging it. Unknown boolean facts match neither true nor false, and
the unpublished `required` state fails typed `observe_filter_unavailable`.
```

### `snapshot`

```text
snapshot --window HANDLE [--depth N] [--max-nodes N] [--out PATH] [--shot]
```

```text
agenterm-cu snapshot
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  snapshot --window HANDLE [--depth N] [--max-nodes N] [--out PATH] [--shot]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --depth N                     walk depth (root = 0, at most 64)
  --max-nodes N                 node budget while the platform walks (1..20000)
  --out PATH                    also write the baseline itself to this path
  --shot                        retain a window PNG beside the baseline

Captures the bounded tree once and stores it as a named baseline, so
`diff` can answer "what changed since" without the caller holding the
tree. The reply carries snapshot_id, the node count and truncated. With
--shot, one Executor call also captures a PNG under the same snapshot id;
the full window identity is checked before the tree and after the PNG.
This is a sequential identity-clamped observation, not an atomic instant.
Tree, PNG, or identity failure publishes neither member of the pair.

Baselines live beside the receipts, under the same audit directory:
<audit dir>/cu-snapshots/<target>/w<window>/<snapshot_id>.json, with an
optional same-id .png, so AGENTERM_CU_AUDIT_PATH relocates audit, receipts
and baselines together. Each window keeps its 32 newest baseline pairs and
older pairs are dropped, so a poll loop cannot grow the store without end.
```

### `diff`

```text
diff --window HANDLE [--base SNAPSHOT_ID] [--advance] [--max N]
```

```text
agenterm-cu diff
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  diff --window HANDLE [--base SNAPSHOT_ID] [--advance] [--max N]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --base SNAPSHOT_ID            baseline to compare against (default: the window's most recent)
  --advance                     store the walk just read as the next baseline
  --max N                       changes per bucket (default 200, at most 2000)

Walks the window again and returns added / removed / changed nodes in
the same shape `query` returns; a changed row carries `changed` with the
names of the fields that differ (role, name, parent_id, states, bounds,
actions, text, identifier). The current walk reuses the BASELINE's budget
so a difference is a difference in the window, never a difference in how
far each side looked.

Without --base the window's most recent baseline is used and
base_selected_by says "most-recent"; base carries the id that was used.
--advance stores the walk it just compared as the next baseline in the
same call, so an agent polls a window with one verb per tick and misses
nothing between replies; next_base names the id to expect.

Node ids are positional paths (/0/3/1) -- which is what makes them
usable with invoke --node -- so inserting a sibling renumbers the ones
after it and one real insertion can read as one added plus several
changed. The field names say which: a renumbered node changes name /
role, a moved one changes only bounds.

Each bucket is capped at --max with truncated set; walk_truncated says
the tree walk itself hit its budget on either side.
```

### `verify`

```text
verify --window HANDLE --expect '[{"node"|"index"|"name"|"titleIncludes"[+"role"]|"identifier"|"role",
                                   "value"?, "checked"?, "expanded"?, "focused"?}, ...]'
```

```text
agenterm-cu verify
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  verify --window HANDLE --expect '[{"node"|"index"|"name"|"titleIncludes"[+"role"]|"identifier"|"role",
                                     "value"?, "checked"?, "expanded"?, "focused"?}, ...]'

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --expect JSON                 array of closed-shape items; an unknown key is a usage error

One tree read; all met -> ok + verified, a mismatch -> "unverified", a
state the node does not expose -> "unsupported" (fail closed), an unknown
key -> usage. name/titleIncludes alone is page identity (WebArea title).
```

### `menu-inspect`

```text
menu inspect --window HANDLE [--depth N] [--max-nodes N] [--title T [--exact]]
             [--enabled true|false] [--offset N] [--max N]
```

```text
agenterm-cu menu-inspect    (also: menu inspect)
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  menu inspect --window HANDLE [--depth N] [--max-nodes N] [--title T [--exact]]
               [--enabled true|false] [--offset N] [--max N]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --depth N                     0 = bar items, default 1, at most 8
  --max-nodes N                 node budget 1..5000
  --title T                     title filter (substring; --exact for equality)
  --exact                       match --title exactly
  --enabled true|false          keep only enabled / disabled items
  --offset N                    page start
  --max N                       page size

The application's menu bar read in the background (never opens a menu,
never activates): items with exact title paths, depth (0 = bar items,
default 1, <= 8), enabled / checked / has_submenu; node budget 1..5000;
counts visited / matched / returned / truncated.
```

### `get-text`

```text
get-text --window HANDLE [--name PAT] [--role ROLE]
```

```text
agenterm-cu get-text
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  get-text --window HANDLE [--name PAT] [--role ROLE]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --name PAT                    unique showing node whose name contains PAT (wait matching)
  --role ROLE                   narrow --name to one role

One-shot independent AT-SPI Text.GetText on the unique showing named
node, or with no --name on the node carrying the AT-SPI focused state --
the same text authority wait --text-equals polls, without a timeout. Not
send-text / paste / copy matched.text, last_text_write_via, the WebKit eval
helper queued-job OK, or a tree snapshot text. Missing Text typed-fails
(a11y_text_unavailable). Never XTest / --coords / screenshot.
```

### `get-extents`

```text
get-extents --window HANDLE --name PAT [--role ROLE]
```

```text
agenterm-cu get-extents
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  get-extents --window HANDLE --name PAT [--role ROLE]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --name PAT                    unique showing node whose name contains PAT (wait matching)
  --role ROLE                   narrow --name to one role

Independent AT-SPI Component.GetExtents(Screen). Snapshot node.bounds do
not count. Empty extents typed-fail (a11y_extents_unavailable).
```

### `get-selection`

```text
get-selection --window HANDLE --name PAT [--role ROLE]
```

```text
agenterm-cu get-selection
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  get-selection --window HANDLE --name PAT [--role ROLE]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --name PAT                    unique showing node whose name contains PAT (wait matching)
  --role ROLE                   narrow --name to one role

Independent AT-SPI Text.GetNSelections + GetSelection(0), plus one
independent Text.GetText slice for the selected scalar range in `text`.
Not the select / send-keys reply payload. Missing Text typed-fails
(a11y_selection_unavailable). n=0 is empty success with text="".
```

### `get-caret`

```text
get-caret --window HANDLE --name PAT [--role ROLE]
```

```text
agenterm-cu get-caret
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  get-caret --window HANDLE --name PAT [--role ROLE]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --name PAT                    unique showing node whose name contains PAT (wait matching)
  --role ROLE                   narrow --name to one role

Independent AT-SPI Text.CaretOffset / GetCaretOffset. Not the set-caret
reply payload. Missing Text typed-fails (a11y_caret_unavailable).
```

### `wait`

```text
wait --timeout-ms MS (--window-count-gte N | --window-title-contains PAT | --focused-handle HANDLE
                      | --window HANDLE --expect JSON [--absent]
                      | --node-name-contains PAT [--node-role ROLE] [--window HANDLE]
                      | --text-equals TEXT --name PAT [--role ROLE] --window HANDLE
                      | --text-contains SUB --name PAT [--role ROLE] --window HANDLE) [-- TEXT]
```

```text
agenterm-cu wait
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  wait --timeout-ms MS (--window-count-gte N | --window-title-contains PAT | --focused-handle HANDLE
                        | --window HANDLE --expect JSON [--absent]
                        | --node-name-contains PAT [--node-role ROLE] [--window HANDLE]
                        | --text-equals TEXT --name PAT [--role ROLE] --window HANDLE
                        | --text-contains SUB --name PAT [--role ROLE] --window HANDLE) [-- TEXT]

arguments:
  --timeout-ms MS               deadline (default 5000); timeout is typed with the last observation
  --window-count-gte N          at least N top-level windows
  --window-title-contains PAT   some window title contains PAT
  --focused-handle HANDLE       that window holds focus
  --expect JSON                 same matcher as verify; polls until every item is met
  --absent                      with --expect, wait until every listed item is absent
  --node-name-contains PAT      a showing node's name contains PAT (--node-role narrows)
  --text-equals TEXT            independent Text.GetText on --name equals TEXT
  --text-contains SUB           independent Text.GetText on --name contains SUB
  --name PAT                    unique showing node whose name contains PAT (wait matching)
  --role ROLE                   narrow --name to one role
  --window HANDLE               window handle from `windows` (numeric or App#N)

--window HANDLE --expect JSON uses the same matcher as verify: polls
until every item is met, ambiguity / unobservable state fail at once,
timeout is typed with the last observation. --absent reverses that one
closed expectation and waits until every listed item is absent.
--text-equals / --node-text-equals and --text-contains / --node-text-contains
poll AT-SPI Text.GetText on the unique showing named node until that
independent text equals TEXT or contains SUB. send-text / paste / copy
matched.text, last_text_write_via, and the WebKit eval helper's queued-job
OK are not this condition. Timeout is typed ("timeout") and reports the
last GetText. Never screenshot / XTest / --coords. `--` ends flag parsing.
```

### `screenshot`

```text
screenshot [--out PATH | PATH] [--window HANDLE]
```

```text
agenterm-cu screenshot    (also: shot)
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  screenshot [--out PATH | PATH] [--window HANDLE]

arguments:
  --out PATH                    PNG path (default: a temp file named by pid)
  --window HANDLE               window handle from `windows` (numeric or App#N)

Last resort, never the primary observation: the a11y verbs are. --window
is never a positional path. `shot` is the MCU spelling.
```

### `device-screenshot`

```text
device-screenshot --list
device-screenshot --out PATH [--device NAME|UID] [--timeout-ms N]
```

```text
agenterm-cu device-screenshot
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  device-screenshot --list
  device-screenshot --out PATH [--device NAME|UID] [--timeout-ms N]

arguments:
  --list                        inventory sources plus host Camera authorization and usbmux evidence
  --out PATH                    new PNG path; existing destinations are never overwritten
  --device NAME|UID             exact published source name or uid (optional for a sole source)
  --timeout-ms N                first-frame deadline, 1..120000 (default 10000)

The host capture inventory and usbmux pairing are independent evidence. An
empty source list never proves the phone is locked or untrusted. Camera TCC
denial or undecided consent is reported as a host error; an authorized host
with no DAL source is device_source_not_published. Frame bytes are never
printed, and a successful capture atomically creates a new output path. This
uses Camera permission, not Screen Recording.
```

### `zoom`

```text
zoom --window HANDLE (--region X,Y,W,H | --local-region X,Y,W,H) --out PATH [--replace] [--pad N]
```

```text
agenterm-cu zoom
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  zoom --window HANDLE (--region X,Y,W,H | --local-region X,Y,W,H) --out PATH [--replace] [--pad N]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --region X,Y,W,H              screen rectangle to crop (the space node bounds use)
  --local-region X,Y,W,H        window-local rectangle to crop (top-left origin)
  --out PATH                    PNG path
  --replace                     overwrite an existing file
  --pad N                       pixels of context kept around the region (default 8, at most 512)

Crops one region out of the window's own capture, so a caller can look
at a detail without a full-screen image. Exactly one rectangle is required.
--region is in screen coordinates -- the same space node bounds and query
--within use -- so a node's bounds can be passed straight in. --local-region
uses the window's top-left point as its origin. The native executor reads the
window bounds once, translates with checked arithmetic, and then uses the same
clipping and capture path as --region.

A region that does not intersect the window is typed
"region_outside_window" and NO file is written; a region that straddles
an edge is clipped to the window. --pad adds context around a region that
already intersects; it never rescues one that misses.

The crop is a clip of the window capture, never a screen grab: the reply
reports the capture size and the point -> pixel scale it applied (a
Retina window is captured at 2x, and the region is scaled into that
space). A window-local reply additionally reports region_space and the exact
requested_local_region. The legacy `zoom --x1/--y1/--x2/--y2` compatibility
shape applies its archived 20% padding in bounded arithmetic, requires an
explicit output path, and routes through --local-region without persisting
hidden zoom state. Still the last resort, like `screenshot`: the a11y verbs
are the primary observation.
```

### `pointer-position`

```text
pointer-position
cursor                                              (alias)
```

```text
agenterm-cu pointer-position    (also: cursor)
  scope: observe    family: Accessibility: observe

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  pointer-position
  cursor                                              (alias)

Observes absolute screen coordinates without injecting any pointer event
(macOS: a read-only CGEvent sample; the journey reads it around every
click / close to prove the real pointer stayed put). `cursor` is the MCU
spelling.
```


## a11y-actuate

### `app-menu-invoke`

```text
app-menu-invoke --app EXACT --path PATH
```

```text
agenterm-cu app-menu-invoke
  scope: actuate    family: Accessibility: actuate

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  app-menu-invoke --app EXACT --path PATH

Resolves one running macOS application by case-insensitive exact name and unique
process identity, freezes its complete window set and foreground identity, reserves
a durable receipt, and presses one exact enabled leaf menu path without activation.
Delivery and business-effect read-back are separate: a successful native press stays
performed even if the action removes the source window; the reply then states whether
the mark/tree effect was independently verified. Linux and Windows return the typed
app_menu_platform_unsupported result; use menu-invoke --window there.
```

### `pointer-grab`

```text
pointer-grab
```

```text
agenterm-cu pointer-grab
  scope: actuate    family: Accessibility: actuate

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  pointer-grab

Grabs the root-window pointer on Linux X11, proves the grab with an
independent X11 connection, then releases before returning. Other hosts
answer typed unsupported; use pointer-position to observe coordinates.
```

### `pointer-ungrab`

```text
pointer-ungrab
```

```text
agenterm-cu pointer-ungrab
  scope: actuate    family: Accessibility: actuate

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  pointer-ungrab

Releases any grab held by this connection and proves grab is available
again through an independent X11 probe. Other hosts answer typed
unsupported; use pointer-position to observe coordinates.
```

### `invoke`

```text
invoke --window HANDLE (--node PATH | --index N | --name PAT [--role ROLE] | --identifier ID
                        | --focused [--role ROLE] | --selector PATH)
       <press | set-value TEXT | select-option NAME | set-checked true|false
        | set-expanded true|false | increment | decrement | scroll-to
        | set-selection START:LENGTH>
```

```text
agenterm-cu invoke
  scope: actuate    family: Accessibility: actuate

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  invoke --window HANDLE (--node PATH | --index N | --name PAT [--role ROLE] | --identifier ID
                          | --focused [--role ROLE] | --selector PATH)
         <press | set-value TEXT | select-option NAME | set-checked true|false
          | set-expanded true|false | increment | decrement | scroll-to
          | set-selection START:LENGTH>

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --node PATH                   node path id from tree / query
  --index N                     flat index from tree --flat
  --name PAT                    unique showing node whose name contains PAT (wait matching)
  --role ROLE                   narrow --name to one role
  --identifier ID               accessibility identifier
  --focused                     the application's own focused control (combine only with --role)
  --selector PATH               MCU selector path (not with --node/--index/--name/--identifier)
  <action> [VALUE]              exactly one action after the flags, at most one VALUE

One semantic a11y action; never activates or raises the window. Two
showing matches -> "ambiguous", none -> "a11y_node_not_found", an action
the node does not offer -> "unsupported". set-checked / set-expanded are
desired states (already there = verified no-op). The reply carries verified
true|false with the reason and a receipt (target, node, action, before /
after state). --focused acts on the application's own focused control (what
`focused` reports), bound by id / role / identifier in the same tree read;
--role narrows it ("unverified" when the focused control has another role).
```

### `menu-invoke`

```text
menu invoke --window HANDLE --path 'Menu/Item' | '["Menu","Item"]'
```

```text
agenterm-cu menu-invoke    (also: menu invoke)
  scope: actuate    family: Accessibility: actuate

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  menu invoke --window HANDLE --path 'Menu/Item' | '["Menu","Item"]'

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --path PATH                   'Menu/Item' or a JSON array of exact titles

Press one menu item by exact path in the background; every segment must
be exactly one enabled item before anything is pressed
("a11y_menu_item_not_found" / "..._ambiguous" / "..._disabled"), the last
must be a leaf ("..._not_leaf"), a bare menu is "invalid_input"; verified
by mark read-back / tree diff.
```

### `click`

```text
click (--window HANDLE --node ID | --window HANDLE --name PAT [--role ROLE] | --coords X,Y --degraded)
      [--button left|right|middle] [--clicks N]
dclick ...                                          (alias of click --clicks 2)
rclick ...                                          (alias of click --button right)
```

```text
agenterm-cu click    (also: dclick, rclick)
  scope: actuate    family: Accessibility: actuate

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  click (--window HANDLE --node ID | --window HANDLE --name PAT [--role ROLE] | --coords X,Y --degraded)
        [--button left|right|middle] [--clicks N]
  dclick ...                                          (alias of click --clicks 2)
  rclick ...                                          (alias of click --button right)

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --to HANDLE                   MCU spelling of --window (not both; --to desktop is not mapped)
  --node ID                     node path id
  --name PAT                    unique showing node whose name contains PAT (wait matching)
  --role ROLE                   narrow --name to one role
  --coords X,Y                  screen coordinates; requires --degraded
  --degraded                    admit the coordinate fallback
  --button left|right|middle    pointer button (default left)
  --clicks N                    click count (default 1)

--name reuses wait NodeNameContains matching, then the --node AT-SPI path.
`dclick` is click --clicks 2 and `rclick` is click --button right (MCU
spellings).
```

### `drag`

```text
drag --window HANDLE (--from-name PAT --to-name PAT [--from-role ROLE] [--to-role ROLE] | --from X,Y --to X,Y) [--button left|right|middle] [--steps N] [--degraded]
```

```text
agenterm-cu drag
  scope: actuate    family: Accessibility: actuate

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  drag --window HANDLE (--from-name PAT --to-name PAT [--from-role ROLE] [--to-role ROLE] | --from X,Y --to X,Y) [--button left|right|middle] [--steps N] [--degraded]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --from-name PAT               unique showing source node whose name contains PAT
  --to-name PAT                 unique showing target node whose name contains PAT
  --from-role ROLE              narrow --from-name to one role
  --to-role ROLE                narrow --to-name to one role
  --from X,Y                    screen point of the press; must be inside the window
  --to X,Y                      screen point of the release
  --button left|right|middle    pointer button (default left)
  --steps N                     intermediate moves between press and release (default 12, at most 64)
  --degraded                    admit the path that moves the user's real pointer

Two addressing modes, never mixed. `--from-name` / `--to-name` resolve both
nodes, read independent AT-SPI `Component.GetExtents(Screen)` centers, then
deliver one bounded `GenerateMouseEvent` press / moves / release between
them (`path`: `named-extents-generate-mouse-drag`). No `--coords`, no
`--degraded`, and no `data.verified` on that path — independent
`get-extents` geometry plus `pointer-position` is the read-back.

The coordinate path is one press, a bounded series of moves and one release,
delivered as one gesture. There is no semantic a11y path for a coordinate
drag, so this is the pointer, and the reply always says WHICH path ran
(`path`) plus whether a window-local one existed (window_local_available).

macOS has no window-local pointer injection at all: mouse events posted
to a pid arrive with no window for AppKit to route them through, so the
only working path is the global one that MOVES THE USER'S REAL CURSOR.
That makes --degraded a required opt-in there, exactly as for
click --coords; without it the verb refuses and names the path it would
have taken. Nothing is injected before that refusal.

--from must be inside the window (else typed "drag_outside_window",
nothing performed); --to may leave it, and to_inside_window says so. The
read-back is where the pointer ended up: the release happened at --to, so
pointer_after must equal it, and the receipt records pointer_before and
pointer_after either way. tree_changed reports whether the window's tree
moved, as supporting evidence.
```

### `focus`

```text
focus [--window HANDLE] (--node ID | --window HANDLE --name PAT [--role ROLE])
```

```text
agenterm-cu focus
  scope: actuate    family: Accessibility: actuate

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  focus [--window HANDLE] (--node ID | --window HANDLE --name PAT [--role ROLE])

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --node ID                     node path id
  --name PAT                    unique showing node whose name contains PAT (wait matching)
  --role ROLE                   narrow --name to one role

Gives one node the accessibility focus through the a11y tree; never
raises or activates the window.
```

### `hover`

```text
hover --window HANDLE --name PAT [--role ROLE]
```

```text
agenterm-cu hover
  scope: actuate    family: Accessibility: actuate

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  hover --window HANDLE --name PAT [--role ROLE]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --name PAT                    unique showing node whose name contains PAT (wait matching)
  --role ROLE                   narrow --name to one role

One-shot AT-SPI GenerateMouseEvent("abs") at Component.GetExtents center.
addressing=accessibility-tree via=generate-mouse-event-abs. Never --coords,
XTest, or screenshot.
```

### `send-text`

```text
send-text [--window HANDLE [--name PAT [--role ROLE]]] [--allow-browser-chrome] [--] <text...>
type ...                                            (alias)
```

```text
agenterm-cu send-text    (also: type)
  scope: actuate    family: Accessibility: actuate

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  send-text [--window HANDLE [--name PAT [--role ROLE]]] [--allow-browser-chrome] [--] <text...>
  type ...                                            (alias)

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --name PAT                    unique showing node whose name contains PAT (wait matching)
  --role ROLE                   narrow --name to one role
  --allow-browser-chrome        with --window and no --name: write browser chrome (omnibox, toolbar, tab strip) instead of refusing
  --                            ends flag parsing so the text may start with a dash

--name writes via AT-SPI EditableText (SetTextContents / InsertText) or
AT-SPI Text + toolkit set-value when EditableText is absent (Chrome
renderer AX; WebKitGTK AT-SPI id + eval helper); a node with no writeable
text interface typed-fails (never XTest). --window without --name writes
that same path on the showing focused node (same innermost Text candidate
as get-text --window). Never XTest when --window is set. Without --window
stays the plain type-into-focused inject. `--` ends flag parsing. `type`
is the MCU spelling.
When the window's focused control is browser chrome (omnibox, toolbar, tab
strip) the write is refused with focused_node_is_browser_chrome unless
--allow-browser-chrome is passed; pass --name to address a page control
instead.
```

### `send-keys`

```text
send-keys [--window HANDLE [--name PAT [--role ROLE]]] [--allow-browser-chrome] [--] <keys...>
key ...                                             (alias)
```

```text
agenterm-cu send-keys    (also: key)
  scope: actuate    family: Accessibility: actuate

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  send-keys [--window HANDLE [--name PAT [--role ROLE]]] [--allow-browser-chrome] [--] <keys...>
  key ...                                             (alias)

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --name PAT                    unique showing node whose name contains PAT (wait matching)
  --role ROLE                   narrow --name to one role
  --allow-browser-chrome        with --window and no --name: write browser chrome (omnibox, toolbar, tab strip) instead of refusing
  --                            ends flag parsing so a chord may start with a dash

--name delivers AT-SPI Device/key events (DeviceEventListener
NotifyEvent); a node with no key interface typed-fails (never XTest).
--window without --name targets the showing focused node (same innermost
Text candidate as get-text --window). Prefers DeviceEventListener; plain
typeable text falls back to the AT-SPI EditableText/Text write path when
that interface is absent (con Command; Chrome; Reasonix). Never XTest when
--window is set. Without --window, the OS only acknowledges global focused
injection: the reply deliberately says performed=true, verified=false and
delivered=false because there is no target read-back. Use an exact window/node
semantic action when delivery proof matters; JSON ok alone is not proof. `--`
ends flag parsing. e.g. ctrl+c / enter / k. `key` is the MCU spelling.
When the window's focused control is browser chrome (omnibox, toolbar, tab
strip) the write is refused with focused_node_is_browser_chrome unless
--allow-browser-chrome is passed; pass --name to address a page control
instead.
```

### `scroll`

```text
scroll --window HANDLE --name PAT [--role ROLE]
```

```text
agenterm-cu scroll
  scope: actuate    family: Accessibility: actuate

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  scroll --window HANDLE --name PAT [--role ROLE]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --name PAT                    unique showing node whose name contains PAT (wait matching)
  --role ROLE                   narrow --name to one role

One-shot AT-SPI Component.ScrollTo(TopEdge). addressing=accessibility-tree
via=scroll-to. Missing / false / UnknownMethod typed-fails
(a11y_scroll_unavailable). Never Action scroll*, XTest wheel, --coords, or
screenshot.
```

### `scroll-wheel`

```text
scroll-wheel --window HANDLE --name PAT [--role ROLE] --dx DX --dy DY
```

```text
agenterm-cu scroll-wheel
  scope: actuate    family: Accessibility: actuate

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  scroll-wheel --window HANDLE --name PAT [--role ROLE] --dx DX --dy DY

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --name PAT                    unique showing node whose name contains PAT (wait matching)
  --role ROLE                   narrow --name to one role
  --dx DX                       -100..=100; positive is left, negative is right
  --dy DY                       -100..=100; positive is up, negative is down

Bounded wheel delivery at the named node's AT-SPI screen center without
leaving the physical pointer displaced. addressing=accessibility-tree
via=xtest-wheel-at-extents-center. Independent Component.GetExtents before
and after must move or the command typed-fails (a11y_scroll_wheel_no_effect).
Never --coords or screenshot.
```

### `select`

```text
select --window HANDLE --name PAT --start N --end M [--role ROLE]
```

```text
agenterm-cu select
  scope: actuate    family: Accessibility: actuate

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  select --window HANDLE --name PAT --start N --end M [--role ROLE]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --name PAT                    unique showing node whose name contains PAT (wait matching)
  --start N                     selection start offset
  --end M                       selection end offset
  --role ROLE                   narrow --name to one role

One-shot AT-SPI Text.SetSelection(0, start, end).
addressing=accessibility-tree via=set-selection. Missing Text /
UnknownMethod typed-fails (a11y_selection_unavailable). SetSelection false
typed-fails (a11y_selection_no_effect). Never XTest, mouse-drag, --coords,
or screenshot. The reply is not proof; observe with get-selection.
```

### `set-caret`

```text
set-caret --window HANDLE --name PAT --offset N [--role ROLE]
```

```text
agenterm-cu set-caret
  scope: actuate    family: Accessibility: actuate

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  set-caret --window HANDLE --name PAT --offset N [--role ROLE]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --name PAT                    unique showing node whose name contains PAT (wait matching)
  --offset N                    caret offset
  --role ROLE                   narrow --name to one role

One-shot AT-SPI Text.SetCaretOffset. addressing=accessibility-tree
via=set-caret-offset. Missing Text / UnknownMethod typed-fails
(a11y_caret_unavailable). SetCaretOffset false typed-fails
(a11y_caret_no_effect). Never XTest, --coords, or screenshot. The reply is
not proof; observe with get-caret.
```

### `pointer-move`

```text
pointer-move --to desktop --x X --y Y
move ...                                            (alias)
```

```text
agenterm-cu pointer-move    (also: move)
  scope: actuate    family: Accessibility: actuate

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  pointer-move --to desktop --x X --y Y
  move ...                                            (alias)

arguments:
  --to desktop                  explicit global coordinates; --to <handle> answers typed unsupported
  --x X                         signed 32-bit screen x
  --y Y                         signed 32-bit screen y

Moves to absolute screen coordinates without any press / release / click
/ drag / wheel side effect. Window-local pointer is not mapped: use click
--window. `move` is the MCU spelling.
```

### `pointer-scroll`

```text
pointer-scroll --to desktop --dx DX --dy DY
wheel ...                                           (alias)
```

```text
agenterm-cu pointer-scroll    (also: wheel)
  scope: actuate    family: Accessibility: actuate

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  pointer-scroll --to desktop --dx DX --dy DY
  wheel ...                                           (alias)

arguments:
  --to desktop                  desktop-level delivery at the current pointer; window-local is not mapped
  --dx DX                       -100..=100; positive is left, negative is right
  --dy DY                       -100..=100; positive is up, negative is down

Emits one bounded wheel delta at the current pointer without moving the
physical pointer or changing the focused top-level window. At least one axis
must be non-zero. A successful reply proves mechanism acceptance and those
two invariants only; delivery_verified remains false until an owning public
black-box court observes application content movement. Receipt reserved before
dispatch and closed after invariant read-back.
```


## process

### `ps`

```text
ps [--pid N] [--parent N] [--app EXACT] [--name SUB] [--command SUB] [--cpu-above PCT] [--memory-above-mb MB] [--sort pid|cpu|memory] [--sample-ms N] [--max-visited N] [--offset N] [--max N]
ps --pid N --depth N [--files] [--ports] [--max N] [--max-visited N]
```

```text
agenterm-cu ps
  scope: observe    family: Processes

usage (after global flags):
  ps [--pid N] [--parent N] [--app EXACT] [--name SUB] [--command SUB]
     [--cpu-above PCT] [--memory-above-mb MB] [--sort pid|cpu|memory]
     [--sample-ms N] [--max-visited N] [--offset N] [--max N] [--meta]
  ps --pid N --depth N [--files] [--ports] [--max N] [--max-visited N]

arguments:
  --pid N                       exact process id
  --parent N                    exact parent process id
  --app EXACT                   case-insensitive exact executable name
  --name SUB                    executable-name substring
  --command SUB                 command filter; output is digest + byte length
  --cpu-above PCT               sampled CPU percentage threshold
  --memory-above-mb MB          resident-memory threshold
  --sort pid|cpu|memory         stable result order
  --sample-ms N                 CPU sampling interval, 10..=10000
  --max-visited N               bounded detail-probe population, 1..=10000
  --depth N                     descendant depth, 0..=64; requires --pid
  --files                       attach bounded descriptor detail
  --ports                       attach bounded socket detail
  --meta                        compatibility flag; metadata is always present
  --offset N                    page start
  --max N                       page or descendant limit

List mode composes bounded filters and stable pagination. CPU percentage comes
from two identity-bound native samples over the stated interval. Command text
is never returned. PID detail mode returns a cycle-safe bounded tree and typed
file/socket attachments; unavailable platform providers stay explicit.
```

### `process-state`

```text
process-state --pid N
process state --pid N
```

```text
agenterm-cu process-state    (also: process state)
  scope: observe    family: Processes

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  process-state --pid N
  process state --pid N

arguments:
  --pid N                       positive process id to observe

Returns live, dead, or unknown through the shared process-observation
facade. A live result includes the platform start identity when available.
Unknown is fail-closed and never means dead. Future mutation commands must bind
their target to this identity rather than trusting a reusable pid alone.
```

### `process-argv`

```text
process-argv --pid N [--offset N] [--limit N] [--values]
process argv PID [--offset N] [--limit N] [--values]
```

```text
agenterm-cu process-argv    (also: process argv)
  scope: observe    family: Processes

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  process-argv --pid N [--offset N] [--limit N] [--values]
  process argv PID [--offset N] [--limit N] [--values]

arguments:
  --pid N                       positive process id (or positional PID)
  --offset N                    first argument index (default 0)
  --limit N                     returned arguments, 1..=4096 (default 100)
  --values                      explicitly include argument text

Reads the native bounded argv vector between two matching process-start
identity observations. The executable path is always visible. By default each
argument contains only its index, UTF-8 byte length and SHA-256; plaintext is
returned only with --values. Pagination is explicit and reports truncation.
A process exit, PID reuse, denied provider or oversized native buffer fails
typed instead of returning a partial or different process.
```

### `process-cwd`

```text
process-cwd --pid N
process cwd PID
```

```text
agenterm-cu process-cwd    (also: process cwd)
  scope: observe    family: Processes

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  process-cwd --pid N
  process cwd PID

arguments:
  --pid N                       positive process id (or positional PID)

Reads the native working directory between two matching process-start
identity observations. This explicit command returns the path; persistent
evidence retains only its UTF-8 byte length and SHA-256. Linux uses procfs and
macOS uses libproc vnode data. Windows returns process_cwd_unsupported because
there is no stable public API; ACU does not read undocumented remote PEB layouts.
Exit, PID reuse, permission denial, unavailable cwd and invalid native data are
distinct typed failures.
```

### `process-environment`

```text
process-environment --pid N [--prefix TEXT] [--offset N] [--limit N] [--values]
process env PID [--prefix TEXT] [--offset N] [--limit N] [--values]
```

```text
agenterm-cu process-environment    (also: process env)
  scope: observe    family: Processes

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  process-environment --pid N [--prefix TEXT] [--offset N] [--limit N] [--values]
  process env PID [--prefix TEXT] [--offset N] [--limit N] [--values]

arguments:
  --pid N                       positive process id (or positional PID)
  --prefix TEXT                 filter names by UTF-8 byte prefix (max 256 bytes)
  --offset N                    first sorted matching entry (default 0)
  --limit N                     entries returned, 1..=5000 (default 256)
  --values                      explicitly include environment values

Reads the bounded native exec-time environment snapshot between two matching
process-start identity observations. This is not a promise to observe mutations
a process made after exec. Entries are split at the first equals sign, raw-byte
sorted, prefix-filtered, then paged; duplicates and empty values are preserved.
By default values are represented only by byte length and SHA-256. --values is
an explicit disclosure request. Non-UTF-8 bytes are never replaced lossily.
Linux uses /proc/PID/environ; macOS uses KERN_PROCARGS2 and typed-fails when the
kernel omits restricted data. Windows returns process_environment_unsupported
rather than reading undocumented remote PEB layouts.
```

### `process-usage`

```text
process-usage --pid N [--watch-ms N --interval-ms N --max-samples N]
process usage --pid N [--watch-ms N --interval-ms N --max-samples N]
```

```text
agenterm-cu process-usage    (also: process usage)
  scope: observe    family: Processes

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  process-usage --pid N [--watch-ms N --interval-ms N --max-samples N]
  process usage --pid N [--watch-ms N --interval-ms N --max-samples N]

arguments:
  --pid N                       positive process id to sample
  --watch-ms N                  bounded observation duration, 1..=86400000
  --interval-ms N               sample interval, 1..=60000 (default 1000)
  --max-samples N               maximum returned samples, 1..=4096 (default 120)

Reads one cumulative resource sample between two matching process-start
identity observations. CPU nanoseconds, resident bytes and page-fault counters
are decimal strings so JSON/JavaScript consumers cannot lose u64 precision.
Watch mode takes an immediate sample, then samples on a monotonic bounded
schedule while the same start identity remains live. It reports every sample
and whether the sample ceiling truncated the requested duration. Richer I/O
counters remain an explicit migration gap.
```

### `process-fds`

```text
process-fds --pid N [--type KIND] [--target-text SUB] [--offset N] [--limit N] [--max-visited N]
process fds PID [filters]
```

```text
agenterm-cu process-fds    (also: process fds)
  scope: observe    family: Processes

usage:
  process-fds --pid N [--type KIND] [--target-text SUB] [--offset N] [--limit N] [--max-visited N]
  process fds PID [filters]

Reads one bounded native descriptor inventory between matching process-start identities. Native scan truncation, read errors, filter matches and caller pagination are reported separately. Darwin uses linked libproc code; Linux reads procfs; Windows returns a typed unsupported result rather than using private handle-table layouts.
```

### `process-maps`

```text
process-maps --pid N [--path SUB] [--permissions rwx] [--executable-only] [--offset N] [--limit N] [--max-visited N]
process maps PID [filters]
```

```text
agenterm-cu process-maps    (also: process maps)
  scope: observe    family: Processes

usage:
  process-maps --pid N [--path SUB] [--permissions rwx] [--executable-only] [--offset N] [--limit N] [--max-visited N]
  process maps PID [filters]

Reads one bounded native virtual-memory inventory between matching process-start identities. Addresses and counters are emitted as decimal/hex strings so JavaScript retains full precision. Non-UTF-8 native paths remain explicitly encoded instead of lossily replaced.
```

### `process-threads`

```text
process-threads --pid N [--name SUB] [--state STATE] [--offset N] [--limit N] [--max-visited N]
process threads PID [filters]
```

```text
agenterm-cu process-threads    (also: process threads)
  scope: observe    family: Processes

usage:
  process-threads --pid N [--name SUB] [--state STATE] [--offset N] [--limit N] [--max-visited N]
  process threads PID [filters]

Reads one bounded native thread inventory between matching process-start identities. Thread ids and time counters are lossless strings, and time_unit names the platform counter domain. Scan errors and truncation remain visible.
```

### `process-sockets`

```text
process-sockets --pid N [--family FAMILY] [--protocol PROTOCOL] [--state STATE] [--endpoint SUB] [--offset N] [--limit N] [--max-visited N]
process sockets PID [filters]
```

```text
agenterm-cu process-sockets    (also: process sockets)
  scope: observe    family: Processes

usage:
  process-sockets --pid N [--family FAMILY] [--protocol PROTOCOL] [--state STATE] [--endpoint SUB] [--offset N] [--limit N] [--max-visited N]
  process sockets PID [filters]

Reads one bounded process-owned socket inventory between matching process-start identities. Darwin uses linked libproc and Linux joins /proc/PID/fd socket inodes to the process network namespace without lsof or another executable. Endpoint and Unix-domain path bytes remain lossless. Windows refuses with a typed unsupported result. Native scan truncation, read errors, filter matches and caller pagination remain separate. Watch/diff is not part of this one-shot command.
```

### `process-cgroup`

```text
process-cgroup --pid N [--start-identity ID]
process cgroup PID [--start-identity ID]
```

```text
agenterm-cu process-cgroup    (also: process cgroup)
  scope: observe    family: Processes

usage:
  process-cgroup --pid N [--start-identity ID]
  process cgroup PID [--start-identity ID]

Reads one bounded Linux cgroup v2 point snapshot while bracketing the exact process start identity, membership path and opened cgroup directory identity. Numeric counters are lossless decimal strings; optional absent kernel files remain structurally distinct from malformed, oversized or inaccessible data. macOS and Windows return process_cgroup_not_applicable rather than pretending process groups or Job Objects have Linux cgroup semantics.
```

### `process-wait`

```text
process-wait --pid N --start-identity ID [--timeout-ms N]
process wait --pid N --start-identity ID [--timeout-ms N]
```

```text
agenterm-cu process-wait    (also: process wait)
  scope: observe    family: Processes

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  process-wait --pid N --start-identity ID [--timeout-ms N]
  process wait --pid N --start-identity ID [--timeout-ms N]

arguments:
  --pid N                       positive process id previously observed
  --start-identity ID           exact value returned by process-state
  --timeout-ms N                monotonic wait limit, 1..=86400000 (default 30000)

Opens a native stable process reference, verifies that its current start
identity equals the caller's prior process-state observation, then waits for
that exact process object. A timeout is a verified live result, not an error;
an identity mismatch fails before waiting. This avoids MCU's repeated PID
        polling and cannot silently follow a recycled pid.
```

### `process-kill`

```text
process-kill --pid N --start-identity ID [--mode forceful|graceful] [--timeout-ms N] --expect exited
kill PID --start-identity ID [--mode forceful|graceful] [--timeout-ms N] --expect exited
```

```text
agenterm-cu process-kill    (also: process kill, kill)
  scope: actuate    family: Processes

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  process-kill --pid N --start-identity ID [--mode forceful|graceful] [--timeout-ms N] --expect exited
  kill PID --start-identity ID [--mode forceful|graceful] [--timeout-ms N] --expect exited

arguments:
  --pid N                       positive process id previously observed
  --start-identity ID           exact value returned by process-state
  --mode MODE                   forceful (default) or graceful
  --timeout-ms N                monotonic exit-verification limit, 1..=86400000 (default 30000)
  --expect exited               required destructive postcondition

Opens a mutation-capable native reference, checks its start identity
against the caller's prior observation, reserves a crash-persistent receipt,
terminates through that exact object, then waits on the same object for exit.
Linux uses pidfd_send_signal and Windows forceful mode uses the retained HANDLE.
macOS refuses the operation because kqueue observation cannot make a later PID
signal atomic against PID reuse. Graceful mode is therefore Linux-only today;
unsupported is typed and no naked-PID fallback is allowed.
```

### `process-set-state`

```text
process-set-state --pid N --start-identity ID running|stopped [--timeout-ms N]
process set-state PID running|stopped --start-identity ID [--timeout-ms N]
```

```text
agenterm-cu process-set-state    (also: process set-state)
  scope: actuate    family: Processes

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  process-set-state --pid N --start-identity ID running|stopped [--timeout-ms N]
  process set-state PID running|stopped --start-identity ID [--timeout-ms N]

Opens one native process object, revalidates the supplied start identity, sends
SIGSTOP/SIGCONT through Linux pidfd or the macOS audit token, then reads the
scheduler state back before returning verified=true. It never reopens the PID as
effect authority. Windows returns process_state_unsupported rather than using
undocumented thread suspension. A no-op requested state is verified and reports
changed=false without reserving an effect receipt.
```

### `process-policy`

```text
process-policy --pid N status
process-policy --pid N --start-identity ID background|normal
process policy PID status
process policy PID background|normal --start-identity ID
```

```text
agenterm-cu process-policy    (also: process policy)
  scope: status=observe; background|normal=actuate    family: Processes

usage:
  process-policy --pid N status
  process-policy --pid N --start-identity ID background|normal
  process policy PID status
  process policy PID background|normal --start-identity ID

arguments:
  --pid N                       positive process id
  status|background|normal      observation or requested policy state
  --start-identity ID           required for background|normal

Status brackets one public macOS proc_pidinfo read with matching process-start
identities and returns the exact Darwin background flags. Linux and Windows
return process_policy_not_applicable rather than translating unrelated priority
or power concepts. background/normal verify the supplied identity, then return
process_policy_exact_authority_unavailable before mutation: the qualification
probe proved a normal macOS process cannot acquire the retained Mach task object
needed to eliminate PID reuse. ACU never falls back to taskpolicy -p PID.
```

### `process-signal`

```text
process-signal --pid N SIGNAL [--start-identity ID] [--timeout-ms N] [--force] [--tree --max N]
process signal PID SIGNAL [--start-identity ID] [--timeout-ms N] [--force] [--tree --max N]
signal PID SIGNAL [--start-identity ID] [--timeout-ms N] [--force] [--tree --max N]
```

```text
agenterm-cu process-signal    (also: process signal, signal)
  scope: actuate    family: Processes

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  process-signal --pid N SIGNAL [--start-identity ID] [--timeout-ms N] [--force] [--tree --max N]
  process signal PID SIGNAL [--start-identity ID] [--timeout-ms N] [--force] [--tree --max N]
  signal PID SIGNAL [--start-identity ID] [--timeout-ms N] [--force] [--tree --max N]

SIGNAL is one of HUP, INT, TERM, KILL, STOP, CONT, USR1 or USR2 (the SIG prefix is optional). The executor opens mutation-capable native process references before binding live start identities and never uses a naked PID as effect authority. TERM/KILL require exact-object exit read-back; STOP/CONT require scheduler-state read-back. HUP/INT/USR1/USR2 prove exact native delivery but remain verified=false because only the target application can acknowledge their meaning. SIGKILL requires --force. --tree freezes the root and every bounded descendant until two complete snapshots agree, delivers deepest-first, and restores only members it found running; pre-stopped members stay stopped. Linux uses pidfd_send_signal, macOS uses retained audit tokens, and Windows refuses arbitrary trees because it has no retained containment object for pre-existing processes. Privileged signal shapes remain a separate consent-provider gap.
```

### `process-watch`

```text
process-watch [--pid N] [--parent N] [--name SUB] [--all] [--duration-ms N --interval-ms N --max-events N --max-processes N]
process watch [selectors] [limits]
```

```text
agenterm-cu process-watch    (also: process watch)
  scope: observe    family: Processes

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  process-watch [--pid N] [--parent N] [--name SUB] [--all] [--duration-ms N --interval-ms N --max-events N --max-processes N]
  process watch [selectors] [limits]

arguments:
  --pid N                       watch one process id
  --parent N                    watch direct children of one parent
  --name SUB                    watch executable-name substring
  --all                         watch the whole bounded inventory
  --duration-ms N               monotonic duration, 1..=86400000 (default 30000)
  --interval-ms N               poll interval, 1..=60000 (default 1000)
  --max-events N                event ceiling, 1..=4096 (default 256)
  --max-processes N             matched inventory ceiling, 1..=5000 (default 1000)

Takes an immediate baseline, then emits started/exited diffs until the
monotonic duration or event ceiling is reached. Every row carries a native
start identity; pid reuse therefore appears as an exit plus a start. An
unverifiable identity on an exact PID fails closed. A broad selector excludes
unidentified processes and reports `coverage_complete: false` plus the count;
it never degrades those rows to pid-only polling. Oversized inventory fails
closed.
```

### `shell-exec`

```text
shell-exec --command TEXT [--timeout-ms N] [--max-output-bytes N]
```

```text
agenterm-cu shell-exec
  scope: actuate    family: Processes

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  shell-exec --command TEXT [--timeout-ms N] [--max-output-bytes N]

Runs one host-shell command through /bin/sh on Unix or PowerShell on Windows. The root is contained before its first instruction; timeout and output-limit failures terminate and reap the owned tree or return cleanup_uncertain. stdout and stderr are drained concurrently under one aggregate byte ceiling. A non-zero shell exit is a completed ok=true observation with success=false and its exact exit fact; transport exec --json remains a separate worker mode.
```


## privilege

### `privilege-plan`

```text
privilege-plan process.set-priority PID|self NICE [--ttl-seconds N]
privilege-plan process.signal PID SIGNAL [--tree --max N] [--force] [--timeout-ms N] [--ttl-seconds N]
privilege plan system.power-action sleep|restart|shutdown [--ttl-seconds N]
privilege plan OPERATION ...
```

```text
agenterm-cu privilege-plan    (also: privilege plan)
  scope: observe    family: Privilege

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  privilege-plan process.set-priority PID|self NICE [--ttl-seconds N]
  privilege-plan process.signal PID SIGNAL [--tree --max N] [--force]
                 [--timeout-ms N] [--ttl-seconds N]
  privilege plan system.power-action sleep|restart|shutdown [--ttl-seconds N]
  privilege plan OPERATION ...

arguments:
  process.set-priority           bind exact identity/current and requested Unix nice
  process.signal PID SIGNAL      bind one exact process and scheduler state
  system.power-action ACTION     bind sleep, restart or shutdown to this installation and boot
  --tree --max N                 bind at most 128 exact descendants and parent edges
  --force                        required exactly for SIGKILL
  --timeout-ms N                 provider read-back limit, 1..=60000 (default 5000)
  --ttl-seconds N                approval lifetime, 1..=600 (default 120)

Planning returns a canonical expiring plan with separate contract_digest and
approval_digest plus a bounded canonical request. Process plans bind stable exact
identity and before state. Power plans bind the installation host identity, the
current operating-system boot identity and one closed action. Every plan reports
consent_requested=false and mutation_performed=false where applicable. Planning
never mutates, starts a provider, invokes a shell, native power effect or consent.
Windows keeps unsupported scheduler/signal semantics typed.
```

### `privilege-apply`

```text
privilege apply --request PLAN --approve DIGEST [--provider-timeout-ms N]
```

```text
agenterm-cu privilege-apply    (also: privilege apply)
  scope: actuate    family: Privilege

usage (after the global flags, including --request-id, --session and --session-lease):
  privilege apply --request PLAN --approve DIGEST [--provider-timeout-ms N]

arguments:
  --request PLAN                 bounded canonical base64url plan returned by privilege plan
  --approve DIGEST               exact approval digest returned with that expiring plan
  --provider-timeout-ms N        full native-consent/provider deadline, 1..=600000 ms (default 120000)

The executor decodes PLAN into the closed typed priority/signal union and binds
the ordinary request/session identity. On Linux it connects only to the fixed
systemd-owned Unix socket. The root broker derives the caller from SO_PEERCRED,
retains its pidfd identity, checks the private replay ledger before consent, and
asks polkit about that exact unix-process subject only for a fresh request. It
uses no shell, pkexec, caller-supplied uid or password capture. A canceled native
dialog is consent_canceled with no effect. Broken transport or an incomplete
after-effect reply is outcome_unknown and is never retried automatically. The
fixed broker package and its platform courts remain a delivery qualification
requirement; catalog presence alone does not claim that elevation is shipped.
```

### `privilege-provider`

```text
privilege-provider status|register|unregister
privilege provider status|register|unregister
```

```text
agenterm-cu privilege-provider    (also: privilege provider)
  scope: status=observe; register|unregister=actuate    family: Privilege

usage:
  privilege-provider status|register|unregister
  privilege provider status|register|unregister

The identifiers and expected executable path are compiled into AgenTerm; callers
cannot select another service, right or binary. status is read-only. register and
unregister require request/session identity and use the ordinary audited,
idempotent mutation path. requires-approval is returned as a distinct daemon
state and provider_ready=false, never as enabled. Linux and Windows return typed
unsupported. Fixture tests do not register a service or open System Settings; a
signed fixed-path macOS court remains required before promotion.
```


## terminal

### `pty-start`

```text
pty-start NAME [--cwd DIR] [-- PROGRAM ARG...]
pty start NAME [--cwd DIR] [-- PROGRAM ARG...]
```

```text
agenterm-cu pty-start    (also: pty start)
  scope: actuate    family: Headless PTY jobs

usage:
  pty-start NAME [--cwd DIR] [-- PROGRAM ARG...]
  pty start NAME [--cwd DIR] [-- PROGRAM ARG...]

Starts one invisible, cross-process PTY job under a deterministic isolated AgenTerm logical instance. Omitting `-- PROGRAM ARG...` selects AgenTerm's platform default terminal shell; an explicit command remains typed argv, never a shell string. AgenTerm server remains the sole POSIX PTY/ConPTY, terminal-tree, retention and process-lifecycle owner. NAME is 1..=64 ASCII letters, digits, dot, underscore or hyphen. Command values are not copied into receipts. A same-name concurrent or existing job fails typed.

On Linux and macOS the headless authority listens on one deterministic Unix socket under the host runtime directory: `$XDG_RUNTIME_DIR/agenterm/{server_scope_id}.sock`, or `~/tmp/agenterm-platform-{uid}/agenterm/{server_scope_id}.sock` when `XDG_RUNTIME_DIR` is unset. The path must stay within the platform Unix-socket length limit (103 bytes on Linux). `pty-start` spawns `agenterm server --instance ephemeral:acu-pty-NAME --empty` beside `agenterm-cu` when that socket is absent; observe verbs on a missing or dead authority fail `pty_job_not_found` with `detail.control=unavailable` rather than inventing PTY output. `pty-list` marks reconciled dead authorities `stale` until `pty-prune`.
```

### `pty-list`

```text
pty-list
pty list
```

```text
agenterm-cu pty-list    (also: pty list)
  scope: observe    family: Headless PTY jobs

usage:
  pty-list
  pty list

Enumerates the bounded private named-job state directory, sorts names, and reconciles each entry with its deterministic AgenTerm authority. A row is running with full epoch/tab/process status, stale when no authority is reachable, or conflicted with a typed reconciliation error. Symlinks, malformed names and more than 4096 entries fail closed; this read-only verb never deletes or starts a job.
```

### `pty-prune`

```text
pty-prune NAME --expect stale
pty prune NAME --expect stale
```

```text
agenterm-cu pty-prune    (also: pty prune)
  scope: actuate    family: Headless PTY jobs

usage:
  pty-prune NAME --expect stale
  pty prune NAME --expect stale

Reclaims one exact named state directory only after taking the same registry lock as pty-start, taking the job lock, and independently proving that the deterministic authority is unreachable. It reserves a persistent receipt before deletion, accepts only the known workspace/settings/lock files, removes no unknown subtree, and verifies the directory is absent. A live or conflicted authority, unknown entry, link-like directory, concurrent start, missing intent or missing state fails typed without deletion.
```

### `pty-status`

```text
pty-status NAME
pty status NAME
```

```text
agenterm-cu pty-status    (also: pty status)
  scope: observe    family: Headless PTY jobs

usage:
  pty-status NAME
  pty status NAME

Returns the durable job identity (name + server scope + epoch + stable @tab), process id, terminal dimensions, dead/finalized state and exit code. A missing authority or an authority that does not contain exactly one job tab fails typed.
```

### `pty-read`

```text
pty-read NAME [--cursor earliest|current|N] [--max-bytes N]
pty read NAME ...
```

```text
agenterm-cu pty-read    (also: pty read)
  scope: observe    family: Headless PTY jobs

usage:
  pty-read NAME [--cursor earliest|current|N] [--max-bytes N]
  pty read NAME ...

Reads the same retained redacted raw PTY byte ring as terminal-output. Base64 is lossless; valid UTF-8 is auxiliary. Continue from next_cursor. Overwritten and future cursors fail typed rather than silently skipping bytes.
```

### `pty-snapshot`

```text
pty-snapshot NAME
pty snapshot NAME
```

```text
agenterm-cu pty-snapshot    (also: pty snapshot)
  scope: observe    family: Headless PTY jobs

usage:
  pty-snapshot NAME
  pty snapshot NAME

Returns the sole durable job tab's bounded structured screen plus the exact server epoch and ordered event cursor. It also persists a bounded baseline beside the audit log and returns snapshot_id. The job name, logical server scope, epoch and stable tab id remain one identity; an epoch change during capture fails typed rather than publishing a substituted snapshot. Continue with pty-events or pty-diff.
```

### `pty-diff`

```text
pty-diff NAME --base SNAPSHOT_ID [--advance] [--max N]
pty diff NAME --base SNAPSHOT_ID [--advance] [--max N]
```

```text
agenterm-cu pty-diff    (also: pty diff)
  scope: observe    family: Headless PTY jobs

usage:
  pty-diff NAME --base SNAPSHOT_ID [--advance] [--max N]
  pty diff NAME --base SNAPSHOT_ID [--advance] [--max N]

Loads one persisted bounded screen baseline and captures the named job again. Success requires the same job, server scope, epoch and stable tab id; restart fails pty_snapshot_authority_changed instead of comparing unrelated terminals. The reply separates changed rows from terminal metadata changes and bounds rows with --max. --advance publishes the current capture as next_base in the same operation. The private store keeps at most 128 baselines total and each record is capped at 2 MiB.
```

### `pty-events`

```text
pty-events NAME --epoch EPOCH --after SEQUENCE [--limit N]
pty events NAME --epoch EPOCH --after SEQUENCE [--limit N]
```

```text
agenterm-cu pty-events    (also: pty events)
  scope: observe    family: Headless PTY jobs

usage:
  pty-events NAME --epoch EPOCH --after SEQUENCE [--limit N]
  pty events NAME --epoch EPOCH --after SEQUENCE [--limit N]

Continues the bounded ordered UI event journal for the sole tab of an exact durable job. The supplied epoch must still match the named authority. Its cursor advances across every scanned event while returned terminal updates remain filtered to the job tab, preventing unrelated activity from trapping the caller in replay. Restart, overwritten history and future cursors fail typed.
```

### `pty-resize`

```text
pty-resize NAME --rows ROWS --columns COLUMNS
pty resize NAME --rows ROWS --columns COLUMNS
```

```text
agenterm-cu pty-resize    (also: pty resize)
  scope: actuate    family: Headless PTY jobs

usage:
  pty-resize NAME --rows ROWS --columns COLUMNS
  pty resize NAME --rows ROWS --columns COLUMNS

Acquires a temporary UI lease on the exact dedicated headless authority, resizes its sole tab, detaches the lease and independently reads the job status back. Success requires the same server epoch and tab plus the exact requested 1..=512 grid. The job lock serializes concurrent mutations; a failed detach or mismatched read-back is typed and retained in a crash-persistent receipt.
```

### `pty-send`

```text
pty-send NAME -- TEXT
pty send NAME -- TEXT
```

```text
agenterm-cu pty-send    (also: pty send)
  scope: actuate    family: Headless PTY jobs

usage:
  pty-send NAME -- TEXT
  pty send NAME -- TEXT

Sends one non-empty literal UTF-8 argument to the exact epoch-scoped job tab and requires AgenTerm's committed control receipt. No Enter or shell interpretation is implicit; include \r or \n in TEXT when the target program requires it. Same-name mutations are serialized.
```

### `pty-wait`

```text
pty-wait NAME (--contains TEXT | --regex PATTERN) [--cursor earliest|current|N] [--timeout-ms N] [--max-match-bytes N] [--max-scan-bytes N]
pty wait NAME ...
```

```text
agenterm-cu pty-wait    (also: pty wait)
  scope: observe    family: Headless PTY jobs

usage:
  pty-wait NAME (--contains TEXT | --regex PATTERN) [--cursor earliest|current|N] [--timeout-ms N] [--max-match-bytes N] [--max-scan-bytes N]
  pty wait NAME ...

Waits for either exact UTF-8 bytes or one Rust byte-regex in the loss-aware retained raw-output stream. The two conditions are mutually exclusive. Regex patterns are 1..=4096 bytes and use a bounded linear-time matcher over the complete retained scan window; unbounded quantifiers are accepted, while lookaround and backreferences fail typed. The regex window is bounded by --max-scan-bytes (1..=67108864, default 16777216), and a found match is bounded by --max-match-bytes (1..=65536, default 4096). A match beyond that bound and an exhausted scan window fail with distinct typed errors. ^ and $ use multiline line semantics; \A and \z refer to the window from the selected cursor through the current visible tail, not the complete lifetime stream. The reply reports byte cursors, match length and scan count but never the matched output or regex text; errors disclose only bounded metadata and a pattern SHA-256. Exact byte matching still detects a needle split across output pages. Overwritten/future cursors, timeout, and finalized-without-match remain distinct typed failures; output is never reduced to a screen snapshot.
```

### `pty-wait-exit`

```text
pty-wait-exit NAME [--timeout-ms N] [--expect-status N]
pty wait-exit NAME ...
```

```text
agenterm-cu pty-wait-exit    (also: pty wait-exit)
  scope: observe    family: Headless PTY jobs

usage:
  pty-wait-exit NAME [--timeout-ms N] [--expect-status N]
  pty wait-exit NAME ...

Waits with a monotonic deadline until both the child and the terminal reader are finalized, then reads the exact exit status. An optional 0..=255 expected status is verified; timeout or mismatch is a typed nonzero failure.
```

### `pty-signal`

```text
pty-signal NAME --signal interrupt|terminate|stop|continue --expect delivered|exited|stopped|running
pty signal NAME ...
```

```text
agenterm-cu pty-signal    (also: pty signal)
  scope: actuate    family: Headless PTY jobs

usage:
  pty-signal NAME --signal interrupt|terminate|stop|continue --expect delivered|exited|stopped|running
  pty signal NAME ...

Signals only the native foreground process group selected by the exact job's retained PTY master. The explicit postcondition is fixed per signal: interrupt=delivered, terminate=exited, stop=stopped, continue=running. POSIX derives authority from tcgetpgrp(master) and verifies bounded exact members; direct ConPTY refuses before mutation because it exposes no retained foreground-process-set authority. The operation never substitutes input bytes, process-name scans or whole-job termination.
```

### `pty-stop`

```text
pty-stop NAME --expect stopped
pty stop NAME --expect stopped
```

```text
agenterm-cu pty-stop    (also: pty stop)
  scope: actuate    family: Headless PTY jobs

usage:
  pty-stop NAME --expect stopped
  pty stop NAME --expect stopped

Requires the explicit destructive postcondition, closes the exact epoch-scoped job tab through AgenTerm's own lifecycle, asks the now-empty authority to shut down, and verifies that its IPC endpoint disappears.
```

### `term-read`

```text
term-read WINDOW [--tail N] [--raw] [--max-bytes N]
term read WINDOW [options]
```

```text
agenterm-cu term-read    (also: term read)
  scope: observe    family: External terminals

usage:
  term-read WINDOW [--tail N] [--raw] [--max-bytes N]
  term read WINDOW [options]

Reads the longest showing TextArea/ScrollArea accessibility buffer in one exact desktop window. The native handle, owner pid and application are revalidated around the bounded tree walk. Padding lines are removed unless --raw; --tail selects recent lines and --max-bytes bounds returned UTF-8. This identity is not an AgenTerm @tab.
```

### `term-send`

```text
term-send WINDOW [--enter true|false] [--foreground] [--expect REGEX] [--verify-timeout-ms N] -- TEXT
term send WINDOW [options] -- TEXT
```

```text
agenterm-cu term-send    (also: term send)
  scope: actuate    family: External terminals

usage:
  term-send WINDOW [--enter true|false] --foreground [--expect REGEX] [--verify-timeout-ms N] -- TEXT
  term send WINDOW [options] -- TEXT

Binds one exact desktop window, process-start identity and terminal text node. No host currently claims a proven background literal-text route, so omission of --foreground fails before any receipt or input. The explicit transaction activates and verifies the exact window, focuses and verifies the selected node, reserves before input, restores prior focus, and requires a newly satisfied postcondition. By default that postcondition is escaped literal TEXT; --expect supplies a bounded regex. Inputs and expectations are persisted only as lengths and SHA-256. Any possibly dispatched but unattributed input remains outcome_unknown and must not be replayed blindly. AGENTERM_NO_ACTIVATE refuses this route before actuation.
```

### `term-wait`

```text
term-wait WINDOW PATTERN [--timeout-ms N] [--interval-ms N] [--max-bytes N]
term wait WINDOW PATTERN [options]
```

```text
agenterm-cu term-wait    (also: term wait)
  scope: observe    family: External terminals

usage:
  term-wait WINDOW PATTERN [--timeout-ms N] [--interval-ms N] [--max-bytes N]
  term wait WINDOW PATTERN [options]

Polls the independently read exact-window terminal buffer against one bounded Rust regular expression. Every poll revalidates handle plus owner identity. Success returns only the bounded match. Timeout returns lengths and SHA-256 digests but no terminal contents, avoiding MCU's five-line content leak. --timeout S and --interval S remain compatibility aliases.
```

### `terminal-list`

```text
terminal-list
terminal list
```

```text
agenterm-cu terminal-list    (also: terminal list)
  scope: observe    family: AgenTerm terminals

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  terminal-list
  terminal list

Returns the product control plane's structured ui-bootstrap
inventory. A reliable identity is server_scope_id + server_epoch + tab @N;
index and title are mutable human labels, not authority.
```

### `terminal-new`

```text
terminal-new [--title TEXT] [--parent @N] [--detached] [-- PROGRAM ARG...]
terminal new [options]
```

```text
agenterm-cu terminal-new    (also: terminal new)
  scope: actuate    family: AgenTerm terminals

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  terminal-new [--title TEXT] [--parent @N] [--detached] [-- PROGRAM ARG...]
  terminal new [options]

arguments:
  --title TEXT                  optional title, at most 4096 UTF-8 bytes
  --parent @N                   optional stable parent id from terminal-list
  --detached                    do not select the new tab
  -- PROGRAM ARG...             exact child command, at most 256 args / 1048576 bytes

Reserves a crash-persistent receipt, calls AgenTerm's existing new-window
control operation, then proves the returned @N exists in the same server scope
and epoch. Parent identity is read back when supplied. Command values and title
text are not copied into the receipt; only bounded byte counts are retained.
```

### `terminal-close`

```text
terminal-close --tab @N --expect closed
terminal close --tab @N --expect closed
```

```text
agenterm-cu terminal-close    (also: terminal close)
  scope: actuate    family: AgenTerm terminals

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  terminal-close --tab @N --expect closed
  terminal close --tab @N --expect closed

arguments:
  --tab @N                      exact stable tab id from terminal-list
  --expect closed               required destructive postcondition

Reserves a crash-persistent receipt, invokes the product's bounded kill-window
operation, then proves that exact @N is absent in the same server epoch. AgenTerm
owns child shutdown, parent promotion and last-tab behavior; ACU does not fake
tmux session semantics or terminate the application window itself.
```

### `terminal-read`

```text
terminal-read --tab @N [--max-bytes N]
terminal read --tab @N [--max-bytes N]
```

```text
agenterm-cu terminal-read    (also: terminal read)
  scope: observe    family: AgenTerm terminals

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  terminal-read --tab @N [--max-bytes N]
  terminal read --tab @N [--max-bytes N]

arguments:
  --tab @N                      stable tab id from terminal-list
  --max-bytes N                 UTF-8 response ceiling, 1..=1048576 (default 262144)

Reads vt100 screen contents through capture-pane --json.
This is a bounded screen snapshot, not an incremental raw-output cursor; the
reply says so explicitly and never invents byte-range parity.
```

### `terminal-send`

```text
terminal-send --tab @N -- TEXT
terminal send --tab @N -- TEXT
```

```text
agenterm-cu terminal-send    (also: terminal send)
  scope: actuate    family: AgenTerm terminals

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  terminal-send --tab @N -- TEXT
  terminal send --tab @N -- TEXT

arguments:
  --tab @N                      stable tab id from terminal-list
  --                            end options; remaining text is sent literally

Uses AgenTerm's own send-keys -l control operation. It
reserves an ACU receipt before sending and retains the server control receipt;
dead/non-writable tabs fail typed instead of falling back to desktop typing.
```

### `terminal-snapshot`

```text
terminal-snapshot --tab @N
terminal snapshot --tab @N
```

```text
agenterm-cu terminal-snapshot    (also: terminal snapshot)
  scope: observe    family: AgenTerm terminals

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  terminal-snapshot --tab @N
  terminal snapshot --tab @N

arguments:
  --tab @N                      stable tab id from terminal-list

Returns one bounded structured screen, including cell runs, styles, cursor,
terminal modes and completeness flags, together with server_scope_id and an
exact {server_epoch, sequence} cursor. This is an event-journal cursor, not a
raw PTY byte offset. Continue it with terminal-events.
```

### `terminal-scroll`

```text
terminal-scroll --tab @N up|down|page-up|page-down [ROWS]
terminal-scroll --tab @N top|bottom
terminal scroll ...
```

```text
agenterm-cu terminal-scroll    (also: terminal scroll)
  scope: actuate    family: AgenTerm terminals

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  terminal-scroll --tab @N up|down|page-up|page-down [ROWS]
  terminal-scroll --tab @N top|bottom
  terminal scroll ...

Mutates only the local scrollback viewport of one exact stable tab. It never sends cursor keys or mouse input to the terminal application. Before and after snapshots must retain the same server scope, epoch, tab and grid; the structured scrollback offset must match the product reply. Alternate-screen ownership is refused typed. A clamped boundary remains an honest success with viewport_changed=false.
```

### `terminal-screenshot`

```text
terminal-screenshot --tab @N --out ABSOLUTE_PATH
terminal screenshot --tab @N --out ABSOLUTE_PATH
```

```text
agenterm-cu terminal-screenshot    (also: terminal screenshot)
  scope: observe    family: AgenTerm terminals

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  terminal-screenshot --tab @N --out ABSOLUTE_PATH
  terminal screenshot --tab @N --out ABSOLUTE_PATH

Captures the already-active exact stable tab through AgenTerm's real renderer. Observe authority never activates or selects an inactive tab. The new destination is published atomically without replacing a regular file, symlink or reparse point. Success independently binds server scope, epoch, tab, screen generation, scrollback offset, grid, pixel dimensions, byte count and PNG SHA-256; no terminal text or image bytes enter stdout or audit receipts.
```

### `terminal-events`

```text
terminal-events --tab @N --epoch EPOCH --after SEQUENCE [--limit N]
terminal events --tab @N --epoch EPOCH --after SEQUENCE [--limit N]
```

```text
agenterm-cu terminal-events    (also: terminal events)
  scope: observe    family: AgenTerm terminals

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  terminal-events --tab @N --epoch EPOCH --after SEQUENCE [--limit N]
  terminal events --tab @N --epoch EPOCH --after SEQUENCE [--limit N]

arguments:
  --tab @N                      stable tab id from terminal-snapshot
  --epoch EPOCH                 exact server epoch from terminal-snapshot
  --after SEQUENCE              last consumed event sequence
  --limit N                     events scanned per page, 1..=64 (default 64)

Reads AgenTerm's bounded ordered UI delta journal. Restart, overwritten history
and a future sequence fail typed. The reply filters published events and screen
updates to the requested tab, but advances its cursor across every scanned
event so unrelated tabs cannot trap a reader in an infinite replay loop.
complete/truncated say whether the cursor reached the current server position.
```

### `terminal-output`

```text
terminal-output --tab @N [--cursor earliest|current|N] [--max-bytes N]
terminal output --tab @N ...
```

```text
agenterm-cu terminal-output    (also: terminal output)
  scope: observe    family: AgenTerm terminals

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  terminal-output --tab @N [--cursor earliest|current|N] [--max-bytes N]
  terminal output --tab @N ...

arguments:
  --tab @N                      stable tab id from terminal-list
  --cursor CURSOR               earliest/current bootstrap or prior next_cursor (default earliest)
  --max-bytes N                 bounded page size, 1..=1048576 (default 65536)

Reads the retained redacted raw PTY byte stream without UTF-8 loss. The reply
always carries base64 plus cursor bounds; valid UTF-8 pages also carry utf8.
Continue from next_cursor. A cursor older than earliest_cursor fails typed as
terminal_output_gap, and a cursor beyond current_cursor fails typed as
terminal_output_future_cursor. This cursor is independent of terminal-events.
```

### `terminal-wait`

```text
terminal-wait --tab @N (--contains TEXT|--exited|--finalized) [--timeout-ms N]
terminal wait --tab @N ...
```

```text
agenterm-cu terminal-wait    (also: terminal wait)
  scope: observe    family: AgenTerm terminals

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  terminal-wait --tab @N (--contains TEXT|--exited|--finalized) [--timeout-ms N]
  terminal wait --tab @N ...

arguments:
  --tab @N                      stable tab id from terminal-list
  --contains TEXT               current screen contains text
  --exited                      child exit code is present
  --finalized                   child exited and reader/parser tail drained
  --timeout-ms N                monotonic deadline, 1..=86400000 (default 5000)

Polls the typed control plane at 50 ms with a monotonic
deadline. Exactly one condition is required. --finalized is stronger than
--exited and should precede a final read when late PTY output matters.
```


## browser

### `page-js`

```text
page-js [--window HANDLE] --expression EXPR [--port N | --pid PID]
        [--target-id ID | --target-url SUB | --target-title SUB | --match SUB]
page read --js EXPR [...]                           (MCU spelling)
```

```text
agenterm-cu page-js    (also: page read)
  scope: observe    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  page-js [--window HANDLE] --expression EXPR [--port N | --pid PID]
          [--target-id ID | --target-url SUB | --target-title SUB | --match SUB]
  page read --js EXPR [...]                           (MCU spelling)

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --expression EXPR             JavaScript expression (MCU: --js)
  --port N                      CDP listener port on 127.0.0.1 (default 9222)
  --pid PID                     browser process whose explicit --remote-debugging-port is used (exclusive with --port)
  --target-id ID                exact CDP target id (from page-targets)
  --target-url SUB              case-insensitive url substring of the page target
  --target-title SUB            case-insensitive title substring of the page target
  --match SUB                   case-insensitive substring across target title, URL and description

Second knife: CDP Runtime.evaluate on 127.0.0.1:N (default 9222).
MAIN-world Function constructor is refused. No listener -> typed
unsupported with backend debugger-runtime-evaluate. One selector picks the
page target (tab): exact id, a case-insensitive substring of its URL/title,
or `--match` across title + URL + description. No match -> "cdp_target_not_found", more than one ->
"cdp_target_ambiguous" (candidates in error.detail). None keeps the first
page. The chosen id/url/title is echoed; a background tab is evaluated in
place, never selected or raised. Chrome / Brave must be started with
--remote-debugging-port=9222 for every CDP path; that port answers any
local process, so open it only while needed.
```

### `page-targets`

```text
page-targets [--port N | --pid PID] [--browser-profile SUB]
page targets [--port N | --pid PID] [--browser-profile SUB]     (MCU spelling)
```

```text
agenterm-cu page-targets    (also: page targets)
  scope: observe    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  page-targets [--port N | --pid PID] [--browser-profile SUB]
  page targets [--port N | --pid PID] [--browser-profile SUB]     (MCU spelling)

arguments:
  --port N                      CDP listener port on 127.0.0.1 (default 9222)
  --pid PID                     browser process whose explicit --remote-debugging-port is used (exclusive with --port)
  --browser-profile SUB         case-insensitive substring of the window's browser_profile (Chromium profile name)

The CDP /json inventory: id, url, title, type, attached, websocket
(offered or not). Pick a --target-id here. No listener -> typed
unsupported. `--pid` is exclusive with `--port` and performs an identity-bound
exact-process discovery without port scanning or command-line publication.
Chrome / Brave must be started with
--remote-debugging-port=9222 for every CDP path; that port answers any
local process, so open it only while needed.

--browser-profile SUB joins the inventory to one profile: only targets
whose title equals (exactly) a tab title of a window whose
browser_profile contains SUB are returned, each with profile_match:
"title", window and browser_profile. This is a heuristic and the reply
says so: one CDP port serves every profile of an instance and a target
carries no profile field, so a title shared across profiles matches each
of them and a differently spelled strip title (memory-saver suffix,
unset document title) is left out. No such window ->
browser_window_not_found before any socket is opened.
```

### `page-text`

```text
page-text --window HANDLE [--max-bytes N] [--within X,Y,W,H] [--depth N] [--max-nodes N]
page-text [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB) [--max-bytes N]
page text ...  |  page read [...]                   (MCU spellings)
```

```text
agenterm-cu page-text    (also: page text)
  scope: observe    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  page-text --window HANDLE [--max-bytes N] [--within X,Y,W,H] [--depth N] [--max-nodes N]
  page-text [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB) [--max-bytes N]
  page text ...  |  page read [...]                   (MCU spellings)

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --max-bytes N                 text budget (default 16 KiB, max 1 MiB)
  --within X,Y,W,H              a11y only: screen rectangle the rows must intersect
  --depth N                     a11y only: walk depth (default 64)
  --max-nodes N                 a11y only: walk budget (default 6000)
  --port N                      CDP listener port on 127.0.0.1 (default 9222)
  --pid PID                     browser process whose explicit --remote-debugging-port is used (exclusive with --port)
  --target-id ID                exact CDP target id (from page-targets)
  --target-url SUB              case-insensitive url substring of the page target
  --target-title SUB            case-insensitive title substring of the page target
  --match SUB                   case-insensitive substring across target title, URL and description

The page's visible text in reading order as compact rows {id, role,
text} (+ name when it differs, focused, editable / actionable) -- never a
screenshot. Two backends, one row shape, so the caller does not care
which answered:

--window HANDLE reads the a11y tree of that window (rows carry bounds;
backend ax / uia / at-spi2). On macOS Chromium only the active tab has a
web-area there, so a background tab needs `tab select` first. Web
static-text keeps its words in AXValue; a link / button is one row (its
inner text merged). A container role is never a row of its own even when
the backend hands back its whole concatenated text (Windows UIA reports a
Chromium web root as Document, whose TextPattern is the entire page): the
rows are the nodes inside it, each with an id worth clicking. Then invoke
--node / click --node. Walk budget defaults depth 64 / 6000 nodes because
the platform's 1000-node breadth-first budget is spent on browser chrome
before web content.

--target-id | --target-url | --target-title | --match [--port N] reads the CDP page
target instead (backend "cdp", via Accessibility.getFullAXTree; fallback
Runtime.evaluate innerText walk when that domain is unavailable). This
reaches a background tab in a background window and changes nothing
about which tab or window is active (focus_changed: false). Row `id` /
`node` is then the backend DOM node id that page click --node / page
fill --node take; no bounds (page find carries the box). One backend per
call. Default 16 KiB (max 1 MiB), truncated flag.
```

### `page-find`

```text
page-find [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB)
        (--selector CSS | --text SUB | --role R [--name SUB])
page find ...                                       (MCU spelling)
```

```text
agenterm-cu page-find    (also: page find)
  scope: observe    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  page-find [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB)
          (--selector CSS | --text SUB | --role R [--name SUB])
  page find ...                                       (MCU spelling)

arguments:
  --port N                      CDP listener port on 127.0.0.1 (default 9222)
  --pid PID                     browser process whose explicit --remote-debugging-port is used (exclusive with --port)
  --target-id ID                exact CDP target id (from page-targets)
  --target-url SUB              case-insensitive url substring of the page target
  --target-title SUB            case-insensitive title substring of the page target
  --match SUB                   case-insensitive substring across target title, URL and description
  --selector CSS                CSS selector (DOM.querySelectorAll)
  --text SUB                    case-insensitive substring of the node's words (AX tree; lifted to the enclosing button / link)
  --role R                      AX role (button, link, textbox, StaticText, ...; case-insensitive)
  --name SUB                    with --role: case-insensitive substring of the accessible name

The matching nodes of one CDP page target (a background tab in a
background window included; nothing is activated): {node, path, tag,
role, name, text, value, editable, ax_id, box}. --selector runs
DOM.querySelectorAll; --text and --role filter Accessibility.getFullAXTree
(a text hit inside a button / link is lifted to that control, so the
click lands on it). `node` is the backend DOM node id the actuators take
(page click --node / page fill --node); `box` is the layout box in
viewport CSS px. Zero matches -> cdp_node_not_found; more than 20 are
counted (total) and cut (truncated). The target selector rules are those
of page-js (cdp_target_not_found / cdp_target_ambiguous).
```

### `page-click`

```text
page-click [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB)
        ((--selector CSS | --text SUB | --node ID) | --x X --y Y) [--button left|right|middle] [--clicks N]
page click ...                                      (MCU spelling)
```

```text
agenterm-cu page-click    (also: page click)
  scope: actuate    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  page-click [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB)
          ((--selector CSS | --text SUB | --node ID) | --x X --y Y) [--button left|right|middle] [--clicks N]
  page click ...                                      (MCU spelling)

arguments:
  --port N                      CDP listener port on 127.0.0.1 (default 9222)
  --pid PID                     browser process whose explicit --remote-debugging-port is used (exclusive with --port)
  --target-id ID                exact CDP target id (from page-targets)
  --target-url SUB              case-insensitive url substring of the page target
  --target-title SUB            case-insensitive title substring of the page target
  --match SUB                   case-insensitive substring across target title, URL and description
  --selector CSS                CSS selector (DOM.querySelectorAll)
  --text SUB                    case-insensitive substring of the node's words (AX tree; lifted to the enclosing button / link)
  --node ID                     backend DOM node id from page-find / page-text rows
  --x X                         viewport CSS x coordinate (pixel compatibility path)
  --y Y                         viewport CSS y coordinate (pixel compatibility path)
  --button B                    left (default) | right | middle
  --clicks N                    1 (default) ..= 3 press / release pairs

Either resolves exactly one node (zero -> cdp_node_not_found, more ->
cdp_node_ambiguous with candidates; narrow the selector or pass --node), or freezes one
explicit --x/--y viewport point. The node path scrolls into view and takes the box centre.
The point path proves a rendered hit exists, then verifies trusted mousedown/up events at
the frozen point and always attempts release after a successful press. Both dispatch
Input.dispatchMouseEvent on that page target. The tab is not selected and the window is not raised
(focus_changed: false); focus emulation is switched on for the click and
off after so an unfocused page handles it normally. A node without a
layout box -> cdp_node_not_visible, nothing dispatched. Verified by
Node clicks are verified by reading the document and node back; point clicks use the trusted
event sequence so a legitimate no-op target can still be verified. Receipt reserved before
the dispatch, completed after.
```

### `page-download`

```text
page-download [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB)
        (--selector CSS | --text SUB | --node ID) --download-dir PATH [--wait-ms N]
page download ...                                   (MCU spelling)
```

```text
agenterm-cu page-download    (also: page download)
  scope: actuate    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  page-download [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB)
          (--selector CSS | --text SUB | --node ID) --download-dir PATH [--wait-ms N]
  page download ...                                   (MCU spelling)

arguments:
  --port N                      CDP listener port on 127.0.0.1 (default 9222)
  --pid PID                     browser process whose explicit --remote-debugging-port is used (exclusive with --port)
  --target-id ID                exact CDP target id (from page-targets)
  --target-url SUB              case-insensitive url substring of the page target
  --target-title SUB            case-insensitive title substring of the page target
  --match SUB                   case-insensitive substring across target title, URL and description
  --selector CSS                CSS selector for the download control
  --text SUB                    case-insensitive words of the download control
  --node ID                     backend DOM node id from page-find / page-text rows
  --download-dir PATH           existing absolute directory on the browser host
  --wait-ms N                   download start and completion deadline, 1..=300000

Resolves exactly one background-page control, takes the cross-process lock
for that CDP endpoint, then sets Browser.setDownloadBehavior to allowAndName
with download events enabled before dispatching the click. Completion is a
matching Browser.downloadWillBegin plus Browser.downloadProgress state=completed
and a final regular non-symlink file that stat resolves as download-dir/GUID.
The result includes guid, suggested filename, final path, state and decimal byte
counts; file contents are never opened or copied to stdout. The tab is not selected
and the browser window is not raised (focus_changed: false). Every exit attempts
to restore the browser download policy. A held endpoint lock, rejected policy,
canceled transfer, absent start event, deadline, or missing final file fails typed
as cdp_download_busy / _blocked / _canceled / _not_started / _timeout /
_file_missing instead of treating the click acknowledgement as success.
```

### `page-hover`

```text
page-hover [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB) --x X --y Y
page hover ...                                      (MCU spelling)
```

```text
agenterm-cu page-hover    (also: page hover)
  scope: actuate    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  page-hover [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB) --x X --y Y
  page hover ...                                      (MCU spelling)

arguments:
  --port N                      CDP listener port on 127.0.0.1 (default 9222)
  --pid PID                     browser process whose explicit --remote-debugging-port is used (exclusive with --port)
  --target-id ID                exact CDP target id (from page-targets)
  --target-url SUB              case-insensitive url substring of the page target
  --target-title SUB            case-insensitive title substring of the page target
  --match SUB                   case-insensitive substring across target title, URL and description
  --x X                         viewport CSS x coordinate
  --y Y                         viewport CSS y coordinate

Dispatches mouseMoved to one CDP page target without selecting
the tab or raising the window. Coordinates are finite non-negative viewport
CSS pixels. performed means Chromium accepted the event; verified compares
the trusted mousemove target and coordinates with elementFromPoint. The
deepest :hover node is auxiliary because headless/background Chromium may
not maintain it. A point outside
rendered page content is cdp_point_not_found. Receipt reserved before the
dispatch and completed after read-back.
```

### `page-scroll`

```text
page-scroll [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB) --x X --y Y [--dx DX] [--dy DY]
page scroll ...                                     (MCU spelling)
```

```text
agenterm-cu page-scroll    (also: page scroll)
  scope: actuate    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  page-scroll [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB) --x X --y Y [--dx DX] [--dy DY]
  page scroll ...                                     (MCU spelling)

arguments:
  --port N                      CDP listener port on 127.0.0.1 (default 9222)
  --pid PID                     browser process whose explicit --remote-debugging-port is used (exclusive with --port)
  --target-id ID                exact CDP target id (from page-targets)
  --target-url SUB              case-insensitive url substring of the page target
  --target-title SUB            case-insensitive title substring of the page target
  --match SUB                   case-insensitive substring across target title, URL and description
  --x X                         viewport CSS x coordinate
  --y Y                         viewport CSS y coordinate
  --dx DX                       horizontal wheel delta (default 0)
  --dy DY                       vertical wheel delta (default 120)

Dispatches mouseWheel at one viewport point without selecting
the tab or raising the window. The nearest scrollable container is captured
before dispatch and its exact offsets are read back afterwards. performed
means Chromium accepted the wheel event; verified requires left or top to
change. A boundary that cannot move is honestly performed but unverified
(no_observable_scroll_change). Receipt reserved before dispatch.
```

### `page-files`

```text
page-files [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB)
        (--selector CSS | --node ID) FILE...
page files [--node] ID FILE...                     (MCU spelling)
```

```text
agenterm-cu page-files    (also: page files)
  scope: actuate    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  page-files [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB)
          (--selector CSS | --node ID) FILE...
  page files [--node] ID FILE...                     (MCU spelling)

arguments:
  --port N                      CDP listener port on 127.0.0.1 (default 9222)
  --pid PID                     browser process whose explicit --remote-debugging-port is used (exclusive with --port)
  --target-id ID                exact CDP target id (from page-targets)
  --target-url SUB              case-insensitive url substring of the page target
  --target-title SUB            case-insensitive title substring of the page target
  --match SUB                   case-insensitive substring across target title, URL and description
  --selector CSS                CSS selector (DOM.querySelectorAll)
  --node ID                     backend DOM node id from page-find / page-text rows
  FILE...                       1..=16 absolute regular non-symlink files on the browser host

Resolves exactly one enabled input[type=file], refuses more
than one file unless the control has `multiple`, validates bounded regular
non-symlink local files, then calls DOM.setFileInputFiles on the selected CDP
target. FileList basename and size are read back exactly. Replies and persistent
receipts never contain local paths. The tab/window stays backgrounded and no
native file-picker is opened.
```

### `page-drag`

```text
page-drag [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB) --x1 X --y1 Y --x2 X --y2 Y
page drag X1 Y1 X2 Y2 ...                         (MCU spelling)
```

```text
agenterm-cu page-drag    (also: page drag)
  scope: actuate    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  page-drag [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB) --x1 X --y1 Y --x2 X --y2 Y
  page drag X1 Y1 X2 Y2 ...                         (MCU spelling)

arguments:
  --port N                      CDP listener port on 127.0.0.1 (default 9222)
  --pid PID                     browser process whose explicit --remote-debugging-port is used (exclusive with --port)
  --target-id ID                exact CDP target id (from page-targets)
  --target-url SUB              case-insensitive url substring of the page target
  --target-title SUB            case-insensitive title substring of the page target
  --match SUB                   case-insensitive substring across target title, URL and description
  --x1 X                        start viewport CSS x
  --y1 Y                        start viewport CSS y
  --x2 X                        end viewport CSS x
  --y2 Y                        end viewport CSS y

Moves to the start, presses left, moves to the end while
held, and always attempts release after a successful press. The selected tab
is not activated. performed requires all four CDP dispatches; verified requires
the target page to observe a trusted mousedown, held mousemove and mouseup at
the frozen endpoints. A zero-distance drag is rejected before any effect.
```

### `page-dialog`

```text
page-dialog [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB) [--dismiss] [--text TEXT] [--wait-ms N]
page dialog ...                                     (MCU spelling)
```

```text
agenterm-cu page-dialog    (also: page dialog)
  scope: actuate    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  page-dialog [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB) [--dismiss] [--text TEXT] [--wait-ms N]
  page dialog ...                                     (MCU spelling)

arguments:
  --port N                      CDP listener port on 127.0.0.1 (default 9222)
  --pid PID                     browser process whose explicit --remote-debugging-port is used (exclusive with --port)
  --target-id ID                exact CDP target id (from page-targets)
  --target-url SUB              case-insensitive url substring of the page target
  --target-title SUB            case-insensitive title substring of the page target
  --match SUB                   case-insensitive substring across target title, URL and description
  --dismiss                     dismiss instead of accept
  --text TEXT                   prompt response (accept only; redacted from receipt)
  --wait-ms N                   dialog-opening deadline, 1..=30000 (default 3000)

Enables Page events, waits for a real
Page.javascriptDialogOpening, reserves the receipt, then calls
Page.handleJavaScriptDialog and verifies Page.javascriptDialogClosed. A missing
dialog is cdp_dialog_not_open and performs nothing. Dialog message, default
prompt, supplied response and returned userInput are represented only by byte
counts in persistent/public evidence.
```

### `page-fill`

```text
page-fill [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB)
        (--selector CSS | --node ID) --text TEXT [--clear] [--submit]
page fill ...                                       (MCU spelling)
```

```text
agenterm-cu page-fill    (also: page fill)
  scope: actuate    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  page-fill [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB)
          (--selector CSS | --node ID) --text TEXT [--clear] [--submit]
  page fill ...                                       (MCU spelling)

arguments:
  --port N                      CDP listener port on 127.0.0.1 (default 9222)
  --pid PID                     browser process whose explicit --remote-debugging-port is used (exclusive with --port)
  --target-id ID                exact CDP target id (from page-targets)
  --target-url SUB              case-insensitive url substring of the page target
  --target-title SUB            case-insensitive title substring of the page target
  --match SUB                   case-insensitive substring across target title, URL and description
  --selector CSS                CSS selector (DOM.querySelectorAll)
  --node ID                     backend DOM node id from page-find / page-text rows
  --text TEXT                   what to insert (<= 64 KiB; empty only with --clear)
  --clear                       select everything first so the text replaces the field
  --submit                      then dispatch Enter key down / up

Resolves exactly one editable node (an enabled input / textarea /
contenteditable; anything else -> cdp_node_not_editable, nothing
written), DOM.focus, optional select-all (--clear), Input.insertText,
then reads .value (or the text content) back; --submit dispatches Enter
key events afterwards and echoes the document state. Focus emulation is
switched on for the write and off after, so a background tab accepts it
without being brought forward (focus_changed: false). Verified when the
read-back equals TEXT (--clear) or grew by exactly TEXT (append at the
caret); a mismatch is performed but unverified (value_mismatch), for a
page that rewrites its own field. Receipt reserved before the write,
        completed after.
```

### `page-type`

```text
page-type [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB) --text TEXT
page type TEXT ...                                  (MCU spelling)
```

```text
agenterm-cu page-type    (also: page type)
  scope: actuate    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  page-type [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB) --text TEXT
  page type TEXT ...                                  (MCU spelling)

arguments:
  --port N                      CDP listener port on 127.0.0.1 (default 9222)
  --pid PID                     browser process whose explicit --remote-debugging-port is used (exclusive with --port)
  --target-id ID                exact CDP target id (from page-targets)
  --target-url SUB              case-insensitive url substring of the page target
  --target-title SUB            case-insensitive title substring of the page target
  --match SUB                   case-insensitive substring across target title, URL and description
  --text TEXT                   1..=64 KiB inserted at the page's existing editable focus

Freezes the page's already-focused input, textarea or
contenteditable before reserving a receipt; missing or non-editable focus performs
nothing. Input.insertText then writes without selecting the tab or raising its window.
Verification requires the same focused-element path and exact value-length growth
containing the inserted text. Text and field values never enter public or persistent
evidence; only byte counts, element identity and the typed verdict do.
```

### `page-nav`

```text
page-nav [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB) --url URL [--wait-ms N]
page nav ...                                        (MCU spelling)
```

```text
agenterm-cu page-nav    (also: page nav)
  scope: actuate    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  page-nav [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB) --url URL [--wait-ms N]
  page nav ...                                        (MCU spelling)

arguments:
  --port N                      CDP listener port on 127.0.0.1 (default 9222)
  --pid PID                     browser process whose explicit --remote-debugging-port is used (exclusive with --port)
  --target-id ID                exact CDP target id (from page-targets)
  --target-url SUB              case-insensitive url substring of the page target
  --target-title SUB            case-insensitive title substring of the page target
  --match SUB                   case-insensitive substring across target title, URL and description
  --url URL                     where to go (needs a scheme: https:, data:, file:, about:)
  --wait-ms N                   how long to wait for Page.loadEventFired (default 10000, max 120000)

Page.navigate on that page target -- a background tab stays a
background tab, the window is not raised (focus_changed: false) -- then
waits up to --wait-ms for Page.loadEventFired. A navigation Chromium
refuses at once (errorText, e.g. a DNS failure) -> cdp_navigation_failed.
Verified when the load event fired (or readyState reads complete on the
new url); a timeout is performed but unverified (load_timeout) with the
url / title read so far. Reply: final_url, final_title, waited_ms,
receipt.
```

### `page-screenshot`

```text
page-screenshot [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB) --out PATH [--replace] [--activate]
page screenshot ...                                 (MCU spelling)
```

```text
agenterm-cu page-screenshot    (also: page screenshot)
  scope: observe    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  page-screenshot [--port N | --pid PID] (--target-id ID | --target-url SUB | --target-title SUB | --match SUB) --out PATH [--replace] [--activate]
  page screenshot ...                                 (MCU spelling)

arguments:
  --port N                      CDP listener port on 127.0.0.1 (default 9222)
  --pid PID                     browser process whose explicit --remote-debugging-port is used (exclusive with --port)
  --target-id ID                exact CDP target id (from page-targets)
  --target-url SUB              case-insensitive url substring of the page target
  --target-title SUB            case-insensitive title substring of the page target
  --match SUB                   case-insensitive substring across target title, URL and description
  --out PATH                    where the PNG is written (refuses an existing file without --replace)
  --replace                     overwrite --out
  --activate                    actuate: Page.bringToFront first (the only CDP verb that changes the active tab)

Page.captureScreenshot (PNG) of that page target, written to --out with
its sha256 in the reply. Chromium does not paint a tab that is not
visible, so a background or occluded tab may answer
cdp_screenshot_unavailable; this verb never activates the tab to get a
picture. --activate is the one explicit opt-in: it runs Page.bringToFront
first, needs the actuate grant, writes a receipt, and replies
focus_changed: true. Prefer page text / page find, which need no pixels.
```

### `tab-list`

```text
tab-list --window HANDLE
tab list --window HANDLE                            (MCU spelling)
```

```text
agenterm-cu tab-list    (also: tab list)
  scope: observe    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  tab-list --window HANDLE
  tab list --window HANDLE                            (MCU spelling)

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)

The browser tab strip through the a11y tree: index, title, selected per
tab. The strip is read on every backend, whatever it calls the roles:
macOS AX tab-group / radio-button, AT-SPI2 and UIA both "page tab list" /
"page tab" (roles are compared on their alphanumeric core, so separator
and AX-prefix spellings all match). A background tab's content is never in
the tree -- only its row is; use tab select, or the CDP page verbs.

Evidence: macOS is proven on a real Brave instance
(scripts/cu-brave-live-smoke.sh). Linux and Windows are code-complete with
no live run yet; scripts/cu-linux-smoke.sh and
scripts/qjs/cu-windows-browser-smoke.qjs carry the journey and exit with a
typed SKIP until a host with a Chromium-family browser runs them.
```

### `tab-select`

```text
tab-select --window HANDLE (--title SUB | --index N)
tab select ...                                      (MCU spelling)
```

```text
agenterm-cu tab-select    (also: tab select)
  scope: actuate    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  tab-select --window HANDLE (--title SUB | --index N)
  tab select ...                                      (MCU spelling)

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --title SUB                   tab title substring (not with --index)
  --index N                     tab index from tab-list (not with --title)

Presses one tab-strip row in the background so it becomes the window's
active tab; never raises or activates the window. No such tab ->
"a11y_tab_not_found", two title hits -> "a11y_tab_ambiguous"; verified by
reading selected back (already selected = verified no-op). The a11y
fallback when no CDP port is open.
```

### `tab-close`

```text
tab-close --window HANDLE (--title T --exact | --index N) --expect gone [--port N]
tab close ...                                       (MCU spelling)
```

```text
agenterm-cu tab-close    (also: tab close)
  scope: actuate    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  tab-close --window HANDLE (--title T --exact | --index N) --expect gone [--port N]
  tab close ...                                       (MCU spelling)

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --title T                     the tab's exact title (from tab-list); needs --exact
  --index N                     the tab's strip index from tab-list (0-based); the exact selector for same-title duplicates
  --exact                       required with --title: the title is matched exactly, never as a substring
  --expect gone                 checkable postcondition: one fewer strip row with that title reads back
  --port N                      CDP port: close by Target.closeTarget when the title names exactly one page target of the instance; else the a11y path

Destructive, so gated like close: --window H plus --title T --exact
(exact, case-sensitive title equality; no match -> a11y_tab_not_found,
two -> a11y_tab_ambiguous, name one with --index) or --index N (tab-list
order) names the tab, the strip snapshot is written to the receipt
before anything is pressed, and --expect gone is the postcondition read
back from the tab strip (one fewer row with that title, up to 2.5 s).
Any missing part -> refused (destructive_gate) with nothing performed.
The press goes to the tab row's own close button (the button child of
the Chromium tab radio-button). macOS Chromium exposes that button on
the selected (or hovered) tab only, so a background tab is selected
first in the same window (never raising it), closed, and the previously
selected tab is pressed again; the reply says selection_restored
true|false and select_first. With --port N and a listener, a title that
names exactly one page target of the whole instance is closed by
Target.closeTarget instead (via cdp-close-target, no selection change);
no listener, no such target, or two targets fall back to the a11y path
(cdp_fallback says why). A keyboard shortcut is never substituted.
Reply: performed, verified, via, before / after strip rows, receipt.
```

### `browser-profiles`

```text
browser-profiles [--app SUB]
browser profiles [--app SUB]                        (MCU spelling)
```

```text
agenterm-cu browser-profiles    (also: browser profiles)
  scope: observe    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  browser-profiles [--app SUB]
  browser profiles [--app SUB]                        (MCU spelling)

arguments:
  --app SUB                     Brave Origin | Brave Browser | Google Chrome (substring; default: the one running)

Reads the application's Chromium `Local State` (profile.info_cache:
directory -> display name; profile.last_used) and joins each profile to
the inventory windows whose browser_profile equals its name. Rows: {name,
directory, last_used, windows: [handles]}; browser windows whose profile
name is not in Local State are listed under unlisted_windows. --app is a
catalog substring (Brave Origin -> ~/Library/Application
Support/BraveSoftware/Brave-Origin, Brave Browser -> .../Brave-Browser,
Google Chrome -> .../Google/Chrome; Linux under ~/.config; Windows Brave
Browser and Google Chrome under ~/AppData/Local, each including its `User
Data` directory); omitted, the one running catalog application is used
(none -> browser_app_not_found, several -> browser_app_ambiguous). Brave
Origin has no Windows mapping. Any other application -> typed unsupported.
Never touches the browser.
```

### `browser-open`

```text
browser-open --profile NAME [--url URL] [--app SUB] [--timeout-ms N]
browser open ...                                    (MCU spelling)
```

```text
agenterm-cu browser-open    (also: browser open)
  scope: actuate    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  browser-open --profile NAME [--url URL] [--app SUB] [--timeout-ms N]
  browser open ...                                    (MCU spelling)

arguments:
  --profile NAME                profile name from browser-profiles (exact, else unique case-insensitive substring)
  --url URL                     open this URL in the profile (a tab of its window when it has one)
  --app SUB                     Brave Origin | Brave Browser | Google Chrome (substring; default: the one running)
  --timeout-ms N                how long to wait for the window (default 8000, max 120000)

Resolves NAME to its profile directory (exact name first, then a unique
case-insensitive substring; browser_profile_not_found /
browser_profile_ambiguous carry the candidates) and runs macOS `open -na
<app> --args --profile-directory=<dir> [URL]`: the Chromium process
singleton hands that command line to the running instance, which opens a
window of the profile (or, with a URL, a tab in the profile's existing
window) and the user's browser is never quit or restarted. Then polls the
window inventory (default 8000 ms) until a window with that
browser_profile appears that was not in the before snapshot, or -- when
the profile already had a window and a URL was given -- until that
window's title changes. Reply: {handle, browser_profile, title, created,
tab_index, tab_title, tabs: {before, after}} -- the new tab is the
selected row the window's strip gained (diffed against the strip read
before the launch; null when none can be told apart) -- plus the receipt
(reserved before `open`, completed / failed after the read-back); timeout
-> browser_window_not_found. `open` activates the browser, so this is
actuation; nothing here needs a CDP port. Without --url the profile's own
last session is restored: after a browser restart only the last-used
profile comes back by itself, and `browser open --profile X` is how the
other profiles' windows are brought back.
```

### `browser-tabs`

```text
browser-tabs [--profile-instance-id ID | --connection-id ID] [--match SUB | --tab-id N]
browser tabs ...                                    (MCU spelling)
```

```text
agenterm-cu browser-tabs    (also: browser tabs)
  scope: observe    family: Browser page & tabs

usage (after global flags):
  browser-tabs [--profile-instance-id ID | --connection-id ID] [--match SUB | --tab-id N]
  browser tabs ...                                    (MCU spelling)

Returns the complete bounded MV3 tab inventory for one uniquely resolved live profile connection: stable tab/window ids, active state, title and URL, including background tabs. With no selector exactly one live connection must exist. Profile prefixes and direct connection ids are revalidated against the full live inventory; duplicate connections for one profile fail closed. --match filters title or URL case-insensitively and zero matches is success. A truncated connection or tab inventory is an error, never a partial success. Desktop focus is bracketed through a settle window and must remain unchanged. Human profile names and --app are deliberately excluded until they can be cryptographically bound to the extension profile instance.
```

### `browser-bridge-setup`

```text
browser-bridge-setup
browser bridge setup
```

```text
agenterm-cu browser-bridge-setup
  scope: actuate    family: Browser page & tabs

usage:
  browser-bridge-setup
  browser bridge setup

Materializes the fixed ACU MV3 extension and registers this exact agenterm-cu executable as its current-user Native Messaging host for supported existing Chromium-family profiles. The receipt always reports extension_loaded=false and manual_activation_required=true: setup does not launch a browser or claim that Chromium loaded the unpacked extension.
```

### `browser-bridge-connections`

```text
browser-bridge-connections
browser bridge connections
```

```text
agenterm-cu browser-bridge-connections
  scope: observe    family: Browser page & tabs

usage:
  browser-bridge-connections
  browser bridge connections

Lists a bounded inventory of current-user bridge connections only after each record's exact process-start identity and fixed extension/native-host endpoint are validated. Stale or foreign records are not returned.
```

### `browser-bridge-status`

```text
browser-bridge-status CONNECTION_ID
browser bridge status CONNECTION_ID
```

```text
agenterm-cu browser-bridge-status
  scope: observe    family: Browser page & tabs

usage:
  browser-bridge-status CONNECTION_ID
  browser bridge status CONNECTION_ID

arguments:
  CONNECTION_ID                 exact 64-character lowercase hexadecimal id from browser-bridge-connections

Routes the closed protocol-v3 status request only to that exact current-user and process-identity-bound connection. It returns the fixed extension version, persistent profile instance identity and the exact supported command set. A missing, stale, foreign or malformed identity fails typed.
```

### `browser-bridge-tabs`

```text
browser-bridge-tabs CONNECTION_ID
browser bridge tabs CONNECTION_ID
```

```text
agenterm-cu browser-bridge-tabs
  scope: observe    family: Browser page & tabs

usage:
  browser-bridge-tabs CONNECTION_ID
  browser bridge tabs CONNECTION_ID

arguments:
  CONNECTION_ID                 exact 64-character lowercase hexadecimal id from browser-bridge-connections

Returns the extension's independently bounded tab inventory for one exact live connection. It does not select, activate or focus a tab or window.
```

### `browser-bridge-attach`

```text
browser-bridge-attach CONNECTION_ID --tab-id N --session ID --lease TOKEN [--ttl-seconds N] [--timeout-ms N]
browser bridge attach ...
```

```text
agenterm-cu browser-bridge-attach    (also: browser bridge attach)
  scope: actuate    family: Browser page & tabs

usage:
  browser-bridge-attach CONNECTION_ID --tab-id N --session ID --lease TOKEN [--ttl-seconds N] [--timeout-ms N]
  browser bridge attach ...

Resolves the bridge's persistent profile instance identity, proves its connection inventory is complete and unique, verifies the exact tab before and after publishing the canonical profile+tab target lock, and proves desktop focus stayed unchanged. A newly acquired lock is rolled back if post-lock verification fails. The lease is never returned. TTL must cover the entire deadline plus five seconds; another session receives runtime_lock_conflict. Session end, explicit lock release, or expiry releases ownership.
```

### `browser-bridge-reload`

```text
browser-bridge-reload CONNECTION_ID --tab-id N --session ID --lease TOKEN [--ttl-seconds N] [--timeout-ms N]
browser bridge reload ...
```

```text
agenterm-cu browser-bridge-reload    (also: browser bridge reload)
  scope: actuate    family: Browser page & tabs

usage:
  browser-bridge-reload CONNECTION_ID --tab-id N --session ID --lease TOKEN [--ttl-seconds N] [--timeout-ms N]
  browser bridge reload ...

Acquires or renews the same canonical tab lock under a TTL that outlives the whole deadline, requires a complete unique Profile inventory, asks the fixed MV3 worker to replace its Native Messaging connection, and waits for the old process-bound host to disappear and exactly one new host with the same persistent Profile instance identity to appear. The receipt says reload_scope=native-connection: this is reliable control-channel recovery, not a claim that Chromium hot-loaded new extension code. Success proves exact tab identity/presentation and desktop focus stayed unchanged; it never hides focus theft by restoring it. Truncation, zero or multiple replacements, identity drift, tab drift, lost session authority, and deadline expiry fail typed. A post-ack failure reports the performed connection effect and replacement identity so callers cannot retry as if no effect occurred.
```

### `browser-bridge-windows`

```text
browser-bridge-windows CONNECTION_ID
browser bridge windows CONNECTION_ID
```

```text
agenterm-cu browser-bridge-windows
  scope: observe    family: Browser page & tabs

usage:
  browser-bridge-windows CONNECTION_ID
  browser bridge windows CONNECTION_ID

arguments:
  CONNECTION_ID                 exact 64-character lowercase hexadecimal id from browser-bridge-connections

Returns a bounded window inventory for the exact extension connection: stable Chromium window id, closed state vocabulary, focus flag, bounds, tab count and active-tab identity/title. It never activates, focuses, moves or resizes a window.
```

### `browser-bridge-window-state`

```text
browser-bridge-window-state CONNECTION_ID --window-id N --state normal|minimized|maximized
browser bridge window-state ...
```

```text
agenterm-cu browser-bridge-window-state
  scope: actuate    family: Browser page & tabs

usage:
  browser-bridge-window-state CONNECTION_ID --window-id N --state normal|minimized|maximized
  browser bridge window-state ...

arguments:
  CONNECTION_ID                 exact 64-character lowercase hexadecimal id from browser-bridge-connections
  --window-id N                 exact positive Chromium window id from browser-bridge-windows
  --state STATE                 normal, minimized or maximized

Changes only one exact background Chromium window through the exact MV3 connection. A foreground target is refused. Before the extension effect, the native layer captures the exact desktop foreground window; it verifies that identity afterward and restores it if Chromium changed application focus. Success proves requested state, unchanged browser focus ownership, unchanged desktop foreground ownership, tab count and active-tab identity. Failed postconditions trigger bounded rollback; uncertain state or failed focus restoration is returned as a typed failure.
```

### `browser-bridge-window-open`

```text
browser-bridge-window-open CONNECTION_ID --url URL [--focused]
browser bridge window-open ...
```

```text
agenterm-cu browser-bridge-window-open
  scope: actuate    family: Browser page & tabs

usage:
  browser-bridge-window-open CONNECTION_ID --url URL [--focused]
  browser bridge window-open ...

arguments:
  CONNECTION_ID                 exact 64-character lowercase hexadecimal id from browser-bridge-connections
  --url URL                     initial URL for the new ordinary Chromium window
  --focused                     explicitly focus the new window; omitted preserves existing browser focus

Creates one normal window through the exact fixed MV3 connection and returns its stable Chromium window/tab identity. The default verifies that browser focus ownership did not change; --focused is an explicit foreground request and verifies that the new window owns browser focus. A failed postcondition removes the created window and restores the prior browser focus, or fails typed if rollback cannot be proven.
```

### `browser-bridge-debug-read`

```text
browser-bridge-debug-read CONNECTION_ID --tab-id N [--max-frames N] [--max-depth N] [--max-scan N] [--max-results N]
browser bridge debug-read ...
```

```text
agenterm-cu browser-bridge-debug-read
  scope: observe    family: Browser page & tabs

usage:
  browser-bridge-debug-read CONNECTION_ID --tab-id N [--max-frames N] [--max-depth N]
      [--max-scan N] [--max-results N]
  browser bridge debug-read ...

arguments:
  CONNECTION_ID                 exact 64-character lowercase hexadecimal id from browser-bridge-connections
  --tab-id N                    exact positive Chromium tab id from browser-bridge-tabs
  --max-frames N                cross-frame bound, 1..=64 (default 64)
  --max-depth N                 AX depth bound, 1..=20 (default 20)
  --max-scan N                  AX nodes scanned, 1..=5000 (default 5000)
  --max-results N               AX rows returned, 1..=1000 (default 1000)

Reads only bounded AX frame/node identity, role and name facts. Form/control values and arbitrary DOM attributes are excluded. Success requires unchanged tab activation and window focus plus proven debugger detach; every violation fails typed.
```

### `browser-bridge-debug-invoke`

```text
browser-bridge-debug-invoke CONNECTION_ID --tab-id N --frame-id ID --backend-node-id N --role ROLE --name NAME --action focus|press [--expect-role ROLE --expect-name NAME] [--lock-ttl-seconds N] [--timeout-ms N]
```

```text
agenterm-cu browser-bridge-debug-invoke
  scope: actuate    family: Browser page & tabs

usage:
  browser-bridge-debug-invoke CONNECTION_ID --tab-id N --frame-id ID --backend-node-id N
      --role ROLE --name NAME --action focus|press [--expect-role ROLE --expect-name NAME]
      [--lock-ttl-seconds N] [--timeout-ms N]

arguments: CONNECTION_ID, --tab-id, --frame-id, --backend-node-id, --role, --name, --action, --expect-role, --expect-name, --lock-ttl-seconds, --timeout-ms.

Focuses or presses one exact frame/backend-node identity from debug-read, guarded by its role and name. Press requires an exact role/name postcondition. The command requires global --request-id, --session and --session-lease, owns the canonical Profile/tab lock, preserves browser and desktop focus, and proves debugger detach. A delivered-but-unverified effect is outcome_unknown and is never replayed automatically.
```

### `browser-bridge-debug-type`

```text
browser-bridge-debug-type CONNECTION_ID --tab-id N --frame-id ID --backend-node-id N --role ROLE --name NAME --text TEXT [--lock-ttl-seconds N] [--timeout-ms N]
```

```text
agenterm-cu browser-bridge-debug-type
  scope: actuate    family: Browser page & tabs

usage:
  browser-bridge-debug-type CONNECTION_ID --tab-id N --frame-id ID --backend-node-id N
      --role ROLE --name NAME --text TEXT [--lock-ttl-seconds N] [--timeout-ms N]

arguments: CONNECTION_ID, --tab-id, --frame-id, --backend-node-id, --role, --name, --text, --lock-ttl-seconds, --timeout-ms.

Replaces one exact closed-tree editable value and verifies exact equality. The public result and persistent audit contain only the UTF-8 byte count, never the text. Global request identity, canonical Profile/tab ownership, unchanged presentation and proven debugger detach are mandatory.
```

### `browser-bridge-debug-files`

```text
browser-bridge-debug-files CONNECTION_ID --tab-id N --frame-id ID --backend-node-id N --role ROLE --name NAME FILE... [--lock-ttl-seconds N] [--timeout-ms N]
```

```text
agenterm-cu browser-bridge-debug-files
  scope: actuate    family: Browser page & tabs

usage:
  browser-bridge-debug-files CONNECTION_ID --tab-id N --frame-id ID --backend-node-id N
      --role ROLE --name NAME FILE... [--lock-ttl-seconds N] [--timeout-ms N]

arguments: CONNECTION_ID, --tab-id, --frame-id, --backend-node-id, --role, --name, FILE, --lock-ttl-seconds, --timeout-ms.

Sets 1..=32 regular non-symlink files on one exact closed-tree input[type=file] without opening a picker. The renderer verifies FileList basename and size; public and persistent receipts omit local paths. Global request identity, canonical Profile/tab ownership, unchanged presentation and proven debugger detach are mandatory.
```

### `browser-session-start`

```text
browser-session-start NAME --browser PATH [--bridge] [--ready-timeout-ms N] [--ttl-ms N]
browser session-start ...
```

```text
agenterm-cu browser-session-start    (also: browser session-start)
  scope: actuate    family: Browser page & tabs

usage:
  browser-session-start NAME --browser PATH [--bridge] [--ready-timeout-ms N] [--ttl-ms N]

Starts one named browser automation session with an exact browser executable path. --bridge first installs the same executable as the fixed current-user Native Messaging host and asks Chromium to load the fixed extension in the isolated profile; only a later exact live connection proves activation. Readiness is bounded to 1000..=60000 ms (default 15000); lifetime is bounded to 1000..=86400000 ms (default 3600000).
```

### `browser-session-list`

```text
browser-session-list
browser session-list
```

```text
agenterm-cu browser-session-list    (also: browser session-list)
  scope: observe    family: Browser page & tabs

usage:
  browser-session-list

Lists the browser automation sessions owned by this runtime.
```

### `browser-session-status`

```text
browser-session-status NAME
browser session-status NAME
```

```text
agenterm-cu browser-session-status    (also: browser session-status)
  scope: observe    family: Browser page & tabs

usage:
  browser-session-status NAME

Observes one named browser automation session.
```

### `browser-session-stop`

```text
browser-session-stop NAME --expect stopped [--timeout-ms N]
browser session-stop ...
```

```text
agenterm-cu browser-session-stop    (also: browser session-stop)
  scope: actuate    family: Browser page & tabs

usage:
  browser-session-stop NAME --expect stopped [--timeout-ms N]

Stops one named browser automation session and requires the literal stopped postcondition. The deadline is 1000..=60000 ms (default 15000).
```

### `browser-session-remove`

```text
browser-session-remove NAME --expect stopped|failed
browser session-remove ...
```

```text
agenterm-cu browser-session-remove    (also: browser session-remove)
  scope: actuate    family: Browser page & tabs

usage:
  browser-session-remove NAME --expect stopped|failed

Removes one named browser automation session only when its recorded terminal state exactly matches the caller's literal expectation and both recorded processes are independently verified absent.
```

### `page`

```text
page read [--js EXPR] | page targets | page text | page find | page click | page download | page hover | page scroll | page drag | page dialog | page files | page fill | page nav | page screenshot
page [<other>]                                      (typed unsupported)
```

```text
agenterm-cu page
  scope: observe    family: Browser page & tabs

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  page read [--js EXPR] | page targets | page text | page find | page click | page download | page hover | page scroll | page drag | page dialog | page files | page fill | page nav | page screenshot
  page [<other>]                                      (typed unsupported)

MCU page: `page read --js` -> page-js, `page read` (no --js) -> the CDP
page-text, `page targets` -> page-targets, `page text` -> page-text (a11y
with --window, CDP with a target selector), and the CDP background-tab
verbs `page find` / `page click` / `page download` / `page hover` / `page scroll` / `page
drag` / `page dialog` / `page files` / `page fill` / `page nav` / `page screenshot` map to their typed CDP verbs. Any
other page sub-verb answers typed unsupported.
```


## clipboard

### `clipboard-read`

```text
clipboard-read [--metadata-only | --type T [--max-bytes N] [--out PATH [--replace]]]
clipboard [read] [T] [...]                          (MCU spelling)
clip                                                (text only)
```

```text
agenterm-cu clipboard-read    (also: clip, clipboard, clipboard read)
  scope: observe    family: Clipboard

usage (after the global flags, e.g. agenterm-cu --target current --grant observe):
  clipboard-read [--metadata-only | --type T [--max-bytes N] [--out PATH [--replace]]]
  clipboard [read] [T] [...]                          (MCU spelling)
  clip                                                (text read)

arguments:
  --metadata-only               list native formats without reading any clipboard payload
  --type T                      one native type name (positional T also accepted)
  --max-bytes N                 byte budget (default 1 MiB, max 16 MiB); requires --type
  --out PATH                    write the bytes (0600); requires --type
  --replace                     overwrite --out

--metadata-only returns provider/type inventory with payload_read=false and
never asks the host for clipboard contents. No --type: Unicode text plus host
type names. --type T: one native type as bounded bytes (default 1 MiB, max 16
MiB), sha256, utf8 or base64. --out writes the bytes (0600; --replace
overwrites). Requires observe. `clip` with no arguments reads text; `clip TEXT`
publishes direct UTF-8 text through clipboard-write. `clipboard` with no
sub-command is clipboard-read.
```

### `clipboard-write`

```text
clipboard-write --type T (--path P | --text TEXT)
clipboard write T P                                 (MCU rich-file spelling)
clip TEXT                                           (MCU text spelling)
```

```text
agenterm-cu clipboard-write    (also: clipboard write)
  scope: actuate    family: Clipboard

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  clipboard-write --type T (--path P | --text TEXT)
  clipboard write T P                                 (MCU rich-file spelling)
  clip TEXT                                           (MCU text spelling)

arguments:
  --type T                      native type name; direct text requires text/plain;charset=utf-8
  --path P                      regular file (<= 16 MiB)
  --text TEXT                   direct UTF-8 text (<= 1 MiB), mutually exclusive with --path

Publish one native type from a regular file (<= 16 MiB), or direct UTF-8 text
(<= 1 MiB), and verify its byte length and sha256 by native read-back. The
receipt never echoes direct text (actuate).
```

### `clipboard-write-file`

```text
clipboard-write-file --path P
clipboard write-file P                              (MCU spelling)
```

```text
agenterm-cu clipboard-write-file    (also: clipboard write-file)
  scope: actuate    family: Clipboard

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  clipboard-write-file --path P
  clipboard write-file P                              (MCU spelling)

arguments:
  --path P                      file to reference

Put a file reference on the clipboard (macOS POSIX file / Linux
text/uri-list / Windows CF_HDROP), not the file bytes (actuate).
```

### `clipboard-clear`

```text
clipboard-clear [--apply]
clipboard clear [--apply]                           (MCU spelling)
```

```text
agenterm-cu clipboard-clear    (also: clipboard clear)
  scope: actuate    family: Clipboard

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  clipboard-clear [--apply]
  clipboard clear [--apply]                           (MCU spelling)

arguments:
  --apply                       perform it; without --apply the reply is planned only

Empty the clipboard. Without --apply this is planned and performs nothing
(actuate).
```

### `copy`

```text
copy --window HANDLE [--name PAT [--role ROLE]]
```

```text
agenterm-cu copy
  scope: actuate    family: Clipboard

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  copy --window HANDLE [--name PAT [--role ROLE]]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --name PAT                    unique showing node whose name contains PAT (wait matching)
  --role ROLE                   narrow --name to one role

Copies AT-SPI Text.GetText onto the native clipboard (Linux X11:
SetSelectionOwner, not xclip). addressing=accessibility-tree via=gettext.
--name targets the unique showing named node. --window without --name
copies that same path on the showing focused node (same innermost Text
candidate as get-text --window; con Command via=gettext on a second con
that never steals the resident control socket; Chrome GetTextField;
Reasonix Message Reasonix... under scripts/reasonix-desktop-a11y.sh). Never
XTest when --window is set. A node with no Text interface typed-fails
(never XTest / --coords / screenshot). Close the circuit with paste
--window (no --text / no --name) then get-text --window / wait
--text-equals; copy matched.text does not count.
```

### `paste`

```text
paste --window HANDLE [--name PAT [--role ROLE]] [--allow-browser-chrome] [--text TEXT] [-- TEXT]
```

```text
agenterm-cu paste
  scope: actuate    family: Clipboard

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  paste --window HANDLE [--name PAT [--role ROLE]] [--allow-browser-chrome] [--text TEXT] [-- TEXT]

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --name PAT                    unique showing node whose name contains PAT (wait matching)
  --role ROLE                   narrow --name to one role
  --allow-browser-chrome        with --window and no --name: write browser chrome (omnibox, toolbar, tab strip) instead of refusing
  --text TEXT                   seed the clipboard first (the write still reads the clipboard)
  --                            ends flag parsing so --text may start with a dash

Writes clipboard text via native AT-SPI EditableText / Text
(addressing=accessibility-tree). --text only seeds the clipboard; the field
write always reads the clipboard. --name targets the unique showing named
field. --window without --name writes that same path on the showing
focused node (same innermost Text candidate as get-text --window; con
Command via=editable-text on a second con that never steals the resident
control socket; Chrome GetTextField; Reasonix Message Reasonix...). Never
XTest when --window is set. A node with no writeable text interface
typed-fails (never XTest / --coords / screenshot). Close the circuit with
get-text --window / wait --text-equals; paste matched.text does not count.
`--` ends flag parsing.
When the window's focused control is browser chrome (omnibox, toolbar, tab
strip) the write is refused with focused_node_is_browser_chrome unless
--allow-browser-chrome is passed; pass --name to address a page control
instead.
```


## placement

### `window-place`

```text
window-place --action <id> [--window HANDLE] [--x X --y Y --width W --height H]
    ids: center|fullscreen|left-half|right-half|top-half|bottom-half
         upper-left|lower-left|upper-right|lower-right
         next-third|previous-third|next-display|previous-display
         larger|smaller|undo|redo|move|resize|frame  (or SpectacleWindowAction* constants)
```

```text
agenterm-cu window-place
  scope: actuate    family: Window placement

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  window-place --action <id> [--window HANDLE] [--x X --y Y --width W --height H]
      ids: center|fullscreen|left-half|right-half|top-half|bottom-half
           upper-left|lower-left|upper-right|lower-right
           next-third|previous-third|next-display|previous-display
           larger|smaller|undo|redo|move|resize|frame  (or SpectacleWindowAction* constants)

arguments:
  --action ID                   placement action id (positional ID also accepted)
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --x/--y/--width/--height N    the rect for --action frame; all four or none

The product placement catalog shared with the desktop-host menu and
global shortcuts. `frame`, `movewin`, `resize` and `maximize` are the MCU
shorthands and answer as window-place.
```

### `frame`

```text
frame HANDLE|--window H --x X --y Y --width W --height H   (or HANDLE X Y W H)
```

```text
agenterm-cu frame
  scope: actuate    family: Window placement    reply.command: window-place

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  frame HANDLE|--window H --x X --y Y --width W --height H   (or HANDLE X Y W H)

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --x/--y/--width/--height N    the requested rect (positional X Y W H also accepted)

Alias of window-place --action frame. The reply's command is
window-place.
```

### `movewin`

```text
movewin HANDLE|--window H --x X --y Y [--expect XxY]   (or HANDLE X Y)
```

```text
agenterm-cu movewin
  scope: actuate    family: Window placement    reply.command: window-place

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  movewin HANDLE|--window H --x X --y Y [--expect XxY]   (or HANDLE X Y)

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --x/--y N                     new origin (positional X Y also accepted)
  --expect XxY                  checkable postcondition: the window origin reads back at X,Y

Alias of window-place move; keeps the current size. The reply's command
is window-place.
```

### `resize`

```text
resize HANDLE|--window H --width W --height H [--expect WxH]   (or HANDLE W H)
```

```text
agenterm-cu resize
  scope: actuate    family: Window placement    reply.command: window-place

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  resize HANDLE|--window H --width W --height H [--expect WxH]   (or HANDLE W H)

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --width/--height N            new size (positional W H also accepted)
  --expect WxH                  checkable postcondition: the window size reads back at W,H

Alias of window-place resize; keeps the current origin. The reply's
command is window-place.
```

### `maximize`

```text
maximize HANDLE|--window H
```

```text
agenterm-cu maximize
  scope: actuate    family: Window placement    reply.command: window-place

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  maximize HANDLE|--window H

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)

Alias of window-place --action fullscreen. The reply's command is
window-place.
```

### `unmaximize`

```text
unmaximize --window HANDLE --expect unmaximized
```

```text
agenterm-cu unmaximize
  scope: actuate    family: Window placement

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  unmaximize --window HANDLE --expect unmaximized

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --expect unmaximized          checkable postcondition: the window reads back not maximized

Clears EWMH maximization on one exact window without activating its
application. Same destructive gate as `maximize --expect maximized`, with
maximized read-back and the windows inventory `maximized` field.
```

### `orderwin`

```text
orderwin --window HANDLE --relation above|below --relative HANDLE
```

```text
agenterm-cu orderwin
  scope: actuate    family: Window placement

usage (after the global flags, e.g. agenterm-cu --target current --grant actuate):
  orderwin --window HANDLE --relation above|below --relative HANDLE

arguments:
  --window HANDLE               window handle from `windows` (numeric or App#N)
  --relation above|below        above raises --window; below raises --relative
  --relative HANDLE             the other window

MCU relative z-order: above raises --window, below raises --relative,
then reads the order back and answers from what it read:
window_order_not_applied when the raise did not take. Linux raises with
the EWMH _NET_RESTACK_WINDOW (no focus change); macOS AXRaise cannot
reorder a background application's windows, so it refuses rather than
activating it.
```


## transports

### `exec`

```text
exec [--grant observe,actuate] [--grant-id ID] [--grant-store PATH] --json '<command-json>'
exec [...] --json -  |  --json-stdin  |  -            (JSON command on stdin)
```

```text
agenterm-cu exec
  scope: none       family: Transports

usage:
  agenterm-cu exec [--grant observe,actuate] [--grant-id ID] [--grant-store PATH] --json '<command-json>'
  agenterm-cu exec [...] --json -  |  --json-stdin  |  -            (JSON command on stdin)

arguments:
  --json JSON|-                 the command payload, or - for stdin
  --json-stdin                  read the payload from stdin
  --grant S                     strict scopes (also --grant=S); CLI wins over AGENTERM_CU_GRANT
  --grant-id ID                 persisted grant selector (exclusive with every other source)
  --grant-store PATH            store override; valid only with --grant-id

Executes one Command serialized as JSON (the same shape `verbs --json`
consumers build). The ssh and vnc transports spawn `agenterm-cu --grant …
exec --json -` on the worker side; global flags may precede `exec`. The
command's own target and scope apply; grant sources never union.
```


## host

### `grant`

```text
grant create --target current --scopes S --operations OP[,OP...] --ttl-ms N (--one-shot|--max-uses N) [--grant-store PATH]
grant list [--grant-store PATH]
grant revoke --grant-id ID [--grant-store PATH]
```

```text
agenterm-cu grant
  scope: none       family: Grants, host & help

usage:
  agenterm-cu grant create --target current --scopes S --operations OP[,OP...] --ttl-ms N (--one-shot|--max-uses N) [--grant-store PATH]
  agenterm-cu grant list [--grant-store PATH]
  agenterm-cu grant revoke --grant-id ID [--grant-store PATH]

arguments:
  --scopes S                    observe, actuate or observe,actuate
  --operations OP[,OP...]       exact canonical operations this grant may invoke
  --ttl-ms N                    lifetime
  --one-shot                    single use
  --max-uses N                  bounded use count
  --grant-store PATH            explicit store (test / admin seam)

Grant management is local/current only. It refuses ambient
AGENTERM_CU_GRANT* and AGENTERM_CU_AUTH* selectors; --grant-store is an
explicit test/admin seam. A created grant is selected on later commands
with --grant-id ID, which is mutually exclusive with --grant and the
environment. Operations are frozen in the grant; a later command cannot
inherit authority merely because it uses the same observe/actuate scope.
Schema-2 grants remain listable and revocable but fail closed as
operation_unbound when used.
```

### `host`

```text
host
hotkeys                                             (compatibility alias)
```

```text
agenterm-cu host    (also: hotkeys)
  scope: none       family: Grants, host & help

usage:
  agenterm-cu host
  agenterm-cu hotkeys                                             (compatibility alias)

Runs this binary as the resident desktop host: a status menu and global
shortcuts that route the placement catalog through the same Command ->
Executor chain as the CLI. `hotkeys` is the compatibility alias.
```

### `help`

```text
help [<verb> | ssh | vnc | rdp]
<verb> --help                                       (same as help <verb>)
```

```text
agenterm-cu help
  scope: none       family: Grants, host & help

usage:
  agenterm-cu help [<verb> | ssh | vnc | rdp]
  agenterm-cu <verb> --help                                       (same as help <verb>)

With no argument: the grouped command list. With a verb (any spelling):
usage, arguments and the long reference for that verb. The topics ssh,
vnc and rdp describe the transports. An unknown name is a typed usage
error that lists near matches.
```

### `verbs`

```text
verbs [--json | --text]
```

```text
agenterm-cu verbs
  scope: none       family: Grants, host & help

usage:
  agenterm-cu verbs [--json | --text]

arguments:
  --json                        JSON array (the default when stdout is not a terminal)
  --text                        aligned text table

Every row of the verb table: name, command, aliases, grant, family,
summary, usage and args. The JSON form is the machine-readable surface for
agents and generated docs; it is exactly what `--help` and `help <verb>`
render.
```
