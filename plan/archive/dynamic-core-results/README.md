# dynamic-core — research track index

A clean-room research track on **"maximum information and control from minimum
resources"**: how small can a self-extending native core be, and where does the
growth come from. Every question here is settled by a **decisive experiment**
(criteria fixed *before* building, a decision tree, a kill criterion, a time
box) rather than by argument. The method itself is packaged as
[`.claude/skills/decisive-experiment/SKILL.md`](../../../.agents/skills/decisive-experiment/SKILL.md) —
read it before writing a new Q spec.

> **原点在哪：** 起点问的是"要用上 JIT/AOT/FFI/mmap 里的哪些"——这个提法本身被测量纠正了。
> 真正的一句话是"agent 产出的、构建内核时不知道的逻辑，要能在构建内核时也不知道的机器上正确
> 跑起来，且过程可验证"。二十二个 Q 是这句话逐词拆开测出来的结果，不是二十二件互不相干的事。
> 完整推导见 [`SYNTHESIS.md`](./SYNTHESIS.md) 开头的「初心」一节——**先读那里，再读问题板**。

> 📄 **Reading the whole track at once:** [`SYNTHESIS.md`](./SYNTHESIS.md) — the cross-Q
> synthesis (shape / cost / permanent residues / conditions under which it fails / what it
> means for the north star), with an evidence grade on every claim and a
> **「被推翻的预判」** section. It obeys [`COMPARABILITY.md`](./COMPARABILITY.md)'s 口径
> constraints, so numbers it refuses to compare are marked **未测定**, not guessed.

> ⚠️ **Not AgenTerm product scope.** No version plan owns this, no `PRD.md`
> capability state changes. Specs live in `plan/design-*-experiment.md`, code
> lives here.

## The question board

Status vocabulary: **decided** / **running** / **specced, not started** /
**candidate (not specced)**. A candidate carries **no conclusion**, however
obvious it looks.

### 按初心分组读这张表

下面的大表是**按发生顺序**平铺的 Q0–Q21。它们不是二十二件互不相关的事——是
[`SYNTHESIS.md`「初心」一节](./SYNTHESIS.md#初心一句话以及它为什么会长成二十二个实验)
那一句话逐词拆开测出来的结果。这里按**回答的是哪个问题**给同一批 Q 加一层导航分组，
**不改表格本身、不删任何行**。一个 Q 答了不止一个问题就出现在不止一组——是真实的重叠，
不是分类没做干净。

| 家族 | 问的是什么 | Q |
|---|---|---|
| **跑起来**（执行策略：生码 vs 链接/解释） | 需要生成新字节去执行，还是可以只靠链接到机器上已存在的二进制来解释？两者是不是同一件事？ | Q0 Q2 Q9 Q10 Q12 Q16 Q19 |
| **构建时不知道的机器**（ISA/OS 轴） | ISA 与 OS 这两根轴，各自能不能推迟到运行时才决定？ | Q0 Q5 Q8 Q12 |
| **并不知道的逻辑**（中立 IR 的边界） | 中立 IR 能装下多少未知逻辑，边界在哪、为什么在那 | Q1 Q5 Q7 Q21 |
| **可验证** | agent 产出的东西，哪些能被机器验、哪些只能信、边界在哪 | Q4 Q13 Q14 Q15 Q16 Q17 Q19 |
| **分发/复用**（第二层怎么组合） | 适配器/载荷这第二层，怎么复用而不重演 JVM 式的钦定注册表 | Q3 Q18 |
| **形状边界**（call 原语能表达什么） | ④ `call` 的数据描述能表达到哪一步（float/struct/varargs） | Q6 Q20 |

前四族对应 SYNTHESIS 初心表的四行，**Q16 记在「可验证」而不是「逻辑」**：它组装的虽是 IR
相关的八个部件，但真正的产出（S2——产出时等价验证在被迫解释的平台上失效）是一条可验证性
结论，不是中立性结论；SYNTHESIS 那张表原来把它放错了行，本轮已改（见该文件初心表下的校核）。
后两族（分发/复用、形状边界）**不是**"一句话"四个词的直接展开，是支撑同一个北极星的两个
相邻问题——第二层怎么不裂片化、`call` 原语的数据形状能表达多少——SYNTHESIS 的初心表因此
只保留四行，不是遗漏，见该文件的说明。

| Q | Question | Status | Conclusion so far | Spec | Code |
|---|----------|--------|-------------------|------|------|
| **Q0** | **How many layers?** 1 layer (mechanism + platform adaptation fused) vs 2 layers (frozen minimal kernel + replaceable payload) | **decided** | **2-layer**, but only on the experiment's own priority order — see below. The decisive-by-design metric ③ **tied**; the deferred ④ is what separated them | [`design-dynamic-core-experiment.md`](../design-dynamic-core-experiment.md) | `core/` `adapters/` `payloads/` `pack/` `build/` → [`RESULTS.md`](./RESULTS.md) |
| **Q1** | **Can a neutral IR defer *all* ABI/layout decisions to lowering?** Same ISA, two incompatible ABIs (SysV64 vs Win64) | **decided** | **Bounded neutrality.** ① passes — `pure_compute` lowers **byte-identically** to both ABIs and runs. The **ABI-placement half is clean**: registers, spill order, shadow space, red zone, return register are derived from the semantic signature alone, zero IR involvement (`CreateProcessA`, 10 args → 6 spilled, runs). This **overturns Q0's read** that one ABI must be fixed and bridged — Q0 conflated *mechanism* with *content*. Every leak (L1–L5) is OS-interface **content**: non-neutral naming, semantic-vs-native arity, and above all **OS struct layout has no neutral form** (spawn collapses to a coarse intent, the two lowerings share nothing). **L2 is the core dilemma: neutral OS calls require encapsulation, which the kernel forbids — so encapsulation did not vanish, it moved into the lowerer and regrows at O(targets × intents).** ⑤ splits it: ABI back-end ~20–30 lines/target (**fixed**) vs OS-interface content ~90–110 lines/target (**linear in intents**). ③ never trips the 200% ceiling | [`design-neutral-ir-experiment.md`](../design-neutral-ir-experiment.md) | `ir/` → [`RESULTS.md`](./ir/RESULTS.md) |
| **Q2** | **How big is a minimal usable IR→native lowerer (X bytes), and does it belong inside or outside the kernel?** | **decided** | **A real no-free-lunch tradeoff, because X is NOT small.** X = **3003 B** flat-safe (in-kernel jump-table version ~2777) against a minimal-kernel baseline of **~2932 B** → **X/kernel = 102%**, ten times over the **25% threshold pinned before the build** at which "in-kernel is obvious" would have triggered. ③ total delivery favors **in-kernel** by a *constant* ~1.5 KB (6360 vs 7816 L); ④ frozen TCB favors **out-of-kernel** (~2.93 KB vs ~6.2 KB, ≈2.1×). ② (139 x86-specific / 162 shared LOC, 46% per-ISA) **does not separate the two placements** — both replicate per ISA; what it actually measures is the new slope *codegen itself* introduces (per-ISA native footprint roughly doubles vs Q0's ~2.7 KB). kill criterion (X > 8× kernel) **not** triggered. Implementation surfaced a decisive out-of-spec constraint: a lowerer run as a **flat non-relocated blob must be memset-free and jump-table-free** (a real `jmpq *%rcx` was found and suppressed) — **measured cost +8% on X** and a more fragile build. **口径/execution caveats travel with these numbers**: X is a **Linux/ELF-flat byte measurement of an artifact that was never executed**; "X ≈ the whole kernel" is a **cross-口径** comparison (U7); the kernel baseline's own 口径 was **never documented and is reconstructed** in RESULTS ④ | [`design-lowering-cost-experiment.md`](../design-lowering-cost-experiment.md) | `lowering/` → [`lowering/RESULTS.md`](./lowering/RESULTS.md) |
| **Q3** | **Composition of the second layer** — can adapter packages be *reused* (not merely coexist) without a central registry anointing an official one? Failure mode is fragmentation, the mirror image of the JVM monopoly. Q0's ⑥ proved coexistence only | **decided** | **Reuse achievable, bounded by discovery.** Content addressing shares an adapter on disk (① pass: stored once, saving linear in N) *and* lets incompatible versions coexist with no anointing (② yes), at a bounded, adapter-count-**O(1)** in-kernel cost (③ +609 B; +1648 B to verify). Boundary (④): it dedups *bytes* not *behavior* (reproducible builds make same-source→same-hash hold), and provides **no name→hash discovery** — the one place anointing could re-enter. Fragmentation is *converted*, not unsolved | [`design-adapter-reuse-experiment.md`](../design-adapter-reuse-experiment.md) | `reuse/` → [`reuse/RESULTS.md`](./reuse/RESULTS.md) |
| **Q4** | **Verifiability** — "hand it a blob and it executes" is an unverifiable execution surface (Thompson's trusting-trust, amplified when an agent produces its own code). Target shape: one neutral IR lowered by independent paths must be **behaviourally equivalent**, as a *structural invariant*, not an after-the-fact check (diverse double-compilation) | **decided** | **bounded structural achievability** — the invariant is real & un-forgettable (a construction gate; a mutated neutral byte is refused) but covers **only the neutral core**. Unverifiable-by-structure fraction = intent regions (Q1's L1–L5): **0% pure → ~30–41% file-I/O → ~45–56% spawn**. Beyond it: after-the-fact differential testing (Tier B), collapsing to Tier C for zero-shared/un-runnable targets. Does **not** slide into a correctness proof | [`design-equivalence-invariant-experiment.md`](../design-equivalence-invariant-experiment.md) | `equiv/` → [`RESULTS.md`](./equiv/RESULTS.md) |
| **Q5** | **The ISA axis** — Q0/Q1/Q2 deliberately pin x86_64. What does a second ISA cost: is IR neutrality preserved, and does "N kernels, one per ISA" stay bounded? This is the track's **largest untested assertion**: Q0 §0.2 asserts "the kernel does not grow with ISA count, you just build N of them" **by reasoning only**, and the whole `N×M → N` collapse rests on N being cheap. Note Q1 counted its 202-line x86-64 encoder as *shared* because both its targets were the same ISA — a second ISA moves that into per-ISA cost, so Q1's two-way split must be re-derived as a **three-way** one (shared / per-ISA / per-target) | **decided** | **§0.2 assertion HOLDS; N×M→N stands.** IR + payloads lowered to aarch64 **byte-identical** (① gate passes — no ISA hint, no new type). A second ISA costs a **bounded ~307-LOC per-ISA lowerer bucket** + **+13% kernel** (four primitives 568→644 B), both **linear in ISA count only, constant in intents/OS** — not the multiplicative blow-up that would falsify §0.2. Three-way split: shared 238 / per-ISA 307–350 / per-target 99–137 each. ISA-axis leaks I1–I5: reach content **and set** is per-(ISA,OS) not per-OS (aarch64 has no `open`/`fork`); immediates & alignment stay in the lowerer (non-leaks); **the ABI-placement axis collapses into the ISA on aarch64** (one AAPCS64 for both OSes — so Q1's "ABI is per-target" is itself ISA-relative); struct-layout (Q1 L3) is ISA-independent. Encoder **26/26 vs LLVM**; byte-measured + encoder-validated, not executed (no aarch64 host) | [`design-isa-axis-experiment.md`](../design-isa-axis-experiment.md) | `isa/` → [`isa/RESULTS.md`](./isa/RESULTS.md) |
| **Q6** | **Primitive completeness** — is the four-primitive floor stable, or does it creep? Q0 needed no fifth primitive kind, but the second capability forced *completing* ④ `call` (arg ceiling 7→11) at a cost paid by **both** variants' kernels. Open: is that a one-time step or a slope? | **decided** | **Floor creeps partially — two floors, opposite answers.** The **kernel** floor is **stable**: three capabilities of divergent shape (mmap-file arity 7 / dir arity 2 / socket arity 3) did **not** force ④'s ceiling (11) up — Q0's 7→11 was a **one-time step, not a hidden per-capability slope** (① measured, boolean gate not tripped); socket/mmap/dir add **0 kernel bytes** (they are just more ③+④ calls). **Claim K of §1.1 ("内核永不为覆盖能力而变大") HOLDS.** But the completeness **claim** (Claim R, "nothing unreachable") is **FALSIFIED** for one class: **struct layout (`offsetof`)** — 2 of 3 capabilities need a field offset and **nothing in ①②③④ produces one**; it is only **bakeable** (unverified per-target trust) or, where a host publishes layout (Linux BTF), **fetched via ③+④ + a payload parser** — absent host publication (Windows system structs) it is **irreducibly baked**. Portability work is **transferred, not eliminated** (confirms Q7's L3a by construction; refutes Q0 §④(b2)'s slope fear). Is a 5th primitive **necessary**? **Mechanically NO** (Declare = ③+④ usage: GetSystemInfo/RtlAddFunctionTable/BTF-read), **conceptually YES** (a "do-only" model makes the metadata concern invisible). Closed list = **FIVE, host-conditional** (① memory ② execute ③ reach ④ call ⑤ declare); ⑤ in-kernel floor = **+182 B .text** but **avoidable** (0 if left as ③+④ + baked table). No genuine **sixth** kernel class (orchestration = payload code; callbacks = ①②④). **Not** "no closed list" — the list closes, with an asterisk | [`design-primitive-completeness-experiment.md`](../design-primitive-completeness-experiment.md) | `primitives/` → [`RESULTS.md`](./primitives/RESULTS.md) |
| **Q7** | **OS-interface content as DATA** — Q1 & Q4 independently hit the *same* seam (OS-interface content L1–L5). Can that content stop being per-target hand-written *code* and become *data* (tables) an intent-/target-agnostic marshaller interprets? If so, the seam improves on growth, verifiability, and reuse at once | **decided** | **Bounded reachable.** For the **single-native-call family** (Alloc/Open/Read/Close/Write) the OS content fully becomes data over one fixed marshaller and **executes** (pure→163, rhp→`a49d2cbecc13994f`): marginal **code** cost of +1 intent and +1 same-ISA target = **0** (engine has zero `match intent` / target branch — verified), all growth is data, and the data is schema-checkable (④: recipe well-formedness caught pre-emit; but the L1 name→number *binding truth* stays trust). Seam stays **code** at (a) **L3b orchestration/control-flow** (multi-call dataflow, SysV fork/branch — forcing it to data = the IDL slide the experiment detects) and (b) **I2 cross-ISA restructuring** (`openat`/`clone`: the syscall *set* changes shape, Q5). L3's layout half **L3a** tablifies only **in query form + host oracle = the missing 5th primitive Declare**. ③ engine ~70–112 fixed LOC vs Q1's growing ~90–110/target; emitted bytes ≤ Q1. ⑤ dimensional correction (Q5): layout=per-OS, reach=per-(ISA,OS), ABI=per-ISA — the seam is not one flat table | [`design-os-interface-as-data-experiment.md`](../design-os-interface-as-data-experiment.md) | `tables/` → [`tables/RESULTS.md`](./tables/RESULTS.md) |
| **Q8** | **Executable-memory floor** — the reference survey (`plan/reference-cross-target-execution.md` §7.1) claims platforms are systematically clawing back the ground primitives ①(mem RW↔RX) ②(jump into it) ③(raw syscall) stand on ("marked executable" ≠ "the platform lets you execute"). Measured on this real Windows Server 2022 / x86_64 box, not cited | **decided** | **Windows floor holds by default; "shaken" downgraded to "deployment precondition".** Default policy (`DynamicCode = 0x0`): ①② fully available — all three exec-memory routes (RW→RX flip = the Q0 kernel path, direct RWX, section-object map) jump in and run. **ACG** (`ProcessDynamicCodePolicy.ProhibitDynamicCode`) is **opt-in (default off)**; once enabled it cuts **all three** routes with `ERROR_DYNAMIC_CODE_BLOCKED` (1655) — stronger than the survey's "two paths". Gap is **policy-configurable** for our own process (we can just not enable it) but **hard inside a process ACG is externally imposed on** without `AllowThreadOptOut`. Two measured conditional windows: RX made **before** ACG still executes after (one-shot AOT), and `AllowThreadOptOut` + per-thread opt-out restores the flip. ③'s raw-syscall half is unused on Windows (Q0 kernel returns −1); its **symbol-resolution half + primitive ④ are unaffected by ACG** (don't need exec memory). **Model impact:** definitions unchanged, but ② must be polymorphic (direct RX / cross-process / **interpret**) with interpretation a first-class fallback, and ③ must be split into raw-syscall vs symbol-resolution halves. Linux/macOS/iOS/OpenBSD **not testable here (no WSL)** — kept as **"unverified transcription"** per the survey; iOS "no legal path at all" would be the one hard gap | [`design-executable-memory-floor-experiment.md`](../design-executable-memory-floor-experiment.md) | `platform/` → [`platform/RESULTS.md`](./platform/RESULTS.md) |
| **Q13** | **Declare without a host layout oracle** — Q6 closed the primitive list at five "host-conditional," but that condition **fails on Windows** (no BTF): every struct-field offset a capability needs enters as a **baked constant = unverified per-target trust**, and a wrong bake fails **silently** on a platform-version change. Q13 asks: does `Declare` have a form **better than bake-and-pray**? Can a wrong layout assumption be **detected (fail-fast)** rather than silently crash — and at what cost, with what permanent residue? | **decided** | **只能烤但可检测 (bake-only, but detectable).** Windows offers **no runtime `offsetof` oracle** (① — `GetSystemInfo` publishes machine-fact *values* but each is gated behind an unpublished offset = bootstrap circularity; a real oracle would need PDB = the excluded pathology), so **baking is unavoidable**. **But a wrong bake is DETECTABLE** (② — the main gate): for all three of Q6's baked facts a ③+④-only self-check **fired on a deliberately corrupted offset** on the real box (constructive filename read-back; socket bind+getsockname round-trip → WSAEAFNOSUPPORT; GetSystemInfo cross-field), converting Q6's silent trust into explicit fail-fast at **~18–38 LOC/fact and +0 kernel bytes** (③, vs Q6's +182 B-or-bake-and-pray in-kernel forms). Residue (④): fields with **no semantic round-trip** (write-only-unvalidated / unpredictable-read / loosely-validated `cb`) stay silent-trust; detection also has a **strength gradient** — constructive & round-trip are airtight *modulo naming*, cross-field is **weak** (bootstraps from other baked offsets — proven live when the check caught a real 32-bit-vs-x64 offset bug I had baked). Against Q4 (⑤): layout binding-truth **is** checkable where naming binding-truth is not, so the trust set **shrinks {naming + layout} → {naming}** — layout exits the hole, *modulo* the symbols the check calls. **bake-and-pray → bake-and-detect** | [`design-declare-detection-experiment.md`](../design-declare-detection-experiment.md) | `declare/` → [`declare/RESULTS.md`](./declare/RESULTS.md) |
| **Q9** | **Interpretation as a first-class backend** — Q8's direct consequence: it split the four primitives into ①②(generate new code, fragile under ACG/hardened/iOS) vs ③④(reach existing code, robust), forcing "interpretation must be a first-class fallback from day one." Key insight: the interpreter runs the *same* neutral IR — it is another **backend**, not another architecture. How big, how slow, how much does it cover, and does Q1's L1–L5 seam survive interpretation? | **decided** | **Interpretation IS a viable first-class fallback.** ① all three Q1 payloads run through `interp::run` on the **byte-identical unchanged IR** (pure→163, rhp→`a49d2cbecc13994f`, spawn→`exit=07`/7) — the IR is a **completely interpretable** backend surface, no IR change. ② eval-core **55 LOC / 1908 B**, whole interpreter **136 LOC / 3177 B** = **21% of Q1's lowerer** (487 LOC / 14819 B) — *all three in one 口径 (object `.text`, msvc, std, same command), so this ratio holds*. **"kernel-magnitude (3.4× the 568/644 B four-primitive kernel)" has been withdrawn**: those kernel numbers are Q5's `prim.rs` on `*-linux-gnu`, `no_std`+`panic=abort` (**not Q0's kernel**, as this row previously said) — cross-std/no_std and cross-target, so the ratio is **not measured**. The conclusion "the interpreter is a few KB" needs no ratio: 1908 B / 3177 B says it directly. ③ the one hard cost is **compute-bound inner loops only**: ~**77× vs optimized native** (JIT ceiling), ~5× vs Q1's naive lowering — but **OS-bound payloads = 1.0×** (interpretation overhead is drowned by the OS call). So: first-class **availability** fallback, performance **平替 on OS-bound / 降级可用 on hot compute**. ④ eval-core's ISA-specific LOC = **0** (machine-code-free, verified) → collapses Q5's per-ISA 307–350 LOC bucket to ~0; the lowerer's 14819 B is mostly the x86-64 encoder the interpreter simply lacks. ⑤ **L1–L5 survive verbatim** — same 9 kernel32 symbols, same injected constants, same `STARTUPINFOA=104`/offset-0 layout, same 32-bit out-params — content identical to `win64.rs`, only syntax differs. **The OS seam is a property of the OS interface, orthogonal to execution method; interpretation eliminates the ISA machinery, not the seam.** | [`design-interpreter-backend-experiment.md`](../design-interpreter-backend-experiment.md) | `interp/` → [`interp/RESULTS.md`](./interp/RESULTS.md) |
| **Q12** | **The landing gate** — Q8 tested the *first* gate (can I get executable memory & jump in). The survey (§7.2/§7.4) names a *second* gate nobody tested: once jumped in, does the hardware/runtime actually let the bytes run — **CET-IBT/ENDBR64** landing pads, **I-cache** coherence, **Windows x64 unwind** registration, **±2GB placement** truncation. Which bite on a real box, and which of our products (Q2 lowerer / Q9 interpreter) trip them? | **decided** | **On this x86_64 box, not one second-gate hazard bites our products today — "second gate" downgraded to "hardening-platform deployment precondition," isomorphic to Q8's first-gate verdict.** ① CPUID reports **CET_SS=0 / CET_IBT=0 (no CET hardware at all)**, CFG & shadow-stack policy off → IBT cannot trigger; Windows exposes **no runtime forward-edge-IBT toggle** (unlike ACG), so ② is settled by the empirical jump-test not by docs. ② our products carry **0 ENDBR64** (scanned Q2/Q9/Q8 binaries) and Q2's only indirect targets (entry `transmute+entry()`, callback `call [r15+idx*8]`) lack landing pads — yet **all four patterns (P0/P0b/P1-entry/P2-callback) jump in and return 42**; generated frames have **no unwind entry** (`RtlLookupFunctionEntry`=NULL) but current payloads don't unwind through them (**latent, not biting**); I-cache is **free on x86**. ③ **the ±2GB truncation is real and the worst kind — MEASURED silent**: a >2GB `call rel32` (3GB delta) truncated via Q2's verbatim `as i32` with **no emit error** and **silently returned the wrong decoy callee (99, not 42), no crash** — but Q2 is **structurally immune** (it never emits a PC-relative reference to an external symbol: OS calls go through absolute-indirect `call [mem]`, rel32 only for intra-buffer jumps); **any switch to copy-and-patch detonates it**. ④ hardening the codegen route ≈ **+4 B/entry+1 line (ENDBR)**, **~40 LOC+~34 B (unwind, deferrable)**, **0 (placement, already immune)** — ≈1% of Q2's X=3003 B. ⑤ **the interpreter is structurally immune to ALL FOUR second gates** (no generated bytes to land on / flush / unwind / relocate): proven by source + **measured under ACG** (clean-room match-interpreter computes correctly with zero executable pages while the codegen path dies at 1655) — a **third** structural advantage of interpretation after Q9's size/ISA, and the hardest; the ENDBR/unwind/placement work Q2 must add, the interpreter adds **zero**. ARM BTI/PAC/IC-DSB-ISB **not testable here** — unverified transcription | [`design-landing-gate-experiment.md`](../design-landing-gate-experiment.md) | `landing/` → [`landing/RESULTS.md`](./landing/RESULTS.md) |
| **Q10** | **Copy-and-patch stencils** — Q2 measured X=3003 B ≈ the whole kernel and named its own untested alternative: let a *real compiler* emit machine-code templates (stencils) at build time so the runtime "lowerer" is only **memcpy + a relocation applier**. Reviewer's hypothesis: X collapses to a few hundred bytes, ISA cost moves to stencil *data*, the in/out-kernel tradeoff **dissolves**. Tests the track's "mechanism-fixed, capability-via-data" principle on the **codegen** axis | **decided** | **NOT worth it at this scale; tradeoff NOT dissolved — reviewer's expected value refuted.** All three payloads execute byte-identically via stencils (pure→163, rhp→`a49d2cbecc13994f`, spawn→`exit=07`/7). ① the *pure* memcpy+relocation applier **is** tiny (**651 B**) as predicted — but that was never the costly part: the **opcode decode/dispatch survives as code and dominates** (`emit` 3541 B), so whole runtime code = **4515 B** and whole footprint (code+data, flat, Q2口径) = **5826 B ≈ 1.94× Q2's 3003 B** — *bigger*. ② stencil data 406 code B + 55 holes (tight 571 B), scaling **opcodes × ISAs** + per-`argc` CALL variants. ③ recomputed, the tradeoff **shifts both endpoints up** and keeps its shape; "X negligible ⇒ obviously in-kernel" does **not** trigger (X grew). ④ **control flow confirmed un-stencilable** (a stencil can't leave CPU flags live across its boundary; branch target is a layout-time offset) → ~20 B applier-emitted residual/branch; **±2 GB PC32 placement constraint real and hit** (§7.4). ⑤ +1 ISA becomes per-ISA **data** (strippable) + irreducible per-ISA code residual — a form-change toward data, **not a size win**. Bigger than Q2's compiler in Q2's own 口径 (5826 vs 3003 B); **vs Q9's interpreter the size axis is 口径-incomparable → not measured** (see [`COMPARABILITY.md`](./COMPARABILITY.md) U2) | [`design-stencil-lowering-experiment.md`](../design-stencil-lowering-experiment.md) | `stencil/` → [`stencil/RESULTS.md`](./stencil/RESULTS.md) |
| **Q15** | **Is the interpreter a policy-enforcement point for agent-produced code?** Q9/Q12 measured *usability* (size / speed / coverage / runs on a hardened platform) and **never measured safety** — but the track was starting to be read as "interpretation is safer." What can an interpreter actually refuse, what can it not, what does each instruction cost, and what would the JIT pay for the same policy? | **decided** | **The structural safety advantage is REAL but on the wrong layer.** ① At the **instruction** layer the interpreter enforces bounds / step limit / alloc budget / OS-call surface / (partial) dataflow — all five negative tests refused, all five succeed with checks off. ② **Outside the intent boundary its control is ZERO** (the main criterion): `spawner`'s dangerous content (`cmd.exe /c exit 7`) **is not an IR value at all** — rodata = 0 bytes, `SpawnWait` extern nargs = 0; it lives in the seam *below* the IR, so there is no `Val` for taint/allowlist/bounds to gate. Allow `SpawnWait` and the interpreter sees **only the exit code**. ③ Cost is **cheap**: all per-instruction checks on = **1.47×** on `pure_compute`; eval-core **1908 → 2935 B (+1027 B / +54%)** (Q9's 口径, recomputed on this toolchain — Q9 core re-measured at 1908 B, parity passes). ④ vs JIT: the two **intent-boundary** policies (OS-call surface, memory budget) are **O(1) on both routes** — same chokepoint, **JIT has no disadvantage**; the three **per-instruction** policies are O(1) shared code for the interpreter vs **O(ops) emitted guard bytes** or a load-time verifier for the JIT (§4.1 eBPF `verifier.c` = 20,065 lines — you cannot have that and a few-KB core). ⑤ Merged with Q4: the build-time equivalence guard and the run-time control point **stop at exactly the same place**, and **the residual hole is identical** (L1–L5, after the call goes out). **Net: "interpretation is safer" is half-right, and wrong on the half that matters** — the real danger surface (what the OS / child process does after the call) sits at the intent boundary, where both routes are equal and neither reaches past it | [`design-policy-enforcement-experiment.md`](../design-policy-enforcement-experiment.md) | `policy/` → [`policy/RESULTS.md`](./policy/RESULTS.md) |
| **Q14** | **Behavioural verification of naming bindings** — Q4/Q13 declared the naming binding truth (「符号 index 1 是不是真的 `CreateFileA`」) **irreducible** (R3): the only way to test the call is to make it (circular, Thompson), leaving **{naming}** as the last un-checkable trust hole after Q13 pulled layout out. But Q13's own FACT1 already did behavioural naming verification by accident (constructive filename read-back FIRES if the symbol is not really `FindFirstFileA`). Q14 asks it head-on: can a naming binding be **behaviourally verified**, to what degree, at what cost, and does the circularity **converge to a small root trust set**? | **decided** | **命名可行为式验证 (Tier B / needs execution), converges to N≈0 residual — R3's "irreducible" is REFUTED.** ① Boolean gate PASSES: all four tested bindings PASS-on-correct **and** FIRE-on-mis-bind on the real box (`lstrlenA`/`MulDiv`/`lstrcmpiA` pure + `CreateFileA` round-trip cluster); a mis-bind = resolving a different export into the slot = exactly a lying resolver, caught every time. ② Verifiable **iff** a constructible known-in→known-out contract or OS round-trip exists (the majority of symbols a payload calls); residue = no-contract symbols (`OutputDebugStringA`/`ExitProcess`), weak indirect-only (`Sleep`), the resolver root. ③ **8 LOC/pure symbol, 40 LOC/cluster, +0 kernel bytes** (payload-side, Q13 envelope; pure checks cheaper). ④ **the circularity CONVERGES** (main criterion): a resolver that lies about a *contract-bearing* symbol is **caught by that symbol's own check** (stronger than the going-in N≈1-root hypothesis), and pure functions verify against an **external** oracle (math) with **zero** in-process trust — so the chain bottoms out at a **residue class {Thompson trigger, no-contract symbols, ambient kernel}**, and **N (residual trusted named-bindings) ≈ 0** for contract-bearing payloads. ⑤ Q13's "modulo naming" caveat **dissolves**; the trust set shrinks **{naming} → {no-contract + Thompson + kernel}**. **Honest limit:** Tier B (requires EXECUTION), so Q4's structural execution-free guard still cannot reach naming, and the Thompson trigger genuinely survives — Q4/Q13 were right that naming is unqueryable *structurally*, wrong to read that as *irreducible trust* | [`design-naming-verification-experiment.md`](../design-naming-verification-experiment.md) | `naming/` → [`naming/RESULTS.md`](./naming/RESULTS.md) |
| **Q18** | **Discovery without anointing** — Q3 proved content addressing gives `hash→content` but left one residue: **no `name→hash` discovery**, "the one place anointing could re-enter." Can a `name→hash` mechanism exist that both works and introduces no anointing — at what cost, with what residue? Or is discovery not a runtime problem at all (payloads pin hashes at build time)? | **decided** | **无钦定发现可达 AND discovery is a build-time concern (runtime no residue).** ① Two consumers resolve the **same name** `fileio` to **different hashes** via two non-conflicting directories and **both run correctly & simultaneously** (`len=0023` v1 / `len=0008` v2, Windows). ② **No anointing** — no default/global directory, **no global fallback** (a directory lacking a name misses, exit-23 probe), directories disagree and coexist; the version-coexistence discriminator passes where a single registry cannot represent "fileio = both v1 and v2 depending on who asks". ⑤ **flips the frame:** the build-time-pinned baseline (`loader_hash`, **zero discovery code**, byte-identical to Q3's loader at 2672 B) yields the **same two outcomes** — `name→hash` hoists fully to build time, so **Q3's residue R7 is a build-time concern, not a runtime hole where anointing re-enters**. ③ runtime discovery layer = **+1424 B** clean-ELF (removable; Windows PE +2048 B coarse). ④ residue = **local, plural, optional** trust (you trust the directory/hash you picked — never "one, shared, forced"); the only irreducible piece, *does this hash really behave as the name promises* (naming truth), belongs to Q14 not discovery | [`discovery/SPEC.md`](./discovery/SPEC.md) | `discovery/` → [`discovery/RESULTS.md`](./discovery/RESULTS.md) |
| **Q16** | **Composition / seam audit** — 16 experiments each measured one axis in isolation; nobody ran them *together*. Assemble the 8 decided parts (Q1 IR + Q9 interp + Q15 policy + Q7 tables + Q4 guard + Q13 Declare + Q3 content-addr + Q6 arity) into one binary and look for conflicts *between* parts, not within them | **decided** | **Composes and runs (3 payloads, real box), but with one genuine incompatibility.** Main product is the **seam list** (S1–S8): most parts co-exist at a cost, but **S2 is a true break** — Q4's Tier-A structural guard is a *build-time, execution-free* gate that compares **two codegen lowerings**; a pure interpreter produces **zero** lowerings, so on ACG/iOS platforms that **force** interpretation, Q4's produce-time guard is inapplicable (nothing to cross-check) and drops to Tier B, or Tier C under ACG where even the lowerings can't be built. **The track's strongest verification result (Q4 execution-free equivalence) and its strongest portability/hardening result (interpretation as the floor) cannot both hold on hardened platforms.** Also S4: Q3 content-addressing dedups *stripped bytes* while Q4 needs *IR to re-lower* — they sit at incompatible lifecycle points (new matrix contradiction X6). Other seams (S3/S5/S6/S7/S8) are表面 or cost-only. `[实测·真机执行 pure_compute 三后端；rhp/spawn codegen 仅字节]` | [`compose/RESULTS.md`](./compose/RESULTS.md) | `compose/` → [`compose/RESULTS.md`](./compose/RESULTS.md) |
| **Q19** | **Structural verification for the interpreter path** (solution hunt for Q16's S2) — Q4's Tier-A guard compares two codegen lowerings; a pure interpreter emits none, so on forced-interpretation platforms the produce-time guard is inapplicable. Can the interpreter path have a *different* Tier-A gate — verifying the **IR itself** rather than comparing outputs? | **decided** | **Only closes an adjacent (well-formedness) hole; S2's equivalence hole stays open — but S2's wording is refuted.** Built a 98 LOC / 634 B IR structural verifier with all four Tier-A properties (produce-time / execution-free / construction-gate / un-forgettable): 3 real payloads PASS, 5 injected bad-IR (OOB index, undefined callee, arity mismatch, illegal branch target, rodata OOB) all FIRE. **But it proves a *different* property than Q4**: Q4 verifies **equivalence** (a *relational* property needing ≥2 execution bodies); Q19 verifies **well-formedness** (a *unary* property needing 1). A single interpreter body cannot yield equivalence structurally. Boundary (measured, all PASS = can't catch): well-formedness **≠ memory safety** (`oob_store` passes — exactly where eBPF spends 20k lines) **≠ semantic correctness**, and L1–L5 seam is untouched. **Net:** the produce-time axis is **not absent** on the interpreter floor (well-formedness IS there); S2's headline survives **re-scoped** — produce-time *equivalence* verification is structurally absent on the floor, produce-time *well-formedness* verification is present on both paths. `[实测·真机执行]` | [`verify/RESULTS.md`](./verify/RESULTS.md) | `verify/` → [`verify/RESULTS.md`](./verify/RESULTS.md) |
| **Q17** | **Recursive / self-hosting boundary** (solution hunt for R5's intent-boundary residue) — Q15 found zero control/observation past the intent boundary. If the other side of that boundary is *also* one of our cores running neutral IR, does observation/verification/policy come back? | **decided** | **Partial: the seam relocates and changes character, it does not shrink.** ① Boolean gate PASSES, and beats Q15 point-for-point: core A spawns core B as a real OS process, hands it serialized neutral IR, and gets back a **per-intent runtime trace** (`Alloc[18]→ptr`, `WriteStdout[ptr,18]→18`) plus captured side-effect output — not just an exit code. A pre-flight scan lets A reject a malicious sub-task's IR before it runs, and B enforces its own allow/deny at the same O(1) chokepoint Q15 found. ② (main product) **The new seam is NOT smaller.** It splits into three: the launch half is Q15's L1–L5 spawn seam **verbatim, reused once** (not shrunk); the A↔B channel is a **net-new** fixed encode/decode surface (though its naming half genuinely is neutral — one shared 1-byte op/intent tag vocabulary, better than Q1's disjoint per-target symbol tables); the OS-content seam (L2–L5: struct layout, arity, sentinels) is **carried unchanged into B** — B still owes `STARTUPINFOA.cb=104` the moment it really opens a file. Globally: same-size OS-content seam **plus** a new channel seam — total machinery only grows. ③ Not applicable to foreign binaries, existing native libraries, or anything where "the other side" isn't our own IR — the gain covers only self-produced components. ④ Cost: in-process 1.2 µs → cross-boundary **≈11.4 ms (≈9000×)**; cheap in bytes (210–286 B modules), expensive in latency — viable only for coarse-grained sub-tasks, not per-instruction. **What's real:** the wall A faces changes from an opaque foreign command to a checkable, structurally-verifiable neutral IR plus cooperative self-reporting — a genuine observability upgrade, but forcing an uncooperative/compromised B to be honest is still impossible (Thompson/R3 residue survives untouched). `[真机执行]` | [`recursive/RESULTS.md`](./recursive/RESULTS.md) | `recursive/` → [`recursive/RESULTS.md`](./recursive/RESULTS.md) |
| **Q20** | **The shape boundary of `call`** (solution hunt for R6) — Q6 found float/SIMD, struct-by-value, varargs, `sret` inexpressible at *any* arity: not a slope problem, a shape one, and it blocks most real Win32/POSIX APIs. Can float and struct-by-value be added as **data** (a `Kind` tag riding the same ABI-placement axis Q1 proved deferrable) rather than a new primitive? | **decided** | **R6 narrows meaningfully, not uniformly.** Extended ④'s signature description with an `Int`/`Float` per-argument/return `Kind` tag, dispatched through the *same* shape ④ already had; XMM-vs-GPR placement delegated entirely to the host codegen. **Real APIs, real box**: `sqrt(144.0)==12.0`, `pow(2.0,10.0)==1024.0`, `ldexp(1.5,4)==24.0` (float-then-int positional case — the one a naive SysV-style separate-counter port would silently break), `PtInRect` (struct-by-value `POINT`, both directions correct). **Float: closed, ~12–16 LOC, no new primitive** — confirms it's data on the placement axis, exactly as Q1 already proved for registers/shadow-space/red-zone. **Struct-by-value ≤ register width: closed, 0 extra LOC** (runs through the unmodified baseline once the caller bakes field order — falls into the known offsetof-class residue, not a new one). **Struct-by-value > register width / `sret`: left open, honestly** — reasoned closeable via Q1's existing constant-injection mechanism, but no real system API needing it was found after searching kernel32/user32/gdi32/msvcrt/common POSIX libc (they overwhelmingly pass structs by pointer), so it was not forced closed without evidence. **Varargs: split** — closed for any finite/static shape (each concrete call site is just another data-classified call), permanent for genuinely runtime-determined shapes (the IR's `Call` arity is static at authoring time). SIMD out of scope by design. `[真机执行]` | [`shape/RESULTS.md`](./shape/RESULTS.md) | `shape/` → [`shape/RESULTS.md`](./shape/RESULTS.md) |
| **Q21** | **Can control-flow orchestration be tabified?** (solution hunt for R1, the track's hardest residue — Q7 had already ruled "irreducibly code") — Q7's verdict lumped five `SpawnWait` facts together without ever actually attempting to tabify the sequence. Read precisely, are they one failure mode or several? | **decided** | **Refined, not reversed — and this is the sharpest boundary the whole track has drawn.** Q7's five facts split cleanly on inspection: two are straight-line dataflow (every step always runs, only argument *values* vary), three all share one root cause — a runtime value determining *whether/how many times* something executes (`fork`'s pid branch, argv's runtime length, sentinel-driven branching). **The linear half was actually built and run**: a data-only step table (`ArgSrc` enum, zero `if` in the fixed dispatch loop — grep-verified) drives the real 4-step Windows `SpawnWait` chain end to end on the live box, `cmd.exe /c exit 7` → exit code 7 read back through pure data, `hProcess` extraction folding into argument resolution rather than needing a branch. **The wall was demonstrated by execution, not asserted**: the same table shape pointed at a nonexistent binary has no field able to express "skip Wait/GetExitCode" and silently reports a bogus exit code 0 — that failure *is* the proof of where tabification ends. **Precise missing capability**: conditional transfer of control keyed on a runtime value unknown at table-authoring time — adding it to the schema would give the schema the defining operational-semantics property of an executable program, indistinguishable from this track's own IR. No mini-DSL was invented and relabeled as data. **100% of the happy-path linear chain tabifies** (proven, not argued); what doesn't is specifically data-dependent branching/looping, not "orchestration" broadly and not the Windows-4-step-vs-Linux-3-step platform difference itself. `[真机执行]` | [`orchestration/RESULTS.md`](./orchestration/RESULTS.md) | `orchestration/` → [`orchestration/RESULTS.md`](./orchestration/RESULTS.md) |
| **Q22** | **Assembled minimal reference core** — not a decisive experiment, an assembly. Twenty-one Qs each validated one part in isolation; does bolting six of them (interpret-as-floor, IR well-formedness gate, table-driven OS marshalling, `call`'s float `Kind` tag, content-addressed build-time-pinned adapters, linear step-table orchestration) into one binary actually run — and where do they conflict with each other, not just with a codegen path this build deliberately excludes? | **decided (assembly, not a Q-verdict)** | **Self-consistent and real, with one genuine new gap found by building, not hidden.** ① All payloads — the track's usual three plus a newly-added `FileWrite` intent — load from a content-addressed store, pass the Q19 well-formedness gate, and execute correctly on the real box; a corrupted IR is rejected pre-execution (`ExternIdOutOfRange`), reproduced twice. ② **F1 (the real finding): Q19's verifier checks IR-internal arity consistency only — it has no knowledge of the seam table's own arity contract.** A demo calling `FileWrite` with 1 of its 3 required args **passes `verify()` and then panics inside the interpreter** (index out of bounds), caught and reported rather than quietly patched — a cross-part gap no isolated experiment could have surfaced. Six lesser findings (F2–F7) also logged, including that Q7's op vocabulary had to be re-implemented rather than reused for interpreter dispatch. ③ Whole binary 219,136 B — directionally below Q16's 254,976 B compose binary (this one excludes both codegen lowerers), not divisible into a ratio (different capability sets). ④ `FileWrite`'s marginal cost, newly measured rather than cited: **38 LOC total (1 IR variant + 37 lines of pure step-table data), 0 lines of engine code, 0 new native-call wrappers** — `verify.rs`/`step_table.rs` needed zero changes, confirmed by grep. `[真机执行]` | [`assembled/RESULTS.md`](./assembled/RESULTS.md) | `assembled/` → [`assembled/RESULTS.md`](./assembled/RESULTS.md) |
| **Q23** | **Can a native intent's calling contract be declared at pack-LOAD time instead of baked into the interpreter at COMPILE time — while preserving F1-class verification strength (Q22's `IntentArityMismatch`, `verify()` checking the real call contract, not IR-internal consistency) and Q13's bake-and-detect?** I.e. can an agent teach the interpreter a Win32 API it did not ship knowing about, no recompile, without opening a blind-trust hole? §8 listed 3 unranked directions: (1) structured signature in the pack, (2) Q14-style behavioural probe, (3) the negative (keep human review). | **decided** | **Decisive SPLIT — the 3 directions decompose, they don't compete.** A never-shipped kernel32 export (`MulDiv`→15, `lstrlenA`→5, neither among nativecore's seven) **runs correctly from a runtime-parsed text pack over a generic ④ trampoline, zero recompile, zero codegen** (C1 PASS, real box; the mechanism has no `match symbol` — grep-verified, symbols are pure data). **The F1/arity half moves to load time cleanly — IFF *derived, not declared*** (the real finding): an under-provided IR is caught pre-execution (`IntentArityMismatch{provided:2,contract:3}`) **only** when the contract is DERIVED from the recipe the seam dereferences; a *separately-declared* arity field **re-opens F1 verbatim** (author controls both IR and declaration = single-author circularity — `verify_declared` PASSes a recipe that reads `Sem(2)` behind a declared arity 2, seam would read OOB), whereas `verify_derived` cannot disagree with the seam by construction — **strictly stronger than the compiled path**, which keeps two hand-synced constants. **But the ABI half does NOT move (kill line, honest negative):** a pack that declares the real 3-arg `MulDiv` as 1-arg is internally consistent, `verify()` PASSes, the trampoline calls it with 1 arg → garbage RDX/R8 → returns 1 not 15 — **nothing in a single-author data pack lets `verify()` know the true arity is 3** (Windows x64 exports carry no queryable ABI metadata; this is Q1-L2 ∩ Q14 residue, not a new hole). **`contract_arity()` never machine-verified this either** — it is a human who read the WriteFile contract; **what recompilation actually buys is an independent second-party signature assertion via git+review, not an F1 check.** Direction 2 (Q14 behavioural probe) recovers the binding at run-time (Tier B) for contract-bearing symbols — a mis-bound `lstrcmpiA`-as-strlen FIREs (got -1 ≠ 5) — with Q14's exact residue (no-contract symbols unprobeable) **plus a new bound: probe strength ≤ declared signature, so it catches wrong-bindings but not arity UNDER-declarations.** Concrete middle path named: a human-reviewed *signature registry* (sign the registry, not the binary) restores the second-party assertion without per-intent recompile. `[真机执行]` | (criteria/method/tree in RESULTS §0–§5) | `runtime-intent/` → [`runtime-intent/RESULTS.md`](./runtime-intent/RESULTS.md) |

**Provenance for every Q:** built from public technical knowledge only; no
prior/related implementation is read or referenced (clean-room, per
[`prd/PRD_02_14_research_provenance.md`](../../../prd/PRD_02_14_research_provenance.md)).

**Directory ownership:** Q1/Q2/Q3 run in parallel and each owns exactly one
subdirectory (`ir/`, `lowering/`, `reuse/`). Do not edit another Q's directory.
Q0's code sits at the top level of this directory.

---

# 技术清单 — the horizontal read

The question board above is **vertical**: one question, one verdict, one experiment.
This section is **horizontal**: one *class of technique* per line, with the number that
was actually measured and where it came from. It is the cross-cut of Q0–Q13, and it is
the thing you hand to someone who has to *choose* mechanisms rather than *settle* a
question.

**Provenance is mandatory and is the point of this section.** Every line carries one of:

- **[实测]** — a number produced by a Q in this track, on the host stated below, with a
  reproduce command in that Q's `RESULTS.md`. **It always carries an inline execution
  status** (next block) — `[实测]` alone is no longer a valid tag.
- **[转述未验]** — carried from [`plan/reference-cross-target-execution.md`](../../reference-cross-target-execution.md)
  (below: **R1**) and **never reproduced here**. R1's own §11 warns that its §2/§3/§5/§7/§8
  had **no working WebSearch/WebFetch** — those are model knowledge. R1 §4/§6 *were*
  checked against live primary sources on 2026-08-08; they are still **not our
  measurements**, and are marked **[转述·一手查证]**.

**No third category is allowed.** A transcription is never promoted to a conclusion.

**Execution status — mandatory suffix on every [实测].** `[实测]` was silently carrying two
very different things: *"this ran on a real machine"* and *"we cross-compiled it, measured
its bytes, and never ran it."* Q2's X = 3003 B was the latter while sharing a tag with Q8's
1655. The track-wide posture note below is correct but **evaporates the moment a single row
is quoted on its own** — and these rows *are* being quoted one at a time. So the qualifier
now rides **inline**:

| suffix | means |
|---|---|
| **[实测·真机执行]** | the artifact ran on the stated host and the number describes something that ran |
| **[实测·仅字节测量]** | cross-compiled / isolated and measured with a size tool; **that artifact was never executed** |
| **[实测·编码器验证]** | emitted machine-code bytes checked against an independent encoder/disassembler; not executed |
| **[实测·结构推断]** | established from source structure (LOC count, `grep` discipline check, schema check); no execution involved |

A row whose mechanism ran but whose **quoted bytes** come from an unexecuted build carries
both, e.g. **[实测·真机执行；字节仅测量]** — that combination is the normal case for every
"runs on Windows, bytes measured on the ELF-flat build" number in this track.

> **Host for every [实测] number unless stated otherwise:** Windows Server 2022
> Datacenter 10.0.20348 (real machine), x86_64, `rustc 1.97.0`, `-O`.
> **Linux/SysV artifacts and all aarch64 artifacts are byte-measured and
> encoder-validated, NOT executed** (no WSL, no ARM host) — track-wide posture, restated
> here so no row is read as "runs on Linux".

## ① Execution — how the payload's own logic runs

| technique | verdict | measured | provenance |
|---|---|---|---|
| **Interpretation** | **Required, and must be first-class** — not a patch bolted onto a JIT-shaped design | eval-core **55 LOC / 1908 B**, whole interpreter 136 LOC / **3177 B** (= 21% of the Q1 lowerer's 14819 B); **ISA-specific LOC in eval-core = 0** (grep-verified), which collapses Q5's per-ISA 307–350 LOC bucket to ≈0; **OS-bound payloads 1.0×**, compute-bound **≈77× vs optimized native** (≈5× vs Q1's naive lowering); runs the **byte-identical unchanged** Q1 IR — the IR is a *complete* interpretable backend surface, not a subset. **口径 (do not divide against Q2/Q10):** object `.text`, `x86_64-pc-windows-msvc`, **std + default panic**, and the 3177 B **includes** the OS intent seam (1269 B) | **[实测·真机执行；字节仅测量] Q9** ([`interp/RESULTS.md`](./interp/RESULTS.md)); the "must be first-class" follows from **[实测·真机执行] Q8** |
| **JIT / lowering to native** | **A conditional accelerator, not a floor** — it is the piece the platform can take away | X = **3003 B** flat-safe (in-kernel jump-table version ~2777) against a minimal-kernel baseline of **~2932 B** → **X ≈ the whole kernel**; in-kernel wins total delivery by ~1.5 KB, out-of-kernel halves the frozen TCB (~2.93 KB vs ~6.2–6.4 KB) — a real no-free-lunch tradeoff. Under ACG it is simply unavailable (see ⑥). **Two limits ride with this row:** (a) "X ≈ the whole kernel" is **cross-口径** — X is a flat-blob subtraction (no ELF header / entry / `mem*` intrinsics / primitive table), the baseline is a whole stripped binary (all included); the direction is conservative but the ratio is not one ruler (U7); (b) **the baseline's own 口径 was never documented** and is *reconstructed* in `lowering/RESULTS.md` ④ (two candidate readings 14 B apart) — the in/out-kernel TCB verdict rests on it, so it is held as **reconstructed, not recorded** (N2) | **[实测·真机执行；字节为 Linux/ELF 仅测量] Q2** ([`lowering/RESULTS.md`](./lowering/RESULTS.md)) + **[实测·真机执行] Q8** ([`platform/RESULTS.md`](./platform/RESULTS.md)) |
| **copy-and-patch stencil** | **Measured, and it does not pay at this scale.** R1 §10.1 ranked it #1 of the untried techniques on the argument that it dissolves the Q2 tradeoff. **It does not** — X *grew* | The pure `memcpy` + relocation applier **is** tiny as predicted (**651 B**) — but that was never the expensive part: **opcode decode/dispatch survives as code and dominates** (`emit` 3541 B). Whole runtime code **4515 B**, whole footprint code+data in Q2's own口径 **5826 B ≈ 1.94× Q2's 3003 B**, frozen TCB in-kernel ~8.7 KB vs Q2's ~6.2 KB. **Both endpoints of the tradeoff move up; its shape is preserved.** Bigger than Q2's hand-written lowerer — **this half is a like-for-like comparison** (Q10 built to Q2's 口径 on purpose). **The comparison against Q9's interpreter (3177 B) has been withdrawn**: 5826 B is a `no_std`/`panic=abort` flat-blob subtraction *excluding* the OS seam, 3177 B is a std/default-panic object `.text` *including* it — the axis is **口径-incomparable, i.e. not measured** ([`COMPARABILITY.md`](./COMPARABILITY.md) U2). All three payloads execute byte-identically, so the numbers rest on a working mechanism. Boundaries found: control flow **will not stencilize** (a stencil cannot leave CPU flags live across its boundary; a branch target is a layout-time offset) → ~20 B of residual encoder; `CALL` arity is structural, not a hole → per-`argc` variants; stencil table scales **opcodes × ISAs** | **[实测·真机执行；字节仅测量] Q10** ([`stencil/RESULTS.md`](./stencil/RESULTS.md)); the hypothesis it refutes is **[转述未验] R1 §7.4/§8.1/§10.1** |
| **AOT (pre-lowered native, no runtime codegen)** | **Trivially available**, and it is the one codegen route that survives a hardened process — at the cost of being one-shot | Q0's variant-B payloads *are* precompiled flat native blobs, mapped and jumped, executed on Windows. Q8-T1 measured that memory flipped to RX **before** ACG is enabled still executes **after** — i.e. "lay all the code down before the lock" works, but you can never generate more | **[实测·真机执行（Windows）；Linux 列仅字节测量] Q0** ([`RESULTS.md`](./RESULTS.md)) + **[实测·真机执行] Q8 J5-T1** |

## ② Reach — getting to code that already exists

| technique | verdict | measured | provenance |
|---|---|---|---|
| **Symbol resolution** (`GetProcAddress` / `dlsym`) | **First-class and constantly available** — the one reach mechanism nothing has taken away | ACG (`ProhibitDynamicCode`) does **not** touch `LoadLibraryA`/`GetProcAddress`; with ACG on, the probe keeps calling kernel32 exports and printing normally. It needs no executable memory | **[实测·真机执行] Q8 J6** |
| **FFI — invoke any address from a data description of its args** | **The load-bearing wall.** With ③+④ the kernel implements *nothing* and still covers new capabilities — but only over the integer/pointer word subset | Three capabilities of divergent shape (mmap-file arity 7, dir arity 2, socket arity 3) added **0 kernel bytes** and did **not** force ④'s arity ceiling (11) up — max observed native arity **7**. Q0's 7→11 was a **one-time step, not a per-capability slope**. **Boundary — narrowed by Q20, not uniformly closed.** The original read ("float/SIMD, struct-by-value, varargs, `sret` are shape limits ④ cannot express at any arity — out of scope, not solved") is now imprecise. Q20 **closed float** (an `Int`/`Float` `Kind` tag riding the *same* ABI-placement axis Q1 already proved deferrable, ~12–16 LOC, no new primitive; `sqrt`/`pow`/`ldexp` run on the real box, including the float-then-int positional case a naive port would silently break) and **closed struct-by-value ≤ register width** (0 extra LOC — runs through the unmodified baseline once the caller bakes field order, falling into the known offsetof-class residue rather than a new hole; `PtInRect` runs both directions). **Left open, honestly:** struct-by-value > register width / `sret` (reasoned closeable via the same constant-injection mechanism, but no real system API needing it was found after searching kernel32/user32/gdi32/msvcrt/common POSIX libc — not forced closed without evidence); **varargs** splits — closed for any finite/static shape, permanent for genuinely runtime-determined shapes (the IR's `Call` arity is static at authoring time). SIMD stays out of scope by design | **[实测·真机执行；字节仅测量] Q6** ([`primitives/RESULTS.md`](./primitives/RESULTS.md)); exec-memory independence **[实测·真机执行] Q8 J6**; shape boundary narrowed by **[实测·真机执行] Q20** ([`shape/RESULTS.md`](./shape/RESULTS.md)) |
| **Raw syscall** | **Split the primitive: this half has a completely different foundation from symbol resolution** | Windows: unused by construction — Q0's kernel `raw_syscall` returns −1 and the Windows path goes entirely through ③-symbol + ④. Linux: the adapters' only reach mechanism (**byte-measured, not executed** — no WSL) | **[实测·真机执行] Q8 J6 / Q0** for the Windows half; Linux half **byte-measured only** |
| — the OpenBSD hazard | If true, raw syscall issued **from generated code** kills the process (`msyscall`/pinsyscalls, 7.3+) — which would make the "raw-syscall primitive + emit it into JIT memory" design fatal-by-construction there | **not measured — no OpenBSD host.** Q8 files it explicitly as an unverified transcription | **[转述未验] R1 §7.1**, restated as unverified in Q8's credibility table |

## ③ Description — the class the four-primitive model made invisible

| technique | verdict | measured | provenance |
|---|---|---|---|
| **`Declare` / layout query** (`GetSystemInfo`, `RtlAddFunctionTable`, Linux BTF) | **A real, distinct concern — mechanically not a new primitive, conceptually yes.** The diagnosis: **reaching an *address* ≠ reaching a *description*.** `sym` resolves symbol→address; **no amount of `sym` answers `offsetof`**, and nothing in ①②③④ produces an offset | Q6 ran all three capabilities with **baked** offsets (`WIN32_FIND_DATAA.cFileName`@44, `sockaddr_in.sin_port`@2, `SYSTEM_INFO.dwPageSize`@4 — baked *even to read the host's own answer*). Kernel cost of promoting Declare to a uniform in-kernel query channel = **+182 B `.text`** (550→732), and it is **avoidable → 0** if left as a ③+④ usage pattern + a payload-side baked table. **Claim R of §1.1 ("nothing unreachable") is falsified** for the layout class | **[实测·真机执行；字节仅测量] Q6 ②③**; the query-form requirement independently reached by **[实测·结构推断] Q7 L3a**; the CO-RE/BTF mechanism itself is **[转述·一手查证] R1 §4.1e** |
| **Is a wrong bake detectable?** (the `Declare` follow-up) | **只能烤，但可检测 (bake-only, but detectable).** Windows has **no runtime `offsetof` oracle**, so baking is unavoidable — **but a wrong bake is DETECTABLE, converting Q6's silent per-target trust into explicit fail-fast**. `GetSystemInfo` publishes machine-fact *values*, yet each value sits behind an offset the host does **not** publish (**bootstrap circularity**: you need the layout to read the layout's own answer) | All three of Q6's baked facts fired a ③+④-only self-check on a **deliberately corrupted offset** on the real box (constructive filename read-back; socket `bind`+`getsockname` round-trip → WSAEAFNOSUPPORT 10047; `GetSystemInfo` cross-field), at **~18–38 LOC/fact, +0 kernel bytes** (pure payload-side). Detection has a **strength gradient** — constructive/round-trip airtight *modulo naming*, cross-field **weak** (bootstraps from *other* baked offsets; it caught a real 32-bit-vs-x64 offset bug live). Against Q4 the trust set shrinks **{naming + layout} → {naming}** — layout exits the hole, modulo naming | **[实测·真机执行] Q13** ([`declare/RESULTS.md`](./declare/RESULTS.md)) |
| **Cost of not doing it** | Layout constants get **baked into the artifact**; the pre-Q13 failure mode was a **silently wrong offset, not an error**. **Q13 narrows this**: only the *no-semantic-round-trip* residue stays silent-trust — write-only-unvalidated flags, unpredictable-read (`WIN32_FIND_DATAA.ftCreationTime`), loosely-validated `cb` (`STARTUPINFOA.cb=104`); everything with a round-trip is now fail-fast | Q7's oracle is a **stub** (`FieldSrc::Queried` carries the answer inline) — the finding is "L3a needs a query channel", **not** "here is the oracle". Where a host publishes nothing machine-readable (Windows system structs) the fact is **irreducibly baked** — but a wrong bake is **detectable** wherever the field participates in an API round-trip | **[实测·真机执行] Q6 ②** + **[实测·结构推断（stub）] Q7 deviation 3** + **[实测·真机执行] Q13 ④** |

## ④ Distribution & reuse

| technique | verdict | measured | provenance |
|---|---|---|---|
| **Content addressing** | Dedup + version coexistence + **no registry, no anointing** — at a cost that is **O(1) in adapter count** | Loader mechanism = **+609 B** in-kernel over the Q0 embed loader (Linux, the clean number); **+1648 B** more to *verify* content on load. Two payloads sharing a file adapter: CA 1058 B vs baked 1274 B at N=2, saving **linear in N**. Incompatible v1/v2 coexist by hash and **both run**. Boundary: it dedups **bytes, not behavior**, and provides **no name→hash discovery** — the one place anointing could re-enter | **[实测·真机执行；字节为 Linux/ELF 仅测量] Q3** ([`reuse/RESULTS.md`](./reuse/RESULTS.md)) |
| **Table-driven marshalling** (OS-interface content as data) | For the **single-native-call family**, capability growth becomes **pure data** over a fixed engine | **The slope is the product, and it is clean:** marginal **code** cost of +1 intent = **0**, of +1 same-ISA target = **0**; all growth is **data** (~5–13 LOC/intent, ~**57–58 LOC/target**). Engine = **70 LOC fixed** single-call core (+42 L3a struct-building, +47 schema types → ~70–112 fixed LOC). **The head-to-head against Q1's ~90–110 LOC/target has been restated, because the two do not cover the same capability set:** Q7's engine handles the **single-native-call family only (5 intents)** and **cannot lower `SpawnWait` at all** (Q7 ⑤), while Q1's 90–110 LOC/target **includes exactly that spawn struct-building / fork sequence**. Like-for-like, **inside the single-call family**: Q7 = fixed 70–112 LOC engine **+ 57–58 LOC/target of data** vs Q1 = **90–110 LOC/target of code** — and it is the *slope*, not that intercept pair, that carries the finding. Discipline is structurally checked: `grep -c 'abi.name ==' marshal.rs` → **0** (no per-target branch); `match .*intent` hits **only a doc comment**. Emitted bytes ≤ Q1 (Win64 rhp 1216 vs 1249). Executes | **[实测·真机执行；斜率为结构推断] Q7** ([`tables/RESULTS.md`](./tables/RESULTS.md)) |
| — where it stops | **L3b orchestration/control flow** ✅ **refined by Q21, not reversed.** It splits cleanly: a **linear-dataflow half** (every step always runs, only argument *values* vary) **does tabify** — Q21 built and ran a real zero-`if` data-only step table (`ArgSrc` enum, grep-verified) driving the live 4-step Windows `SpawnWait` chain end to end (`cmd.exe /c exit 7` → exit code 7 read back through data, `hProcess` extraction folding into argument resolution). What stays **irreducibly code** narrows to one root cause, three symptoms: **conditional transfer of control keyed on a runtime value unknown at table-authoring time** (`fork`'s pid branch, argv's runtime length, sentinel-driven branching) — demonstrated by execution, not asserted (the same table pointed at a nonexistent binary has no field able to express "skip Wait/GetExitCode", silently reports exit 0). Plus **I2 cross-ISA restructuring** (aarch64 has no `open`/`fork`: the syscall *set* changes shape, not just its number). Forcing the branching half into data = inventing a call-sequencing bytecode = **the IDL slide** | `spawn_boundary()`: tablable-as-data **1 — now built and run, not just classified**; needs-query-channel **2**; irreducibly-code narrows from **5 (undifferentiated)** to **specifically data-dependent conditional control transfer**. Also unchecked by any schema: whether symbol index 1 *really is* `CreateFileA` — the naming *binding truth* stays trust at produce time (Q14 later shows it is Tier-B verifiable at run time for contract-bearing symbols) | **[实测·结构推断] Q7 ⑤ + [实测·真机执行] Q21**; I2 folded in from **[实测·仅字节测量＋编码器验证] Q5**, analyzed not executed |
| **Relocation / flat-blob discipline** | A flat, non-relocated blob that is itself a **code generator** carries constraints Q0's precompiled payloads never had: it must be **memset-free and jump-table-free** | `llvm-objdump` located a real `jmpq *%rcx` jump table in the emit path; suppressed with `-C llvm-args=-min-jump-table-entries=200` plus taking scratch from primitive ① instead of `memset`. **Measured cost: +8% on X** (2777 → 3003 B) and a more fragile build | **[实测·真机执行；字节为 Linux/ELF 仅测量] Q2** (spec §8 decision trace; build scripts carry the flag) |
| — placement reach | **Real, and hit in practice.** `R_X86_64_PC32` holes silently truncate beyond **±2 GB**; a "give me N bytes" memory primitive that does not guarantee proximity **fails silently, not loudly** | Q10's applier had to co-locate code / register file / const pool / *a copy of the env table* in **one arena** (flipping only the code sub-range to RX) so every rip-relative hole stays in reach. **This confirms R1 §10.3: primitive ① is missing a placement-constraint parameter.** The aarch64 `CALL26` ±128 MB half remains **[转述未验]** (no ARM host) | **[实测·真机执行] Q10 ④** for the x86 PC32 half; **[转述未验] R1 §7.4/§10.3** for the rest |

## ⑤ Verifiability

| technique | verdict | measured | provenance |
|---|---|---|---|
| **Structural equivalence guard + diverse double-compilation** | **A construction gate, not an after-the-fact check** — and it is nearly free | **~55 LOC** of guard (riding on ~60 LOC of region instrumentation coextensive with the lowerer's own control flow); **0 extra bytes in the artifact**; needs **no execution** and no second OS. A mutated Neutral byte makes `build` return `Err` and yields **no runnable bytes** (negative test passes) | **[实测·结构推断] Q4** ([`equiv/RESULTS.md`](./equiv/RESULTS.md)) — the gate is a build-time construction check; **it needs no execution by design** |
| — why a whole-image `memcmp` cannot be the invariant | It passes **only** for `pure_compute`; it would reject every real payload | The strict **Neutral** regions *are* byte-identical across targets (246 / 676 / 623 bytes) — but the shared path also contains **frame size (M1)** and the **entry ctx register (M2)**, which are **baked ABI facts** and byte-*divergent* while behaviourally trivial. Hence: compare **by region** (Neutral = bytes, Control = target label, Frame/CtxReg/Intent = quarantined). **Byte-identity is strictly stronger than equivalence even inside the shared core** | **[实测·仅字节测量] Q4** (a Q4-new finding Q1 did not surface) |
| — the ceiling | Structurally unverifiable fraction = the intent regions = Q1's L1–L5: **0% pure → ~30–41% file-I/O → ~45–56% spawn**. Below that: after-the-fact differential testing (Tier B), collapsing to Tier C where a target is un-runnable or (L5) where equivalence **cannot even be stated** neutrally | **[实测·结构推断] Q4 ②④** |
| **Run-time policy enforcement** (the interpreter as a control point over agent-produced code) | **A real structural advantage — on the wrong layer.** Cheap and genuine at the *instruction* layer; **exactly zero past the intent boundary**, which is where the danger actually is | Interpreter refuses all five instruction-layer classes (bounds / step limit / alloc budget / OS-call surface / direct taint) and each succeeds with checks off; taint is **partial** — it is washed out by a table lookup (`hextab[nibble]`), so `read_hash_print` still writes. **Beyond the intent boundary control = 0**: `spawner`'s `cmd.exe /c exit 7` is **not an IR value** (rodata 0 B, extern nargs 0) — it lives in the seam *below* the IR; allow the intent and the interpreter sees only `Ok(7)`. Cost **1.47×** on `pure_compute`, eval-core **1908 → 2935 B (+54%)** — cheap. **vs JIT:** intent-boundary policies are **O(1) on both** (no JIT disadvantage, and those are the ones that matter); per-instruction policies are O(1) shared code vs **O(ops) emitted guards** or a load-time verifier. Merged with Q4: build-time guard and run-time control point **stop at the same place and leave the same hole** (L1–L5) | **[实测·真机执行；字节仅测量] Q15** ([`policy/RESULTS.md`](./policy/RESULTS.md)); the eBPF verifier contrast is **[转述·一手查证] R1 §4.1b/§4.1f** |

## ⑥ Platform-imposed — not chosen, must be handled

| technique | verdict | measured | provenance |
|---|---|---|---|
| **Three ways to obtain executable memory** (`RW→RX` flip / direct RWX / section object) | **Primitive ② must be polymorphic.** Under default policy all three work; under ACG **all three die** | Default `DynamicCode` policy reads **0x0** (ACG is **opt-in**): M1/M2/M3 each jump in and return 42. With ACG on: M1 fails at `VirtualProtect(→RX)`, M2 at `VirtualAlloc(RWX)`, M3 at `MapViewOfFile(FILE_MAP_EXECUTE)` — **all three, all `1655 = ERROR_DYNAMIC_CODE_BLOCKED`** (code from this machine's own `winerror.h`). Two conditional windows measured: pre-ACG RX survives (one-shot AOT), and `AllowThreadOptOut` + per-thread opt-out restores the flip | **[实测·真机执行] Q8 J1–J5**. This **corrects R1 §7.1**, which says ACG cuts *two* paths |
| — consequence | On an externally-imposed ACG process (and, if R1 is right, iOS), the only general legal path left is **interpretation** | Q8 → Q9: interpretation is measured at 3177 B and 1.0× on OS-bound work — the fallback is affordable | **[实测·真机执行] Q8 §对四原语模型的影响** + **[实测·真机执行；字节仅测量] Q9** |
| **The landing (second) gate — CET-IBT / `ENDBR64`** | **Not biting on this box, and no runtime toggle to force it.** The second gate's forward-edge check needs CET hardware this machine does not have | `CPUID.7.0` reports **CET_SS=0 / CET_IBT=0** — the silicon has **no CET at all** (not "supported-but-off"); CFG & shadow-stack policy off. Our three products carry **0 `ENDBR64`** (scanned Q2/Q9/Q8 binaries); Q2's only indirect targets (entry `transmute+entry()`, callback `call [r15+idx*8]`) have no landing pad — yet **all four jump patterns (P0/P0b/P1-entry/P2-callback) jump in and return 42**. **Methodology limit worth stating:** Windows exposes **no runtime forward-edge-IBT switch** (unlike ACG), so this cannot be "turned on and re-tested" here — the empirical jump-test is the judgement, not docs | **[实测·真机执行] Q12 ①②** ([`landing/RESULTS.md`](./landing/RESULTS.md)); ARM BTI / arm64e PAC equivalents **[转述未验] R1 §7.2** |
| **The landing (second) gate — ±2GB placement (`call rel32`)** | **Real, and the worst kind: MEASURED silent — no crash, no error, wrong callee.** Q2 is structurally immune; a switch to copy-and-patch detonates it | A >2GB `call rel32` (3GB delta) truncated via Q2's verbatim `as i32` with **no emit error** and **silently returned the wrong decoy callee (99, not 42), no crash**. **Q2 is structurally immune** — it never emits a PC-relative reference to an external symbol (OS calls go through absolute-indirect `call [mem]`; rel32 only for intra-buffer jumps). **copy-and-patch is not** — Q10 actually hit it and had to co-locate code / regfile / const-pool / a copy of the env table in **one arena**. (This is the execution-jump form; the rip-relative data-hole form is ④'s placement-reach row) | **[实测·真机执行] Q12 ③** + **[实测·真机执行] Q10 ④**; the aarch64 `CALL26` ±128 MB half **[转述未验] R1 §7.4** |
| **The landing (second) gate — Windows x64 unwind registration** | **Latent, not biting.** Generated frames register no unwind info, but current payloads never unwind through them | `RtlLookupFunctionEntry` on Q2-generated code = **NULL** (no `RUNTIME_FUNCTION`). UB only if an SEH/C++ exception unwinds *through* a generated frame; our payloads exit or return normally, so it does **not** fire today. Hardening ≈ **~40 LOC + ~34 B/function** (deferrable) — or make the frame a leaf (Q2's prologue pushes 5 regs, so that needs vreg-alloc rework) | **[实测·真机执行；硬化代价为估算、未实现] Q12 ②④**; the publish-side *registration* itself is still only Q6's **stub** — **[实测·结构推断（stub）] Q6 deviation 4** |
| **The landing (second) gate — I-cache coherence** | **Free on x86.** | Every hand-assembled stub executed correctly immediately after being written in the same thread, **no `IC/DSB/ISB` barrier** — x86_64 keeps I/D coherent in hardware. The ARM protocol (each *other* thread needs its own `ISB`) is untestable here | **[实测·真机执行] Q12 ②** for x86; ARM `IC/DSB/ISB` **[转述未验] R1 §7.2** |
| — interpretation's answer to all four second gates | **Structurally immune to every one** — no generated bytes to land on / flush / unwind / relocate. The hardest of interpretation's structural advantages, after Q9's size & ISA | Proven by source **and measured under ACG**: the clean-room match-interpreter computes `((7*191)^0xABCD)<<3 = 358304` correctly with **zero executable pages**, while the Q2-style codegen path in the *same* process dies at **1655** at gate 1. The ENDBR/unwind/placement work codegen must add, interpretation adds **zero** | **[实测·真机执行] Q12 ⑤** |
| **Still not handled by any experiment** | ARM BTI / arm64e PAC landing pads & the ARM `IC/DSB/ISB` cross-thread I-cache protocol (no ARM host); TLS access models; code retirement; the **publish side** of unwind — a real `RtlAddFunctionTable` registration (vs Q12's *detection* that the frame is unregistered) and the leaf-function exemption | **nothing measured** on these. Q6's Publish half is an explicit **stub** (call shape only, not a real unwind registration) | **[转述未验] R1 §7.2/§10.5**; stub status **[实测·结构推断（stub）] Q6 deviation 4** |
| **Other platforms' policies** (Linux `MemoryDenyWriteExecute` / `MFD_NOEXEC_SEAL`, macOS `MAP_JIT` + entitlement, iOS "no legal path at all", OpenBSD `wxallowed`) | If iOS is as described it is the one **hard** gap; everything else looks policy-configurable | **not measurable here** (no WSL, not macOS/iOS/OpenBSD). Q8 keeps every one of these as an unverified transcription rather than folding them into its verdict | **[转述未验] R1 §7.1**, listed row-by-row in Q8's credibility table |

## ⑦ In the space, not yet used by us

Everything in this group is **[转述未验]** or **[转述·一手查证]** — *no* line here has a
number of ours behind it. It is here so the space is visible, not so it can be cited.

| technique | why it is on the list | provenance |
|---|---|---|
| **Binary translation** (QEMU TCG, Rosetta 2, FX!32) | The survivors all **avoid** the OS axis rather than solving it (jacket into native libs / sit on an already-ported OS). The one project that really carried a foreign OS surface (Lx86) died of it. Directly usable idea: **persist the lowering result instead of redoing it every load** — three independent convergences (FX!32, Windows XTA cache, Rosetta 2) | **[转述未验] R1 §3.1–3.9, §10.8** |
| **eBPF-style verifier + load-time JIT** | The only shipping system whose constraints resemble ours. Also the clearest "you cannot have both": kernel `verifier.c` = **20,065 lines**, three orders of magnitude over our kernel; userspace eBPF runtimes simply **do not** reimplement it (rbpf's `verifier.rs` = 13 KB) | **[转述·一手查证] R1 §4.1b/§4.1f** (kernel source + GitHub, checked 2026-08-08) |
| **CO-RE / load-time binding against a host description** | The shipping answer to "keep layout out of the payload": payload carries *name + type + relocation record*, loader carries the **layout oracle**, offsets are computed at bind time and folded in. Q7 reached the same shape from the other direction but built **no oracle** | **[转述·一手查证] R1 §4.1e**; our half **[实测·结构推断] Q7 L3a** |
| **"Take the intersection, not the union"** | eBPF's 11 registers are the **intersection of real 64-bit ABIs**, not a neutral choice — deliberately sacrificing neutrality (11 regs, 32-bit zero-extension) buys a per-arch JIT of **~4k lines** instead of a register allocator, and pushes edge architectures onto the interpreter. We have never priced this option | **[转述·一手查证] R1 §4.1a/§10.7** |

---

# 执行层判决 — the three landing routes, judged

The 技术清单 above is *"which techniques exist."* This section is the newer, harder
question the landing experiments (Q10 stencil / Q12 landing gate / Q13 declare) forced:
**three execution routes were built and run to the metal — what is the verdict?** One
four-axis table.

| route | size (**mind the 口径 — see note**) | ISA cost | gate 1 — get executable memory | gate 2 — will the hardware run it |
|---|---|---|---|---|
| **interpret** (Q9 / Q12) | **1908 B** eval-core / **3177 B** whole interpreter — *口径 Q9: object `.text`, msvc, std + default panic; the 3177 **includes** the OS intent seam (1269 B)* | **0** (ISA-specific LOC in eval-core = 0, grep-verified) | **not needed** — never asks for RX memory | **structurally immune** to all four second gates (nothing generated to land on / flush / unwind / relocate) |
| **JIT lowering** (Q2 / Q12) | **3003 B** — *口径 Q2: flat-PIC blob subtraction (code+data), ELF, no_std + `panic=abort`; **excludes** the OS layer (env table / adapters are "not in X"); **this artifact was never executed** — the executed Q2 route is the Windows PE build (7680 / 9216 B, 512-aligned)* | **307–350 LOC/ISA** (Q5: aarch64 307, x86-64 350) | **cut by ACG** — all three exec-memory routes → 1655 | hardening = **+4 B/entry (ENDBR)** and **~34 B/generated function (unwind, deferrable)** of *emitted* bytes; the ~40 LOC of encoder this adds to the lowerer itself is **not measured** |
| **stencil** (Q10 / Q12) | **5826 B** (code+data) — *口径 Q2, deliberately identical, so this one **is** directly comparable to 3003 B* | data + an irreducible per-ISA code residual | **cut by ACG** (same `VirtualAlloc`/`VirtualProtect` path — structural, *not* separately measured under ACG) | **placement detonates on contact** — the ±2GB `PC32` hole; Q10 hit it and had to arena-co-locate |

> **The size column is not one ruler.** Only the JIT and stencil cells share a 口径
> (Q10 built to Q2's flat-blob measurement on purpose — 5826 vs 3003 = 1.94× **is** a
> valid comparison). The interpreter cell is measured differently on three axes at once
> (object `.text` vs flat subtraction; std vs `no_std`+`panic=abort`; **with** vs
> **without** the OS intent layer), so it **must not** be divided against the other two.
> Full boundary table and the recommended unified 口径: [`COMPARABILITY.md`](./COMPARABILITY.md).

**Four limits that MUST travel with this table**, or the section will be read as
over-claiming:

1. **Interpretation's price is compute-density ≈77×** (Q9): OS-bound payloads are **1.0×**,
   but hot compute inner loops run **≈77×** slower than optimized native. The route wins
   the ISA axis and both gate axes *only with this one hard caveat attached*.
2. **Immunity here is a usability argument, not a security argument** — and the security
   question has since been measured separately, **with a split answer**. Q9 measured only
   size / speed / coverage; Q12 measured that the interpreter still *runs* on a hardened
   platform where codegen is blocked (see R1 §12.3 B7). **Q15 then measured safety
   directly: the advantage is real but on the wrong layer.** At the *instruction* layer the
   interpreter is a free O(1) control point (bounds / halting / budget) where the JIT pays
   O(ops) guard bytes or a load-time verifier; but at the **intent boundary — where the
   actual danger lives (what the OS or the child process does after the call) — both routes
   are O(1) and identical, and neither reaches past it.** So this table's "immune" column
   still must **not** be read as "safer": on the danger surface, interpretation has **no**
   safety advantage over JIT.
3. **The single-ISA size axis is UNDECIDED, not decided either way.** The three routes'
   byte numbers were produced under **incompatible 口径** (table note above), so no
   route may be called "the smallest" or "not smaller" on raw bytes. Within the one
   valid pair, stencil (5826 B) **is** 1.94× Q2's JIT (3003 B). Everything else on this
   axis needs a re-measurement in one 口径 — see [`COMPARABILITY.md`](./COMPARABILITY.md) §2 U1/U2.
4. **Two of the three size cells describe artifacts that were never executed.** Q2's 3003 B
   and Q10's 5826 B are **Linux/ELF-flat cross-compiled byte measurements** (no WSL on this
   box); what actually ran, for both, is the Windows PE build of the same mechanism. Q9's
   3177 B is an object-file `.text` — the interpreter itself ran, that object did not. **All
   three mechanisms are execution-verified; none of the three quoted byte numbers is.** And
   Q2's kernel baseline, the denominator of "X ≈ the whole kernel", is **reconstructed rather
   than documented** (`lowering/RESULTS.md` ④, COMPARABILITY N2/U7).

**Verdict — the default framing is inverted.** "JIT as the main path, interpretation as
the fallback" is turned over by three *independent* measurements: interpretation is
**ISA-free** (0 vs 307–350 LOC/ISA — the lowerer's *bulk is* the per-ISA encoder the
interpreter simply lacks), **needs no executable memory** (survives ACG at gate 1), and is
**structurally immune to every second gate**, while the two codegen routes grow *larger the
more platform they must satisfy* — stencil, the fullest codegen form, is **larger than the
JIT it was supposed to shrink (5826 vs 3003 B, same 口径)** and detonates on placement.
**The inversion rests on ISA-scaling + the two platform gates, and on nothing else** — in
particular it does *not* rest on single-ISA byte count, which is not comparably measured
(limit 3). So: **interpretation is the floor; JIT is the conditional accelerator you add
back where the ≈77× compute tax is worth paying and the platform still permits codegen** —
holding the 77×, the un-measured security question, and the un-comparable size axis as the
three standing caveats.

---

# Q0 — 1 layer vs 2 layer, measured

Builds one dynamic core two ways — **1 layer** (mechanism + platform adaptation
fused into one artifact) and **2 layer** (a frozen minimal kernel + a replaceable,
runtime-loaded payload) — across **two operating systems** (Linux and Windows,
x86_64), and measures six numbers. The whole point is the numbers in
[`RESULTS.md`](./RESULTS.md), not the code.

**One-line conclusion:** the decisive-by-design metric ③ (marginal cost of adding a
second OS) is **a tie** — adding Windows grows the existing OS binary by **0 bytes** in
*both* variants. The metric the first run deferred, **④ (marginal cost of +1 capability),
is the one that actually separates them**: a non-user of a new capability grows **0** in
2-layer (new capability = a separate blob) but **+~0.4 KB/capability** in a true
single-product 1-layer. ④ is a *slope* criterion, which §3 ranks above the ② size
intercept, so on the experiment's own priority it tips the balance to **2-layer** (joined
by ⑤ TCB and ⑥ coexistence); 1-layer's only remaining edge is raw ② size. Caveat found:
the capability forced a one-time completion of the ④ `call` primitive (7→11 args) that
grew both variants' kernels. See `RESULTS.md` §④ and the §4 decision trace.

> **Two honest readings survive.** By §4's literal decision tree (which never listed ④
> as a node) it is marginally 1-layer on the ② byte tiebreak; by §3's stated priority
> (slopes outrank intercepts) it is 2-layer. The tree/priority mismatch is a **bug in the
> spec**, recorded rather than papered over — and it is why the skill above insists on
> reconciling the decision tree against the stated priority order.

## The kernel — four primitives, nothing else

```
① memory   mem_alloc (reserve+commit RW) / mem_protect (RW <-> RX)
② jump     load a code blob and transfer control (the variant-B loader)
③ reach    raw_syscall (Linux) + sym = symbol resolution (Windows GetProcAddress)
④ call     invoke any native address from a data description of its args (libffi model)
```

No cross-platform *semantics* live in the kernel — no `open()`, no portable file model.
Platform differences are carried by adapters/payloads. File I/O is done by *calling the
platform's own functions* through ③/④, never by a kernel abstraction.

## Layout

```
core/abi.rs        the ONLY kernel<->payload contract (primitive table); panic handler
core/kernel.rs     the four primitives, Linux + Windows, cfg-gated; loader; entry
                   (④ call: arg ceiling 7->11, raised for spawn's CreateProcessA — §④)
adapters/linux/    read_file/write via raw syscalls; spawn via fork/execve/wait4
adapters/windows/  read_file/write via GetProcAddress+FFI; spawn via CreateProcessA+wait
payloads/pure_compute/     floor payload (no OS)
payloads/read_hash_print/  total-delivery payload (read -> FNV-1a/64 -> print)
payloads/spawn_echo/       +1 capability (§④): spawn child, wait, report exit code
pack/variant_a_onelayer/   static-link everything into one binary; fused.rs = the
                           single-product model (all capabilities in one) for ④'s (b)
pack/variant_b_twolayer/   frozen kernel/loader + flat PIC payload blobs
build/                     build_linux.sh, build_windows.ps1, flat.ld
out/                       build outputs (git-ignored)

ir/                        Q1 (separate spec/owner)
lowering/                  Q2 (separate spec/owner)
reuse/                     Q3 (separate spec/owner)
```

## Build & reproduce

```sh
rustup target add x86_64-unknown-linux-gnu
rustup component add llvm-tools            # for rust-lld / flat-blob extraction
bash research/dynamic-core/build/build_linux.sh
```
```powershell
pwsh research/dynamic-core/build/build_windows.ps1
```

Each script prints the artifact sizes and writes them to `out/`. Correctness-verification
commands (Windows) and the independent reference hash are in `RESULTS.md`.

## What Q0 does NOT do

The first run stopped at ③ per the §4.4 time-box; a **follow-up run added criterion ④**
(one capability: spawn a subprocess) and nothing else — still **no macOS, no second ISA,
no optimization.** Everything else it deliberately left open became Q1–Q6 above.
