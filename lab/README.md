# lab/

Scratch experiments that are **not** product code, **not** `research/` courts,
and **not** CI gates. Each subdirectory is a self-contained probe.

| Dir | Question |
|-----|----------|
| [`mmap-ephemeral/`](mmap-ephemeral/) | mmap mailbox + short-lived worker vs resident (no listen socket) |
