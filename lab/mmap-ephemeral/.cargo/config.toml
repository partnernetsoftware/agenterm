# Force the system linker. A PATH `cc` shim (Claude Code wrapper) breaks
# rustc/link and crate build-scripts on this host.
[target.aarch64-apple-darwin]
linker = "/usr/bin/clang"

[target.x86_64-apple-darwin]
linker = "/usr/bin/clang"
