// Recursion: arguments and results through the function ABI, repeatedly, with
// suspended callers holding their own values live.
function fib(n) {
  if (n < 2) { return n; }
  return fib(n - 1) + fib(n - 2);
}
function main() {
  return fib(12);
}
